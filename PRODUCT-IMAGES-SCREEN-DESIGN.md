# صور المنتج — شاشة مستقلة لإدارتها

> **الحالة: أُجيب عنه ويُبنى الآن (٢٠٢٦-٠٨-٢٣).** أُجيب عن س١ وس٢ وس٣ صراحةً؛ وس٤ وس٥ تُركا بلا
> جواب فأُخذ فيهما الاقتراح المكتوب، وكلاهما قرار واجهة يُقلب بسطر.

## ١. السؤال

المنتج يُصوَّر مرة واحدة — عند إنشائه — ثم لا سبيل إلى صورته أبداً. من يفتح «تعديل المنتج» لا يجد
منتقي صور، وشاشة التفاصيل تعرض المعرض للقراءة فقط. النتيجة أن صورة خاطئة أو قديمة تبقى إلى الأبد،
والمنتج الذي أُنشئ قبل أن تصير الصورة إلزامية يبقى بلا صورة إلى الأبد كذلك.

هذا نقص مقصود ومكتوب، لا سهو: `_needsImage => _editing == null` في
`product_form_page.dart`، ومعه سببه — التبديل عمليتان على الـ API، وتنفيذهما من نموذجٍ قد لا
يُضغط زر حفظه أصلاً يغيّر المنتج قبل أن يلتزم المستخدم بشيء. والبند مسجَّل في
[BACKLOG.md](BACKLOG.md) تحت «تبديل صورة المنتج من شاشة التعديل».

## ٢. القرارات المتَّخذة

| السؤال | القرار |
| --- | --- |
| صورة واحدة أم عدة؟ | **عدة صور لكل منتج** — أُجيب عنه في ٢٠٢٦-٠٨-٢٣ |
| صورة لكل مقاس؟ | **لا، ليس الآن.** تبقى في `BACKLOG.md` كما هي |
| أين تُدار؟ | **شاشة مستقلة**، على نمط `customer_designs_page` — لا داخل نموذج التعديل |
| من يقرأ | `products.view` |
| من يرفع ويحذف ويرقّي | `products.manage` — وهي قاعدة الخادم أصلاً على النقاط الثلاث |

### لماذا شاشة مستقلة وليست حقلاً في النموذج

النموذج يجمع تغييرات ويكتبها دفعة واحدة عند «حفظ». الصور لا تعمل هكذا: الرفع طلبٌ يُنفَّذ فوراً
ويعيد سجلاً بمعرّف، والحذف كذلك. وضعهما في نموذجٍ يعني أن الضغط على «رجوع» بدل «حفظ» يترك المنتج
وقد تغيّرت صوره فعلاً — أي نموذج يكذب على مستخدمه. الشاشة المستقلة تجعل كل عملية تلتزم لحظة وقوعها،
وهذا هو سلوكها الصادق.

## ٣. الـ API — جاهز بالكامل، ولا يحتاج سطراً واحداً

هذا أهم ما في هذه المواصفة: **العمل كله في الواجهة.** الخادم يقدّم منذ زمن ثلاث نقاط، كلها تحت
`can:products.manage` و`scoped()` — فصورة منتج آخر تعطي 404 بلا فحص إضافي:

| العملية | النقطة | ملاحظات |
| --- | --- | --- |
| رفع | `POST /products/{id}/images` | حقول: `image` (مطلوب)، `alt_text`، `is_primary` |
| ترقية/تسمية/ترتيب | `PATCH /products/{id}/images/{image}` | `is_primary` **للترقية فقط** — لا يقبل التنزيل، لأن المنتج يلزمه رئيسية دائماً |
| حذف | `DELETE /products/{id}/images/{image}` | يرفض بـ 422 لو كانت الصورة الوحيدة |

ثلاثة قيود تفرض شكل الشاشة، وكلها من تصميم الخادم لا من رأينا:

- **لا توجد نقطة فهرسة.** `->only(['store','update','destroy'])` — الصور تصل داخل
  `GET /products/{id}` نفسه. فالشاشة تقرأ الصور من المنتج، وتعيد تحميل المنتج بعد كل عملية ناجحة.
- **الملف لا يُستبدل داخل السجل.** «تغيير الصورة» ليس عملية؛ هو رفعٌ ثم حذف، **بهذا الترتيب**.
  عكسه يترك المنتج بلا صورة لو فشلت الثانية، والخادم يمنعه أصلاً برفض حذف الأخيرة.
- **الحذف ينزع الملف من القرص فعلاً.** السجل يُحذف حذفاً ناعماً كبقية النماذج، لكن الملف يذهب بلا
  رجعة — لهذا لا يُعرض «تراجع» عن الحذف، ولهذا يسبقه تأكيد.

## ٤. الشاشة

`ProductImagesPage` على المسار `/products/:id/images`، مبنية بمعرّف المنتج مع اسمه كـ `extra`
ليقول الشريط لمن هذه الصور بلا طلب ثانٍ — كما تفعل `customer_designs_page` حرفاً بحرف.

- **شبكة مربعات** بالصور، الرئيسية أولاً (الخادم يرتّبها: `is_primary` ثم `sort_order` ثم `id`)،
  وعلى الرئيسية وسمٌ يقول «الرئيسية» — وسمٌ بكلمة، لا إطارٌ ملوّن وحده.
- **الضغط على صورة** يفتحها بالحجم الكامل، على نمط `design_viewer`.
- **زر عائم «إضافة صورة»** يفتح `showAttachmentSheet` ثم `AttachmentPicker` — نفس مسار التصاميم،
  ونفس الفحص القبْلي قبل فتح المنتقي.
- **على كل صورة** إجراءان لمن يملك `products.manage`: «اجعلها الرئيسية» (تختفي على الرئيسية نفسها)
  و«حذف». من لا يملكها يرى الشبكة بلا أي زر — غياب، لا تعطيل.
- **الحذف** يمرّ بـ `showDestructiveDialog` كقاعدة المشروع، ويقول إن الملف يُحذف نهائياً.
- **حذف الصورة الأخيرة** مرفوض من الخادم بـ 422. الشاشة تقول ذلك قبل الطلب — «لا يمكن حذف الصورة
  الوحيدة؛ ارفع بديلاً أولاً» — لأن رحلة إلى الخادم لتُرفَض رحلة كان يمكن توفيرها.

المدخل إليها من شاشة تفاصيل المنتج؛ أيّ شكل بالضبط هو **س٤** أدناه.

## ٥. القيود ومرآتها في التطبيق

`media.product_images` على الخادم اليوم: **٥ ميجابايت**، والصيغ `jpeg, jpg, png, webp`.
لاحظ الفرق عن التصاميم: **لا حدّ لعدد الصور على المنتج** (التصاميم محدودة بخمسين لكل عميل) — وهذا
موضوع **س١**.

يُكتب `ProductImageRules` على غرار `DesignRules` حرفاً بحرف: نسخة من أرقام الخادم، ومعها
`reject(PickedFile)` تردّ سبب الرفض بكلمات الخادم نفسها قبل أن تُرفع بايت واحدة — على وصلة موبايل
ليبية، رفع ٦ ميجابايت لتُقابَل بـ 422 دقيقةٌ من وقت شخص وحصّته من الإنترنت، أُنفقت لمعرفة ما كان
معروفاً فوراً. ويلزمها ما يلزم أختها: **اختبار عقد يقرأ ملف الـ PHP ويسقط عند الاختلاف**، على نمط
`design_rules_contract_test.dart` — وهو وحده ما يجعل تكرار رقمٍ في مكانين دفاعاً عنه.

`svg` غائبة عن القائمة، ويجب أن تبقى غائبة: الـ SVG مستند HTML، وواحدٌ يُخدَم من نطاقنا هو
XSS مخزَّن.

## ٦. ما لا يتغيّر

- **نموذج المنتج ولا سطر منه.** «الصورة مطلوبة عند الإنشاء» يبقى كما هو: منتج بلا صورة لا يُنشأ.
- **`ProductImage` في الواجهة** يحمل أصلاً كل ما تحتاجه الشاشة — `url` و`isPrimary` و`sortOrder`
  و`altText` والمقاس والحجم بتسمياتهما العربية. لا تعديل على النموذج.
- **`ProductGallery` و`ProductThumbnail`** يُعاد استعمالهما كما هما؛ لا يُكتب ثانٍ يرسم الشيء نفسه.
- **الباك إند بالكامل** — إلا إن أجاب **س١** بفرض حدٍّ أقصى.

## ٧. الأسئلة — اكتب جوابك تحت كل واحد

### س١ — حدٌّ أقصى لعدد الصور على المنتج الواحد؟

اليوم لا حدّ إطلاقاً. عميلٌ له خمسون تصميماً حدٌّ مقصود ومكتوب، أما المنتج فمفتوح — ومنتجٌ عليه
أربعون صورة يجعل `GET /products/{id}` ثقيلاً ويجعل شبكة الشاشة بلا نهاية. فرض حدّ يعني تعديلاً
صغيراً في الباك إند (`max_per_product` في `config/media.php` + تحقّق في `UploadProductImageRequest`)
وهو الشيء الوحيد في هذه المواصفة الذي يمسّ الخادم.

اقتراحي: **٨ صور**. تكفي كيساً من كل زاوية ولوناً من كل نوع، وتُبقي المنتج خفيفاً.

**الجواب: ٥ صور.** أضيق من المقترح، وهذا هو الأثر الوحيد لهذه المواصفة على الخادم:
`media.product_images.max_per_product` وتحقّق في `UploadProductImageRequest`. يُرفض السادس بـ 422
برسالة عربية تقول العدد، والتطبيق يقول الشيء نفسه قبل أن يفتح المنتقي أصلاً.

### س٢ — إعادة ترتيب الصور سحباً، أم يكفي «اجعلها الرئيسية»؟

الخادم يقبل `sort_order` في `PATCH`، فالترتيب ممكن بلا عمل خادمي. لكن السحب في شبكة على الهاتف
شاشةٌ بحد ذاتها، والفائدة الحقيقية غالباً هي «أيّها تظهر في الكتالوج» — وتلك هي الترقية وحدها.

اقتراحي: **الترقية فقط الآن**، والسحب في `BACKLOG.md`.

**الجواب: الترقية فقط، والسحب إلى `BACKLOG.md`.** فلا تُرسل `sort_order` من هذه الشاشة إطلاقاً،
ويبقى ترتيب الخادم هو الترتيب.

### س٣ — هل نسأل المستخدم عن نصّ بديل (`alt_text`)؟

الخادم يقبله ويصل إلى الواجهة. لكنه حقلٌ يظهر بعد اختيار كل صورة، ولا يقرأه في هذا التطبيق أحد —
لا قارئ شاشة يُعتدّ به بين المستخدمين، ولا موضع يُعرض فيه النص.

اقتراحي: **لا نجمعه**، ويبقى فارغاً كما هو اليوم.

**الجواب: لا يُجمع.** الحقل يبقى في الـ API ولا ترسله الشاشة — فيوم يظهر موضع يقرأه، يُملأ بلا
هجرة ولا تغيير عقد.

### س٤ — من أين تُفتح الشاشة، وماذا يبقى في التفاصيل؟

ثلاثة أشكال:

1. المعرض في التفاصيل كما هو، وتحته زر «إدارة الصور».
2. المعرض نفسه يصير قابلاً للضغط، فيفتح الشاشة.
3. الشاشة من قائمة إجراءات المنتج (الشريط العلوي)، والمعرض للعرض فقط.

اقتراحي: **الأول** — الزر يقول إن الشاشة موجودة، والمعرض يبقى يفعل ما يفعله.

**الجواب: تُرك بلا جواب، فأُخذ الاقتراح (الأول).** زرٌ تحت المعرض في شاشة التفاصيل. تغييره إلى
الشكل الثاني أو الثالث سطرٌ واحد في `product_detail_page.dart` يوم يُطلب.

### س٥ — «استبدال» بضغطة واحدة، أم رفع وحذف منفصلان؟

سؤال «أريد تغيير هذه الصورة» له جوابان في الواجهة: زرٌ واحد «استبدال» ينفّذ الرفع ثم الحذف
متسلسلَين، أو تركُ الأمر عمليتين ظاهرتين يقوم بهما المستخدم.

الزر الواحد أريح، وخطره أن ينجح نصفه: تُرفع الجديدة ثم يفشل الحذف، فيبقى المنتج بصورتين ولا شيء
يقول ذلك. ولأن الرفع أولاً، فأسوأ فشلٍ ممكن هو صورة زائدة — لا منتج بلا صورة.

اقتراحي: **عمليتان ظاهرتان الآن.** الشاشة تعرض الصور والزرَّين، والمستخدم يرفع البديلة ثم يحذف
القديمة — خطوتان مفهومتان، بلا حالة نصف ناجحة تحتاج شرحاً.

**الجواب: تُرك بلا جواب، فأُخذ الاقتراح.** لا زر «استبدال» — رفعٌ وحذفٌ ظاهران. ومع حدّ الخمسة
(س١) يصير الترتيب مفروضاً على أي حال: من بلغ الخمسة يحذف قبل أن يرفع.

## ٨. ما تُرك خارج هذا التغيير

- **صورة لكل مقاس** — تبقى في `BACKLOG.md` بقرارها المكتوب: `variant_id` عمود nullable على
  `product_images` نفسه، لا جدول ثانٍ. المستخدم أكّد في ٢٠٢٦-٠٨-٢٣ أنها ليست أولوية.
- **إعادة الترتيب سحباً** — تابعة لجواب **س٢**.
- **استبدال الملف داخل السجل نفسه** — ليس قراراً لنا: الخادم يرفضه عمداً كي لا يبدأ رابطٌ سُلّم
  فعلاً بالإشارة إلى محتوى مختلف.
