# مدفوعات الطلبية — المواصفة

> **مُنفَّذة.** كل ما دونه أدناه هو ما بُني فعلاً — الجدول والـ API والصلاحيات والشاشة والربط.
> الأسئلة التي سبقت البناء وأجوبتها في [§١٠](#١٠-الأسئلة-وأجوبتها).
> ما يُؤجَّل يُسجَّل في [BACKLOG.md](BACKLOG.md) بسببه.

---

## ١. لماذا

الطلبية اليوم تعرف **كم تساوي** ولا تعرف **كم قُبض منها**. الواقع أن العميل يدفع عربوناً عند الطلب، ثم يكمل عند الاستلام، وبينهما قد يدفع دفعة ثالثة. هذا الفراغ يُسدّ اليوم بورقة خارج النظام.

المطلوب ثلاثة أرقام على الطلبية، وسؤال واحد وراءها: **كم بقي له علينا، وكم بقي علينا له.**

هذا البند كان مؤجَّلاً في [BACKLOG.md](BACKLOG.md) تحت «الدفع»، وقد حان وقته. البند نفسه اقترح عمودين على الطلبية (`payment_status` و`paid_amount`)؛ **المقترح هنا يذهب إلى سجل كامل بدلهما**، للسبب في §٢.

---

## ٢. القرار المحوري: سجل، لا عمود

المطلب الصريح: **«المعاملات المالية مهمة ويجب توفير سبيل للتراجع عليها وعكسها.»**

عمود `paid_amount` يُزاد ويُنقص لا يمكن التراجع عليه، لأنه لا يحفظ ما جرى — يحفظ حصيلته. من كتب ٥٠٠ بدل ٥٠ لا يجد ما يعكسه، ومن سأل «متى دفع العربون وبكم؟» لا يجد جواباً.

فالمدفوعات **سجل يُضاف إليه ولا يُعدَّل**، تماماً كسجل المخزون (`stock_movements`) الذي في هذا المشروع أصلاً ونصّه: «لا شيء يعدّل حركة أو يحذفها، والتصحيح حركة تالية — سجلٌّ يمكن إعادة كتابته لا يشرح شيئاً».

ومنه تُشتقّ الأرقام الثلاثة. **لا يوجد رقم مدفوع يُكتب بيد أحد.**

### أربعة أنواع من القيود، لا نوعان

| النوع | ماذا يعني | أثره |
| --- | --- | --- |
| `payment` — **دفعة** | العميل دفع مالاً | `+` على المدفوع |
| `reversal` — **إلغاء قيد** | القيد كان **خطأً**، لم يحدث أصلاً | `−` من مجموع القيد الذي يلغيه |
| `refund` — **ردّ مبلغ** | المال **رجع للعميل فعلاً** | `−` من المدفوع |
| `write_off` — **شطب فرق** | المال **لن يأتي**، وأُقفل الدَّين بقرار | `+` على المشطوب، ولا يمسّ المدفوع |

**التفريق بين الأخيرين مقصود وليس ترفاً.** كلاهما ينقص المدفوع بنفس المقدار، لكن «موظف كتب رقماً غلطاً» و«أرجعنا للعميل عربونه بعد إلغاء الطلبية» حدثان مختلفان تماماً: الأول لا يُروى في تقرير الصندوق لأنه لم يقع، والثاني حركة نقدية حقيقية خرجت من الدرج. لو جُمعا في نوع واحد لصار «كم رددنا للعملاء هذا الشهر؟» سؤالاً بلا جواب.

**إلغاء القيد يشير إلى قيده** (`reverses_payment_id`)، فالصفّان يُقرآن معاً ويظهر الخطأ وتصحيحه في مكان واحد. **وفهرس فريد يمنع إلغاء نفس الدفعة مرتين** — القاعدة تضمنه، لا تحقّقٌ في الكود يسبقه طلبٌ متزامن (قاعدة `RULES.md` §٨).

**ردّ المبلغ لا يشير إلى دفعة** — قد يردّ العميلُ مبلغاً جُمع من ثلاث دفعات، وإجباره على اختيار واحدة منها كذبٌ في السجل.

**والرابع أُضيف بعد أن اصطدم الواقع بالحارس** — طلبية بـ١١٠ رجع مالها ١٠٥ فبقيت عالقة لا تُسوَّى ولا تُخصم. تفصيله في [§١٢](#١٢-شطب-الفرق--الخمسة-دنانير-التي-لم-ترجع). **وإلغاء القيد يُلغي الشطب كما يُلغي الدفعة**: كلاهما دعوى قد تكون خاطئة (رقم مطبعي، أو شطبٌ على الطلبية الخطأ)، ولا نقد تحرّك في أيّهما ليُستعاد. أما الردّ فلا يُلغى — المال خرج فعلاً.

---

## ٣. الجدول

`order_payments` — الدفعة لا حياة لها خارج طلبيتها، كالبنود والتصاميم، فهي في سياق `Order` نفسه.

| العمود | النوع | ملاحظة |
| --- | --- | --- |
| `id` | bigint | |
| `order_id` | FK · cascade | |
| `type` | string(20) | `payment` \| `reversal` \| `refund` \| `write_off` — `OrderPaymentType` |
| `amount` | decimal(12,2) | **دائماً موجب.** الاتجاه في النوع، لا في الإشارة — نفس نهج `stock_movements.quantity` |
| `method` | string(20) nullable | `PaymentMethod` — أربع قيم — [§١٠ س٢](#س٢--طرق-الدفع) |
| `reference` | string(100) nullable | رقم الإيصال أو الحوالة |
| `receipt_disk` · `receipt_path` · `receipt_original_filename` · `receipt_size_bytes` · `receipt_checksum` | nullable | **الواصل** — انظر §٣.١ |
| `paid_at` | timestamp | **متى تحرّك المال**، لا متى سُجِّل. يفترض «الآن» ويقبل تاريخاً سابقاً — تُسجَّل دفعة أمسِ صباحَ اليوم |
| `notes` | text nullable | |
| `reverses_payment_id` | FK self nullable | يُملأ في `reversal` وحده. **فريد جزئياً** فلا تُلغى دفعة مرتين |
| `recorded_by` | FK users nullable | يُختم من المستخدم الموقَّع، **غير قابل للإرسال** — فلا ينسب أحدٌ قبضاً لغيره |
| `timestamps` + `deleted_at` | | القاعدة الثابتة: soft delete + سجل تدقيق |

**قيد الشكل (`order_payments_shape`)** يذكر الأشكال الثلاثة التي توجد فعلاً، ومنها الرابع الذي أُضيف لاحقاً:

| النوع | `reverses_payment_id` | `method` |
| --- | --- | --- |
| `payment` · `refund` | فارغ | **مطلوبة** |
| `reversal` | **مطلوب** — القيد الذي يُلغيه | فارغة |
| `write_off` | فارغ | فارغة — لم يتحرّك مال |

**الفهارس:** `(order_id, paid_at)` للسجل على الشاشة، و`(type, paid_at)` لتقرير الصندوق لاحقاً.

**لا `updated_at` يعني شيئاً هنا** — لا شيء يعدّل صفاً. العمود موجود لأن كل جدول هنا يحمله.

### ٣.١ الواصل — إجباري مع الحوالة، PDF أو صورة

**الكاش يُرى على الطاولة، والبطاقة تترك أثراً في المصرف، والحوالة لا تترك إلا ورقة يرسلها العميل.** فحوالة مسجَّلة بلا واصل هي كلمة موظف مقابل كلمة عميل يوم يختلفان — ولهذا الملف **جزء من القيد** لا مرفقٌ يُلحق لاحقاً.

**PDF أو صورة (JPG / PNG / WEBP)** — نفس أشكال تصميم العميل. بدأت القاعدة «PDF وحده» بحجة أن الواصل مستند يُصدره المصرف، لكن الواصل الذي يصل فعلاً هو لقطة شاشة من تطبيق المصرف أو صورة تُبعث على واتساب — ورفض الصور كان رفضاً للإثبات الذي يملكه المحل حقاً. قُلبت القاعدة بطلب صاحب العمل (2026-08-22). و`svg` غائبة عمداً كما في التصاميم: ملف SVG وثيقة HTML، وتقديمه من نطاقنا XSS مخزَّن.

**الامتداد المخزَّن يُقرأ من البايتات المتشمَّمة لا من اسم العميل** — صورة JPEG باسم `waseel.pdf` تُخزَّن `.jpg`. ويَنشر الـ API حقل `receipt_is_image` محسوباً من الملف المخزَّن، فيعرف التطبيق أيرسم الواصل بنفسه أم يسلّمه للهاتف — دون أن يحمل نسخة من قائمة الصيغ.

القاعدة مكتوبة **ثلاث مرات عن قصد**، وكل نسخة تفعل شيئاً لا تفعله الأخريان:

| أين | ماذا تعطي |
| --- | --- |
| `PaymentMethod::requiresReceipt()` | تسري على أمر كونسول ومستورد لاحق، لا على الـ API وحده |
| `required_if` في الـ FormRequest | الـ 422 المقروء تحت الحقل نفسه |
| `CHECK` في القاعدة | الضمان — لا مسار يكتب صفاً يخالفها |

ويُضاف إليها رابعةٌ في التطبيق: **حقل الملف يظهر لحظة اختيار «حوالة»**، فيُقال للموظف إن الورقة ناقصة والعميل ما زال أمامه — لا بعد الضغط على «حفظ».

الملف على **قرص خاص** (`MEDIA_RECEIPTS_DISK`، افتراضه `local`) كتصاميم العملاء وعكس صور المنتجات: الواصل يحمل بيانات مصرفية، والقرص العام يوزّع روابط دائمة بلا مصادقة. و`receipt_url` **يُبنى مع كل طلب ولا يُخزَّن**، فهو رابط موقَّع ينتهي.

**اسم الملف مولَّد لا مُرسَل.** عميلان يبعثان `receipt.pdf` يجب ألّا يتصادما، ولا أحد يختار مساراً — اسمٌ من العميل هو محاولة اجتياز مجلدات تنتظر من يجرّبها.

### وعمودان على `orders`

```php
$table->decimal('paid_amount', 12, 2)->default(0)->after('grand_total');
$table->decimal('written_off_amount', 12, 2)->default(0)->after('paid_amount');
```

**لماذا عمود مخزَّن وقد قلنا إن الأرقام تُشتقّ؟** لأن قائمة الطلبيات تفرز وتفلتر عليه، وحسابه بجملة فرعية لكل صف هو N+1 بثوب SQL. يُعاد حسابه من السجل في **نفس المعاملة** التي كتبت القيد، بفعل `RecalculateOrderPayments` — على نمط `RecalculateOrderTotals` القائم حرفياً. فهو ذاكرةٌ للسجل لا مصدرٌ ثانٍ للحقيقة، واختبارٌ يثبت أنه يساوي مجموع السجل دائماً.

**والثاني ذاكرةٌ للسجل نفسه، بمشية واحدة**: `RecalculateOrderPayments` يمرّ على القيود مرةً ويوزّعها على المجموعين — والقيد الذي يُلغي شطباً يعود على المشطوب لا على المدفوع، وهو ما لا يعرفه الصفّ وحده ([`OrderPayment::affectsWriteOff()`](backend/app/Domain/Order/Models/OrderPayment.php)). ولماذا عمودان لا عمود؟ [§١٢](#١٢-شطب-الفرق--الخمسة-دنانير-التي-لم-ترجع).

### ولا عمود `payment_status`

الحالة **تُحسب ولا تُخزَّن**: `paid_amount + written_off_amount` مقابل `grand_total`.

السبب عملي: `grand_total` يتغيّر حين تتغيّر البنود أو يُمنح خصم. عمود حالة مخزَّن كان سيلزم `RecalculateOrderTotals` بتحديثه أيضاً، وأول مسار ينسى ذلك يترك طلبية «مدفوعة بالكامل» وقيمتها ارتفعت. المشتقّ لا يعرف هذا الخطأ.

| الحالة | متى |
| --- | --- |
| `unpaid` — غير مدفوعة | لا مقبوض ولا مشطوب |
| `partially_paid` — مدفوعة جزئياً (عربون) | `0 < المقفَل < grand_total` |
| `paid` — مدفوعة بالكامل | `المقفَل = grand_total` ولا شطب فيه |
| `written_off` — مشطوب فرقها | `المقفَل = grand_total` وفيه شطب — [§١٢](#١٢-شطب-الفرق--الخمسة-دنانير-التي-لم-ترجع) |
| `overpaid` — زائدة | `المقفَل > grand_total` — انظر §٥ |

> **«المقفَل» = `paid_amount + written_off_amount`.** ما يُنهي الدَّين شيئان: مالٌ قُبض، ومالٌ تقرّر ألّا يُقبض — ويبقيان عمودين لأن الفرق بينهما هو المقصود.

---

## ٤. الأرقام الثلاثة

```
المتبقي = grand_total − paid_amount − written_off_amount
```

**عمودان يُقفلان الدَّين، لا عمود واحد.** ما قُبض نقداً، وما تقرّر ألّا يُقبض — ويبقيان منفصلين لأن الفرق بينهما هو بيت القصيد: من يسأل «كم قبضنا؟» يجب ألّا يُعطى رقماً فيه مالٌ لم يصل. انظر [§١٢](#١٢-شطب-الفرق--الخمسة-دنانير-التي-لم-ترجع).

**والشاشة تبقى بثلاث خانات.** الرابع صفرٌ في كل طلبية تقريباً، وخانةٌ رابعة كانت ستُصغّر الثلاثة من أجله؛ فيُقال في سطرٍ تحتها حين يوجد — «منه مشطوب ٥٫٠٠ — لن يُحصَّل».

وتُرسَل مع كل طلبية في `OrderResource` كنصوص، كبقية أرقام المال:

```json
"grand_total": "450.00",
"paid_amount": "150.00",
"written_off_amount": "0.00",
"remaining_amount": "300.00",
"payment_status": "partially_paid",
"payment_status_label": "مدفوعة جزئياً",
"has_unrecorded_money": false
```

`has_unrecorded_money` هو ثمن قرار [س١](#س١--هل-تكتب-تم-التسوية-قيد-دفعة-تلقائياً-٨) معروضاً لا مخفياً — طلبية نهائية بقي عليها مال. انظر §٨.

**المتبقي يُحسب على الخادم ويُرسَل، ولا يُطرح في الهاتف.** نفس قاعدة `OrderTotals` في الفلاتر: «إجمالي يُجمَّع على الخادم ثم يُعاد اشتقاقه على الهاتف هو جوابان لسؤال واحد، وجواب الهاتف هو المصنوع من `double`».

---

## ٥. القواعد التي يفرضها المجال

كلها استثناءات من `DomainException` — تُرمى ولا تُلتقط، بعربيّة تقول ما العمل.

1. **المبلغ أكبر من صفر.** دفعة بصفر ليست حدثاً.
2. **الدفعة لا تتجاوز المتبقي** → `PaymentExceedsRemaining`. يمسك الخطأ المطبعي (٥٠٠ بدل ٥٠) عند بابه.
   **لكن الزيادة الناشئة عن نقص الإجمالي لاحقاً مسموحة** — خصمٌ مُنح بعد دفعة كاملة يجعل المدفوع أكبر من الإجمالي دون أن يخطئ أحد. لذلك القاعدة على **لحظة التسجيل** لا على الأبد، والحالة `overpaid` موجودة لتُروى لا لتُمنع.
3. **الردّ لا يتجاوز المدفوع فعلاً** → `RefundExceedsPaid`. لا نُرجِع ما لم نقبض.
4. **لا دفعة جديدة على طلبية ملغاة** → `OrderIsCancelledForPayment`. أما **الردّ والإلغاء فمسموحان دائماً وفي كل حالة** — مالٌ قُبض يجب أن يبقى قابلاً للإرجاع، وطلبية ملغاة هي أكثر حالة يُردّ فيها العربون. **القفل الذي يمنع الاسترداد خطأ لا حماية.**
5. **القيد المُلغى لا يُلغى مرتين** → `PaymentAlreadyReversed`، والفهرس الفريد وراءه.
6. **لا يُلغى إلا `payment` أو `write_off`** → `EntryCannotBeReversed`. إلغاء إلغاءٍ متاهة؛ من أخطأ في الإلغاء يسجّل دفعة جديدة. والردّ لا يُلغى أيضاً: إعادة العميل للمال حدثٌ — أي **دفعة** — لا ادّعاء بأن الردّ لم يقع.
7. **الحوالة تتطلب واصلها** → `ReceiptRequiredForMethod`. انظر [§٣.١](#٣١-الواصل--إجباري-مع-الحوالة-pdf-أو-صورة).
8. **الشطب لا يتجاوز المتبقي** → `WriteOffExceedsRemaining`. لا يُشطب إلا ما هو مستحق؛ ومالٌ قُبض فعلاً يُخرَج بالردّ لا بالشطب. وعلى طلبية مدفوعة بالزيادة لا يُشطب شيء — تلك تحتاج ردّاً.
9. **لا شطب على طلبية ملغاة** → `OrderIsCancelledForWriteOff`. الملغاة لم تصل العميل فلا مستحق عليها، وشطبُها يكتب خسارةً على أكياس لم تُسلَّم.
10. **الشطب يطلب سبباً** — كالإلغاء تماماً، وهو القيد الوحيد الذي يحوّل نقصاً إلى خسارة. وتاريخه **لحظتُه ولا يُؤخَّر**: الدفعة تُؤرَّخ بيوم انتقال المال، والقرار يُؤرَّخ بيوم اتخاذه، وتاريخٌ يُكتب باليد هنا بابٌ لنقل خسارة الشهر إلى دفاتر الشهر الماضي.

---

## ٦. الصلاحيات

أربع، على نمط الكتالوج:

| الصلاحية | التسمية |
| --- | --- |
| `orders.payments.view` | عرض دفعات الطلبية |
| `orders.payments.record` | تسجيل دفعة على الطلبية |
| `orders.payments.reverse` | إلغاء دفعة أو ردّ مبلغ |
| `orders.payments.write_off` | شطب فرق مبلغ الطلبية |

**المال الخارج له صلاحيته وحده.** من يقبض من العميل ليس بالضرورة من يملك أن يُخرج من الدرج — وهذا أهم خطٍّ في الميزة كلها. القبض عملٌ يومي لموظف الاستقبال؛ الإلغاء والردّ قرارُ من يملك الصندوق.

> ولو أردت فصل الردّ عن الإلغاء في صلاحيتين، فهو سطر واحد — انظر [§١٠ س٤](#س٤--الصلاحيات).

**والشطب صلاحيته وحده، ولم يُضمّ إلى الردّ.** الردّ يُعيد مالاً في يد الشركة؛ الشطب يقرّر أن مالاً لها لن يصل — وهو القيد الوحيد من الأربعة الذي يحوّل نقصاً إلى خسارة. فهو لمن يُسأل عن الدفاتر، لا لكل من يفتح الدرج.

> **إلغاء قيد الشطب يبقى تحت `orders.payments.reverse`** كبقية الإلغاءات: من يُلغيه يُعيد الدَّين إلى الطلبية لا يُسقطه، وهو الاتجاه المحافظ.

**العرض منفصل عن `orders.view` عمداً:** من يطبع الأكياس يرى الطلبية ولا شأن له بمال العميل.

---

## ٧. الـ API

| الفعل | المسار | الصلاحية |
| --- | --- | --- |
| سجل دفعات الطلبية | `GET /orders/{order}/payments` | `orders.payments.view` |
| تسجيل دفعة | `POST /orders/{order}/payments` | `orders.payments.record` |
| ردّ مبلغ | `POST /orders/{order}/payments/refunds` | `orders.payments.reverse` |
| شطب فرق | `POST /orders/{order}/payments/write-offs` | `orders.payments.write_off` |
| إلغاء قيد | `POST /orders/{order}/payments/{payment}/reverse` | `orders.payments.reverse` |

**أربعة مسارات للكتابة لا مسار واحد بحقل `type`** — نفس قرار `stock-movements`: «للوصول أربع أشكال، فلكلٍّ نقطته بجسمه بدل مسار واحد يحمل مُميِّزاً وأربعة حقول اختيارية». الدفعة تطلب طريقة ومرجعاً، والإلغاء لا يطلب إلا سبباً، والردّ بينهما، والشطب مبلغاً وسبباً وحدهما — بلا طريقة ولا تاريخ، إذ لم يتحرّك مالٌ ليكون له أيٌّ منهما.

`write-offs` **تُعلَن قبل `{payment}`** كما `refunds`، وإلا حاول الربط الضمني قراءة الكلمة رقماً وأجاب 404.

`{payment}` **مقيَّد بالطلبية** (`scopeBindings`)، فدفعة طلبية أخرى تُرجِع 404 بحكم البناء لا بحكم تحقّقٍ يتذكّره أحد.

**التسجيل والردّ يُرسلان `multipart/form-data` دائماً**، بواصل أو بدونه. جسمٌ يتغيّر شكله حسب وجود ملف هو صيغتا طلب تُضبطان بدل واحدة، ولارافيل يقرأ الـ multipart بلا ملف كما يقرأ JSON.

**لا `PUT` ولا `DELETE`.** هذا هو الشرط الذي بُني عليه كل ما سبق.

**ولا مسار `/logs` للدفعة**: تاريخها جزء من تاريخ طلبيتها. تُضاف `OrderPayment` إلى `Order::auditTrailSubjects()` فيظهر كل قيد في `GET /orders/{order}/logs` مع بنودها وتصاميمها وانتقالاتها، وتُضاف حالة `order_payment` إلى `AuditSubject`.

**وفلتر على قائمة الطلبيات**: `payment_status` (قابل للتكرار كـ `status`)، فتصير «أرِني ما لم يُدفع» سؤالاً للخادم.

**وعدّادات على محورين** في `GET /orders/summary` (`payment_counts`) وفي `GET /home/summary` (`payments`، قائمةً بعربيّتها كـ`statuses`). المحوران يتقاطعان ولا يُدمجان: «جاهزة» لا تقول شيئاً عن الدفع.

> **العدّاد والفلتر يقرآن تعبير SQL واحداً** — `PaymentStatusExpression` — لأن لا عمود `payment_status` أصلاً. فلا يقف «غير مدفوعة ٧» فوق قائمة من أربعة، والقاعدة مكتوبة مرتين لا ثلاثاً: `PaymentStatus::between()` في PHP، والتعبير في SQL. واختبارٌ يمشي على الحالات الأربع مقابل الـ enum نفسه.

---

## ٨. التسوية والسجل — أهم نقطة تماسّ

هنا يلتقي المبنيّ بالجديد. السؤال طُرح وأُجيب → [§١٠ س١](#س١--هل-تكتب-تم-التسوية-قيد-دفعة-تلقائياً-٨).

اليوم «تم التسوية» تعني **أن مال الطلبية رجع من المندوب**، ويُملأ `collected_amount` وحده حين يختلف المُستلَم عن الإجمالي.

**والقرار: التسوية لا تكتب في السجل شيئاً.** لا يُسجَّل قبضٌ إلا بيد من قبضه. قيدٌ يخترعه الخادم نيابةً عن موظف هو أول كذبة في سجل بُني كلّه ليُصدَّق.

**ولا تُقفل طلبية على دَين.** «تم التسوية» و«غير مدفوعة» معاً كانتا تركيبةً يستطيع المحوران التعبير عنها ولا يستطيعها العمل: هي الوحيدة التي **تُنهي الطلبية وتُضيّع الدَّين في اللحظة نفسها**. فالحركة إلى «تم التسوية» تُرفض ما دام على الطلبية متبقٍّ — `ChangeOrderStatus` يسأل `PaymentStatus::isOutstanding()` قبل أن يكتب شيئاً، ويرمي `SettlementRequiresFullPayment` (٤٢٢) برسالة تسمّي المبلغ:

> لا يمكن نقل الطلبية إلى «تم التسوية» قبل تحصيل قيمتها — المتبقي ٣٠٠.٠٠

يُقرأ من `paid_amount` — مجموع السجل — لا من رقمٍ يرسله من يضغط الزر. والطلبية «مدفوعة بالزيادة» تمرّ: القاعدة عن مالٍ ناقص، لا عن مالٍ مطابق. وكذلك طلبية إجماليها صفر (استلام مكتب بلا توصيل): لا متبقّي عليها فلا شيء يمنعها.

**تُرفض ولا تُخفى.** الحركة تبقى معروضة في `available_transitions`، لأن من يراها هو نفسه من يصلحها: يسجّل الدفعة ثم يسوّي. زرٌّ يختفي بلا سبب يترك المحاسب يبحث عمّا لا يجده، والرسالة تقول له ما ينقص. هذا نفس منطق `ShortageNeedsAQuantity`: القاعدة التي يُصلحها المستخدِم في مكانه تُقال، ولا تُطبَّق بحذف الزر.

**والرسالة تسمّي البابين، لأنهما صارا اثنين:**

> لا يمكن نقل الطلبية إلى «تم التسوية» قبل تحصيل قيمتها — المتبقي ٥٫٠٠. سجّل ما قُبض، أو اشطب الفرق إن كان لن يُحصَّل

الباب الثاني هو [§١٢](#١٢-شطب-الفرق--الخمسة-دنانير-التي-لم-ترجع)، وقد فُتح لأن الأول وحده كان يترك طلبيةً واقعية عالقةً إلى الأبد.

**والفجوة لا تُترك صامتة حين تُفتح من الجهة الأخرى.** طلبية نهائية (`isFinal`) ومتبقّيها أكبر من صفر تحمل على شاشتها سطر تنبيه:

> ⚠️ الطلبية منتهية ولم يُسجَّل قبض ٣٠٠.٠٠

`hasUnrecordedMoney()` بقي على حاله ولم يعد يُشتقّ من تسويةٍ ناقصة — الحارس أعلاه يمنعها — بل ممّا يأتي **بعدها**: ردّ مبلغ على طلبية مسوّاة يُنقص `paid_amount` فيفتح المتبقّي ثانيةً، وطلبيات سُوّيت قبل هذه القاعدة تبقى كما سُجّلت. ولا يمنع شيئاً — يُري النقصَ لمن يصلحه.

**والعكس مرفوض عمداً:** دفعة تكمل المبلغ **لا** تنقل الطلبية إلى «تم التسوية». والاتجاهان ليسا متماثلين ولا يُفترض أن يكونا: الدفع شرطٌ للتسوية، لا سببٌ لها. محور الدفع مستقل عن محور التنفيذ — وهو سبب تأجيله أصلاً في `BACKLOG.md` — وطلبية مدفوعة سلفاً لم تُشحن بعدُ ليست مسوّاة.

---

## ٩. الفلاتر

### الأرقام الثلاثة — في ثلاثة أماكن

سطر واحد، ثلاث خانات متجاورة:

```
┌─────────────────────────────────────────────┐
│  سعر الطلبية      المدفوع       المتبقي      │
│    450.00         150.00        300.00      │
└─────────────────────────────────────────────┘
```

| أين | ماذا |
| --- | --- |
| **رأس تفاصيل الطلبية** | `OrderMoneyRow`، **بديلاً** عن «الإجمالي» المفرد لا فوقه |
| **بطاقة الطلبية في القائمة** | سطر ثالث تحت خط فاصل، مع شارة حالة الدفع بجانب شارة الحالة |
| **رأس شاشة الدفعات** | نفس الويدجت، فوق السجل |

الإجمالي وحده كان جواباً على سؤال، والثلاثة جواب على السؤال نفسه أكمل، وإبقاء الاثنين يكتب الرقم مرتين على شاشة واحدة.

**كل رقم يُرسم كما أرسله الخادم — والطرح منه.** «المتبقي» ليس `grand_total - paid_amount` يُحسب في الهاتف: ذلك جوابٌ ثانٍ لسؤال واحد، والثاني مصنوع من `double`. واختبارٌ يمرّر متبقّياً يخالف الطرح ويتأكّد أن الشاشة تعرضه كما هو.

**المتبقي بلون التنبيه ما دام أكبر من صفر**، وبلون عادي حين يصفر — الصفر بالأحمر يجعل الحالة السليمة تبدو هي المشكلة.

**وما شُطب يُقال في سطر تحت الأرقام، لا في خانة رابعة**: «منه مشطوب ٥٫٠٠ — لن يُحصَّل»، ولا يظهر السطر أصلاً حين لا يوجد شطب. وشارة الحالة تقول «مشطوب فرقها» بلونٍ محايد — لا الأخضر الذي يعني «قُبض كاملاً»، ولا الأحمر الذي يعني «مطلوب».

**وزرّ «شطب الفرق» بعرض الشاشة في سطرٍ وحده** تحت «تسجيل دفعة» و«ردّ مبلغ»، لا ثالثاً في صفّهما — هو الأندر من الثلاثة، وثلاثة عناوين في صفٍّ واحد على هاتف ضيّق تُصغّرها جميعاً. ولا يظهر إلا لمن يملك صلاحيته **وعلى طلبية عليها متبقٍّ**: زرٌّ لا يمكنه إلا أن يفشل أسوأ من غيابه.

**ونافذته حوارية لا ورقة**: حقلان قصيران — مبلغ يفتح على كامل المتبقي، وسبب — وفوقهما جملتان تمنعان استعماله خصماً: «يُقفل المتبقي دون تسجيل أي قبض — سعر الطلبية يبقى ١١٠٫٠٠ والفرق يُقيَّد في السجل خسارةً».

### شاشة «الدفعات» — لا قسم

كانت قسماً تحت «الحساب»، ولم تصمد: طلبية دُفعت على ثلاث دفعات بينها تصحيح هي ستة أسطر، كل سطر بطريقة ومرجع وتاريخ واسم — شاشةٌ من القراءة مدسوسة تحت كل ما تقوله الطلبية.

فصارت **شاشة تُفتح من الزر العائم** (`AppSpeedDial` → «الدفعات»)، فيها الأرقام في الأعلى، ثم الزرّان، ثم السجل يملأ الصفحة. وتُرجِع `true` إن كُتب شيء، فتُعيد الطلبية خلفها القراءة — لأن `paid_amount` من حمولة **الطلبية** لا من حمولة السجل.

والمسار محروسٌ بـ`redirect` على `orders.payments.view` كما هو محروس على الذراع، فرابطٌ عميق لا يتجاوز التحقّق.

**والقيد المُلغى يبقى ظاهراً مشطوباً** ومعه سبب إلغائه تحته؛ إخفاؤه يجعل السجل يكذب بالسكوت.

### الفلتر المتقدم — محوران

ورقة الفلتر صار فيها قسمان و«تطبيق» في أسفلها:

- **حالة الطلبية** — اختيار واحد، كما كانت.
- **حالة الدفع** — **صناديق تأشير**، لأن «أرِني ما لم يُدفع» تعني غير مدفوعة *و* جزئية معاً.

ثلاثة خيارات لا أربعة: «مدفوعة بالزيادة» ليست طابوراً يعمل عليه أحد صباحاً — انظر `PaymentStatus.filterable` — والطلبية الزائدة تقول ذلك على بطاقتها.

**والورقة صارت تُطبّق بزر بدل أن تُغلق على أول نقرة.** ذلك كان صحيحاً حين كان السؤال واحداً؛ ومع سؤالين، الإغلاق على الأول يجعل الثاني غير قابل للوصول إلا بفتح الورقة ثانية.

وبجانب كل صف **عدّاده**، من نفس تعبير SQL الذي يفلتر به الخادم (`PaymentStatusExpression`) — فلا يقف «غير مدفوعة ٧» فوق قائمة من أربعة.

### الرئيسية — لوحة ثانية

`PaymentBoard` تحت «حالات الطلبات»: ثلاث بطاقات بأعدادها، تفتح كل واحدة قائمة الطلبيات مفلترة على **محور الدفع** — فبطاقة «غير مدفوعة ١٢» تفتح كل طلبية غير مدفوعة أياً كانت مرحلتها، وهو ما عدّته.

لوحة منفصلة لا ثلاث بطاقات في لوحة الحالات، لأن المحورين يتقاطعان: طلبية قد تكون منتهيةً وغير مدفوعة، أو مدفوعةً ولمّا تُطبع.

**و«غير مدفوعة» وحدها تأخذ لوناً، وفقط حين تعدّ شيئاً.** لوحة الحالات تعلّمت هذا قبلها: علامةٌ تظهر وهي تعدّ صفراً هي تحذيرٌ من لا شيء، وتُعلِّم القارئ ألّا يصدّق العلامة.

### النموذج — أدوات التطبيق نفسها

- **`AppTextField`** لكل حقل، لا `TextFormField` عارٍ.
- **`AppDropdown<T>`** لطريقة الدفع — ويدجت **عامّة في `core/widgets` تقبل أي مودل**: تأخذ `items` و`labelOf`، وعند الحاجة `keyOf` لمودل بلا مساواة قيمية. لا تعرف شيئاً عن طرق الدفع، فالمنتقي التالي وسيطُ نوع لا ملفٌ جديد.
- **حقل المبلغ يفتح على كامل المتبقي** — وعلى كامل المدفوع في الردّ. أغلب الدفعات هي إقفال الطلبية، فصار الشائع نقرةً واحدة بدل رقم يُكتب في صندوق فارغ.
- **الفاصلة لا تُكتب أصلاً** (`FilteringTextInputFormatter`): `1,500` ألفٌ وخمسمئة عند من كتبها وواحدٌ ونصف عند غيره، ولا ذكاء لاحق يميّز. والأرقام العربية `٠-٩` مسموحة لأنها ما تنتجه لوحة المفاتيح، وتُحوَّل في طريقها إلى الـ API.

**كل هذا خلف صلاحية الخادم**، بنفس نهج `available_transitions`: لا نسخة ثانية من القواعد في Dart.

---

## ١٠. الأسئلة وأجوبتها

> سُئلت وأُجيبت. ما دونها هو المُنفَّذ.

### س١ — هل تكتب «تم التسوية» قيدَ دفعة تلقائياً؟ (§٨)

**سؤالك المقابل: «هل هنالك حالة للتسوية؟»** — نعم. `OrderStatus::Settled` باسم «تم التسوية»، وهي **الحالة الوحيدة التي تلي «تم الاستلام»** وآخر الخط (`isFinal`)، خلف صلاحية `orders.status.settled`. أُضيفت في migration `add_settlement_to_orders` ومعها `collected_amount` و`settled_at`.

**الجواب: لا.** التسوية لا تكتب شيئاً في السجل. لا يُسجَّل الدفع إلا **بيد موظف**، بعد أن تصير الطلبية «تم الاستلام».

فالسجل يقول ما قبضه موظف فعلاً، ولا يُخترع فيه قيدٌ لم يُقبض. وقرار المستخدم صريح: من يقبض هو من يسجّل.

**وثمنه مذكور لا مخفيّ:** طلبية تُسوَّى دون أن يُسجَّل قبضها تبقى «متبقٍّ عليها ٣٠٠» على الشاشة. فبدل قيدٍ مخترع، **يُظهر التطبيق التناقض**: طلبية نهائية ومتبقّيها أكبر من صفر تحمل سطر تنبيه «الطلبية منتهية ولم يُسجَّل قبض ٣٠٠». التنبيه يجعل النقص مرئياً لمن يصلحه، والقيد التلقائي كان سيخفيه.

### س٢ — طرق الدفع

**أربع، وهي المُنفَّذة:**

| القيمة | الاسم |
| --- | --- |
| `cash` | كاش |
| `bank_transfer` | حوالة |
| `bank_card` | بطاقة مصرفية |
| `libyana` | ليبيانا |

سقط الصكّ و«تحصيل عند التسليم» من الاقتراح — لم يُذكرا. **والطريقة إجبارية**: الأربع تغطّي كل ما يقع، فلا حاجة إلى قيمة «غير معروف» يهرب إليها من لم يسأل.

### س٣ — تسمية الخانة الوسطى

**«المدفوع» دائماً.** كلمة واحدة ثابتة، صادقة على الطلبية الناقصة والمكتملة معاً. «العربون» تبقى كلمة الناس، لا كلمة الشاشة.

### س٤ — الصلاحيات

**ثلاث يوم كُتبت، وأربع اليوم — كما في §٦.** الردّ والإلغاء معاً خلف `orders.payments.reverse` — كلاهما مالٌ خارج، وقرار إخراجه واحد. وأُضيفت `orders.payments.write_off` وحدها حين أُضيف الشطب: قرارُ أن مالاً لنا لن يصل ليس قرارَ إخراج مالٍ نملكه.

---

## ١١. خطة التنفيذ

بالتسلسل، كل خطوة باختبارها الفاشل أولاً (TDD · Arrange-Act-Assert · `RULES.md` §٦).

### شريحة أ — القاعدة والمجال

1. `OrderPaymentType` و`PaymentMethod` و`PaymentStatus` enums.
2. Migration `order_payments` + `orders.paid_amount` + الفهرس الفريد الجزئي.
3. `OrderPayment` model (`Auditable` + `SoftDeletes`) + factory + حالة في `AuditSubject`.
4. `RecalculateOrderPayments` + `RecordOrderPayment` + `RefundOrderPayment` + `ReverseOrderPayment` + `StorePaymentReceipt` actions، والاستثناءات السبع.
5. توسيع `OrderService` بأربع دوال — الباب الوحيد للسياق.

### شريحة ب — الـ API

6. الصلاحيات الثلاث في `PermissionName` + المسارات الأربعة.
7. `OrderPaymentController` + FormRequests + `OrderPaymentResource`.
8. حقول المدفوعات في `OrderResource` + فلتر `payment_status` في `OrderFilters`.
9. `OrderPayment` في `auditTrailSubjects()`.
10. `php artisan scramble:analyze` نظيف.

### شريحة ج — العدّادات

11. `PaymentStatusExpression` + `OrderPaymentStatusCountsQuery`، والفلتر يعيد استعمال التعبير.
12. `payment_counts` في `/orders/summary`، و`payments` في `/home/summary`.

### شريحة د — الفلاتر

13. حقول المدفوعات في `Order` model + `OrderPayment` model + التوليد.
14. `OrderPaymentsCubit` + repository + usecases.
15. `AppDropdown<T>` في `core/widgets` — عامّة، تقبل أي مودل.
16. `OrderMoneyRow` في رأس الطلبية وفي بطاقتها وفي شاشة الدفعات.
17. `OrderPaymentsPage` + مسارها + ذراعها في الزر العائم.
18. ورقة «تسجيل دفعة» بحقول التطبيق ومنتقيه، والمبلغ مبدئياً كامل المتبقي.
19. محور الدفع في ورقة الفلتر + `PaymentBoard` في الرئيسية.
20. الصلاحيات الثلاث في `AppPermission`.

> **لا شريحة للتسوية.** `ChangeOrderStatus` لا يُلمس — هذا هو جواب [س١](#س١--هل-تكتب-تم-التسوية-قيد-دفعة-تلقائياً-٨) بعينه.

---

## ١٢. شطب الفرق — الخمسة دنانير التي لم ترجع

> **أُضيف بعد التنفيذ الأول**، حين اصطدم حارس التسوية بحالة يومية لم يكن يعرفها.

### الحالة

طلبية بـ١١٠. رجع المندوب بـ١٠٥ في الظرف، والزبون لن يُطالَب بالخمسة. الطلبية «تم الاستلام»، والمحاسب أمام ثلاثة أبواب كلها مغلقة:

- **«تم التسوية»** مرفوضة ما دام هناك متبقٍّ ([§٨](#٨-التسوية-والسجل--أهم-نقطة-تماسّ)).
- **الخصم** مستحيل: `UpdateOrder` يرفض تعديل طلبية `isClosed()`، والتسليم يُقفلها.
- **دفعة بخمسة** كذبة — وهي الباب الوحيد الذي كان مفتوحاً فعلاً، وهذا بالضبط ما يُبنى السجل لمنعه.

فالطلبية تبقى في «تم الاستلام» إلى الأبد، وطابور «ما لم يُسوَّ» يمتلئ بطلبيات لا يستطيع أحد إنهاءها.

### القرار: قيد رابع، لا خصم ولا دفعة

**ليس خصماً.** الخصم يعيد كتابة الفاتورة إلى ١٠٥ — يُغيّر ما حوسب عليه الزبون بعد أشهر من محاسبته، ويمحو أن شيئاً حدث أصلاً. الشطب يترك `grand_total` على ١١٠ ويسجّل الفجوة **فجوةً**، فتبقى قابلة للعدّ لاحقاً على حقيقتها: خسارة.

**وليس دفعة.** لذلك يحرّك `written_off_amount` لا `paid_amount`، ومن يسأل «كم قبضنا اليوم؟» لا يُعطى رقماً فيه خمسة لم يقبضها أحد. وهذا سبب العمود الثاني على `orders`: الطريق المختصر — أن يزيد الشطبُ المدفوعَ فيمرّ الحارس — كان سيجعل الخمسة التي لم تصل لا تُميَّز عن خمسة وصلت، في تقرير الصندوق وفي الأرباح والخسائر وفي كل سؤال لم يُكتب بعد.

### أثره

| | قبل | بعد الشطب |
| --- | --- | --- |
| `grand_total` | 110.00 | 110.00 |
| `paid_amount` | 105.00 | 105.00 |
| `written_off_amount` | 0.00 | 5.00 |
| المتبقي | 5.00 | 0.00 |
| حالة الدفع | مدفوعة جزئياً | **مشطوب فرقها** |
| «تم التسوية» | مرفوضة | مقبولة |

**و«مشطوب فرقها» حالةٌ خامسة لا «مدفوعة بالكامل».** لا شيء مستحقٌّ عليها — فهي خارج طابور المطالبة — لكنها لم تُدفع كاملةً، وقولُ ذلك هو الكذبة التي بُني الشطب لتفاديها. والحالة تُشتقّ في مكانين اثنين كسابقاتها: `PaymentStatus::between()` في PHP و`PaymentStatusExpression` في SQL، واختبارُ الفلتر يمشي على الخمس مقابل الـ enum نفسه.

### وفي الأرباح والخسائر

`write_offs` سطرٌ بجانب `cash_collected`، **وخارج مجمل الربح عمداً**: التقرير يعترف بالإيراد عند التسليم ولا يحمل جانب مصروفات أصلاً، وطرحُ الشطب من المجمل كان سيخلط رقماً استحقاقياً برقم تحصيلي فلا يُقرأ أيٌّ منهما. والقيود المُلغاة تخرج من الجمع — شطبٌ رُوجع عنه ليس خسارة.

### ما لم يتغيّر

`ChangeOrderStatus` كما هو: الحارس لم يُخفَّف ولم يُستثنَ منه أحد. ما تغيّر أن الطلبية صار في يدها طريقٌ **صادق** لتصير غير مستحقّة، بدل أن تُجبَر على الكذب أو البقاء.

---

*وثيقة حيّة. حين تثبت قاعدة هنا خطأها، تُغيَّر ويُسجَّل السبب.*
