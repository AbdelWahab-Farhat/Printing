# طلبية جديدة — من داخل العميل

> **مُنفَّذ.** أسئلة [§٧](#٧-الأسئلة-المفتوحة) أُجيبت كلها بـ«موافق» على التوصيات، والأجوبة
> مثبَّتة تحت كل سؤال، وما دونه أدناه هو ما بُني فعلاً. وما أُجِّل مسجَّل في
> [BACKLOG.md](BACKLOG.md) بسببه.
>
> ما يخصّ آلة الحالات في [ORDERS-DESIGN.md](ORDERS-DESIGN.md)، والمال في
> [PAYMENTS-DESIGN.md](PAYMENTS-DESIGN.md). هذا الملف عن **أخذ الطلبية** وحده.

---

## ١. القرار

**الطلبية الجديدة تُؤخذ من داخل العميل نفسه** — من شاشة تفاصيل العميل، والعميل معروف قبل أن
تُفتح الشاشة.

وهذا **تصحيح** لما كان مكتوباً في [BACKLOG.md](BACKLOG.md) تحت «أخذ طلبية من التطبيق»:

> شاشة «طلبية جديدة» في الفلاتر: **اختيار العميل**، ثم المدينة والمنطقة، ثم سطر لكل منتج…

الخطوة الأولى تسقط. لماذا:

**العميل ليس حقلاً في النموذج، بل السياق الذي فُتح النموذج بداخله.** الموظف يفتح العميل ليرى
رقمه ومحلاته وتصاميمه، ثم يأخذ منه طلبية — لا يفتح نموذجاً فارغاً ثم يبحث عن اسم هو ينظر إليه.

**وحقل «العميل» في نموذج مفتوح هو أخطر حقل فيه.** الطلبية لا تنتقل بين العملاء بعد إنشائها —
`customer_id` يُقرأ عند الإنشاء ويُتجاهل عند التعديل، وتعليق `OrderData` يقول ذلك صراحةً: «إلغِها
وأعد أخذها». فحقلٌ خطؤه لا يُصحَّح إلا بإلغاء الطلبية لا يُترك مربّع بحث؛ يُنتزع من المسار
ويأتي من الشاشة التي كان الموظف يقرؤها.

**ونتيجة ثالثة تأتي مجاناً:** مكتبة تصاميم العميل ومحلاته وهاتفه كلها في اليد أصلاً — الشاشة
لا تسأل عنها الخادم مرة ثانية.

**ما يبقى في الطريق الآخر:** «طلبية جديدة» لا تصير غير موجودة على شاشة الطلبيات؛ تصير **زراً
يذهب إلى قائمة العملاء** إن أُريد له أن يوجد أصلاً → [س٤](#س٤--هل-يبقى-مدخل-من-شاشة-الطلبيات).

---

## ٢. ما هو مبنيّ بالفعل

**الخادم يقبل الإنشاء كاملاً.** لا سطر واحد ينقص فيه.

| الشيء | الموضع |
| --- | --- |
| `POST /orders` خلف `orders.manage` | [api.php:190](backend/routes/api.php) |
| التحقّق ورسائله العربية | [StoreOrderRequest](backend/app/Application/Api/V1/Requests/Order/StoreOrderRequest.php) |
| الإنشاء في معاملة واحدة + أول سطر في الخط الزمني | [CreateOrder](backend/app/Domain/Order/Actions/CreateOrder.php) |
| التسعير الحيّ للسطر | `POST /products/{id}/quote` خلف `products.view` |
| حراسة الخصم | `orders.discount` داخل النطاق، لا في الطلب |
| اشتقاق نوع التسليم | من `fulfilment_type` على المدينة المختارة — لا مفتاح ثانٍ |

**وفي التطبيق قطعٌ جاهزة تُستعمل كما هي:**

| القطعة | الملف |
| --- | --- |
| اختيار المدينة والمنطقة | [destination_picker_sheet.dart](frontend/lib/features/orders/presentation/widgets/destination_picker_sheet.dart) |
| صفّ «الوجهة» بمظهره | [place_picker_tile.dart](frontend/lib/features/orders/presentation/widgets/place_picker_tile.dart) |
| اختيار تصميم من مكتبة العميل | [design_picker_sheet.dart](frontend/lib/features/orders/presentation/widgets/design_picker_sheet.dart) |
| صفوف المال والإجماليات | [order_money_row.dart](frontend/lib/features/orders/presentation/widgets/order_money_row.dart) · [order_totals.dart](frontend/lib/features/orders/presentation/widgets/order_totals.dart) |
| زرّ الإجراءات على شاشة العميل | [AppSpeedDial](frontend/lib/core/widgets/app_speed_dial.dart) — يُصفّي بالصلاحية وحده |

**والناقص ثلاثة، لا أكثر:**

1. `createOrder` في [order_repository.dart](frontend/lib/features/orders/repositories/order_repository.dart) — الواجهة اليوم تقرأ وتُعدّل وتنقل الحالة، ولا تُنشئ.
2. **منتقي المنتج ومقاسه بتسعير حيّ** — القطعة الوحيدة التي لا وجود لها بأي شكل، وهي نفسها التي
   ينقصها [order_edit_page.dart](frontend/lib/features/orders/presentation/views/order_edit_page.dart) («ما ليس هنا عمداً: إضافة منتج»). تُبنى مرة وتخدم الشاشتين.
3. الشاشة نفسها ومسارها.

---

## ٣. المسار

```dart
// child of Routes.customerDetail
static const String newCustomerOrderPath = 'orders/new';

static String newCustomerOrder(int customerId) => '/customers/$customerId/orders/new';
```

**ابناً لـ`/customers/:id` لا مساراً أعلى**، وبنفس المنطق المكتوب في
[app_router.dart](frontend/lib/core/router/app_router.dart) عن مناطق المدينة: «المنطقة لا حياة
لها خارج مدينتها، والمسار يقول ذلك». طلبيةٌ تُؤخذ بلا عميل ليست طلبية ناقصة، بل شيء لا يُنشأ.
والرابط العميق يحمل العميل معه، فالشاشة تعمل باردة بلا `extra`.

**خارج الـshell** كبقية النماذج: نموذجٌ يُملأ ليس تبويباً يُتصفَّح.

**وحارسٌ على المسار** — `orders.manage` — كما على `/products/new` تماماً: `can()` يجيب فوراً،
فالرابط العميق لا يفتح شاشةً نهايتها الوحيدة 403.

**والخروج:** تُنشئ فتفتح الطلبية — `context.pushReplacement(Routes.order(id))`. استبدالٌ لا
دفع، حتى لا يرجع زرّ الرجوع إلى نموذجٍ أُرسل بالفعل.

---

## ٤. الشاشة

| القسم | ما فيه | ملاحظة |
| --- | --- | --- |
| العميل | الاسم والرمز، **يُقرأ ولا يُختار** | سطر واحد، لا مربّع بحث |
| المحل | `customer_shop_id` — قائمة محلات العميل | اختياري. يُخفى كاملاً إن لم يكن للعميل محل |
| الوجهة | مدينة + منطقة | مطلوبة. سعر التوصيل يأتي من المدينة ولا يُكتب |
| البنود | سطر لكل منتج: منتج ← مقاس ← كمية، بسعر حيّ | سطر واحد على الأقل، وإلا رفض الخادم |
| التصميم | `design_source` من ثلاث + `design_fee` | الأجرة تظهر مع «تصميم من عندنا» وحدها |
| الاستلام | **هاتف واحد**، يُفتح معبَّأً برقم العميل | الاسم والعنوان لا يُسألان — §٤.١ |
| المال | الخصم + الإجمالي التقديري | الخصم خلف `orders.discount`، والإجمالي **تقديري ويقول ذلك** |
| ملاحظات | نص حرّ | |

### ٤.١ لماذا حقل واحد تحت «الاستلام»

الاسم اسم العميل، والعنوان عنوان محلّه — وكلاهما على الطلبية أصلاً من لحظة أخذها، عبر
`customer_id` و`customer_shop_id`. فسؤالهما ثانيةً كان طلباً من الموظف أن ينسخ ما تعرضه الشاشة
التي خلفه، وكل نسخة فرصة لخطأ مطبعي.

**والهاتف وحده الاستثناء**، لأنه الرقم الذي يرنّه المندوب وهو *أحياناً* شخص آخر — أخٌ في المحل،
أو سائق. فيُعرض معبَّأً برقم العميل ويبقى مفتوحاً للتغيير. والتعبئة تحدث **مرة واحدة** لا في كل
إعادة بناء: الشاشة تُعاد بناؤها مع كل ضغطة مفتاح في أي حقل آخر، وتعبئةٌ تتكرر كانت ستكتب فوق ما
يكتبه الموظف.

**وهو قابل للتصحيح بعد الإنشاء في كل حالة عدا «جاري التوصيل»** — نفس قاعدة الوجهة وبنفس سببها:
الطرد عند المندوب ومعه العنوان والرقم على شاشته، ونسختنا تتغيّر ونسخته لا. يرفضه الخادم
(`RecipientPhoneCannotChange`) والشاشة تخفي الحقل، والرفض يُقارَن على **تغيّر** الرقم لا على
إرساله: كل تعديل يعيد إرسال الطلبية كاملة، ورقمٌ يعود كما هو ليس تغييراً.

**ولا سعرٌ يُكتب بيد** — إلا للمنتج الذي يسعّره الكتالوج «حسب الطلب»، وهو الاستثناء الذي
يشرحه [OrderItemData](backend/app/Domain/Order/DTOs/OrderItemData.php) ووحده.

**والإجمالي المرسوم أثناء الكتابة تقديرٌ لا حقيقة**، بنفس قاعدة `order_edit_page`: الرقم الذي
يُعتمد هو ما يعود من الخادم بعد الحفظ.

**القالب الأقرب** لسطور متعدّدة بأخطاء لكل سطر:
[product_form_page.dart](frontend/lib/features/products/presentation/views/product_form_page.dart).

---

## ٥. الصلاحيات

| الشيء | الصلاحية |
| --- | --- |
| ظهور «طلبية جديدة» على شاشة العميل | `orders.manage` — يُصفّيها `AppSpeedDial` بنفسه |
| فتح المسار (رابط عميق) | `orders.manage` — `redirect` على المسار |
| حقل الخصم | `orders.discount` — يُخفى، **والخادم يرفض** |
| منتقي المنتج | `products.view` — نفس ما يطلبه `quote` |

---

## ٦. الاختبارات

كلٌّ منها كُتب **فاشلاً أولاً** ومقسومٌ ظاهراً إلى Arrange/Act/Assert:

| الملف | ما يحرسه |
| --- | --- |
| `OrderTest::test_a_deactivated_customer_cannot_be_sold_to` | الخادم يرفض الأخذ من عميل معطَّل — ومعه اختبارٌ ثانٍ يثبت أن النشط ما زال يُباع له، حتى لا يمرّ رفضٌ يقع على الجميع |
| [take_order_test.dart](frontend/test/features/orders/take_order_test.dart) | `٣٠٠` تصل ASCII · الفاصلة تصير نقطة · الأجرة لا تُرسل إلا مع «من عندنا» · خانة خصم فارغة **تغيب** ولا تُرسل صفراً |
| [take_order_wire_test.dart](frontend/test/features/orders/take_order_wire_test.dart) | البنود تُرمَّز ولا تُسلَّم كائناتٍ · ما تركه الموظف فارغاً يغيب عن الجسم · المال نصوص |
| [take_order_cubit_test.dart](frontend/test/features/orders/take_order_cubit_test.dart) | العميل يأتي من الشاشة لا من المُنادي · الضغطة الثانية أثناء الإرسال تُهمَل · طلبية بلا بنود لا تُرسل |
| [line_quote_cubit_test.dart](frontend/test/features/orders/line_quote_cubit_test.dart) | الكمية تُطبَّع قبل السؤال · الفارغة لا تسأل · جوابان في الطريق ويظهر الأخير وحده · «حسب الطلب» لا تُسأل أصلاً |
| [product_quote_test.dart](frontend/test/features/products/product_quote_test.dart) | كل رقم يعود كما أرسله الخادم نصاً · الرفض يُعرض بجملته |
| [new_order_page_test.dart](frontend/test/features/orders/new_order_page_test.dart) | العميل يُعرض ولا يُختار · الخصم يغيب بلا `orders.discount` · الأجرة تغيب إلا مع «من عندنا» · طلبية بلا بند لا تُرسَل · **هاتف الاستلام يُفتح برقم العميل ولا يُكتب فوق ما يُطبَع** |
| `OrderTest::test_the_recipient_phone_freezes_with_the_destination` | الرقم يُقفل في «جاري التوصيل» ويُصحَّح قبلها — ومعه اختبارٌ أن إعادة إرساله كما هو ليست تغييراً، وإلا لتعذّر تعديل الملاحظات على طرد في الطريق |
| [order_invoice_cubit_test.dart](frontend/test/features/orders/order_invoice_cubit_test.dart) | الرقم يُفتح من الطلبية · تفريغه يمسحه ولا يخزّن فراغاً · طلبية على الطريق لا تذكره أصلاً · الرفض يقع تحت الحقل |
| [customer_detail_actions_test.dart](frontend/test/features/customers/customer_detail_actions_test.dart) | الإجراء يظهر بـ`orders.manage`، ويغيب بدونها **وعلى العميل المعطَّل** |

**وما لم يُختبر بعد:** منتقي المنتج ([product_picker_sheet.dart](frontend/lib/features/orders/presentation/widgets/product_picker_sheet.dart)) وصفّ البند
([order_line_row.dart](frontend/lib/features/orders/presentation/widgets/order_line_row.dart)) — منطقهما (التسعير، الطبع، ترتيب الأجوبة) مغطّى في
`LineQuoteCubit`، والباقي رسمٌ وتمرير. مسجَّل في [BACKLOG.md](BACKLOG.md).

---

## ٧. الأسئلة المفتوحة

> اكتب جوابك تحت كل سؤال. توصيتي مكتوبة، فـ«موافق» جواب كامل.

### س١ — الزر: إجراء في القائمة، أم قسم «طلبيات العميل»؟

شاشة العميل اليوم تعرض هويته وحالته واتصاله ومحلاته — **ولا تعرض طلبياته**، مع أن
`orders(customerId: …)` مبنيّ في الواجهة منذ البداية.

- **(أ)** إجراء «طلبية جديدة» في `AppSpeedDial` بجانب «تعديل» و«التصاميم». شريحة واحدة صغيرة.
- **(ب)** (أ) **ومعه** قسم «آخر الطلبيات» في جسم الشاشة، وفي رأسه الزر — فيرى الموظف ما طلبه
  الرجل قبل أن يأخذ منه طلبية جديدة. يحتاج `customerId` على `OrdersFilter` (لا تحمله اليوم).

**توصيتي: (أ) الآن، و(ب) شريحةً تاليةً.** القرار الذي طلبتَه — أن الطلبية تُؤخذ من داخل
العميل — يقف كاملاً على (أ)، و(ب) شاشةٌ ثانيةٌ تُبنى بجانبها لا شرطٌ لها.

**الجواب: (أ).** إجراء واحد في `AppSpeedDial`. وقسم «آخر الطلبيات» شريحةٌ تالية → [BACKLOG.md](BACKLOG.md).

### س٢ — المدينة: تُبدأ فارغة أم مقترحة؟

لا رابط بين محل العميل والمدينة في القاعدة — المحل يحمل إحداثيات (`latitude`/`longitude`) لا
`city_id`. فلا يوجد اقتراح **صحيح** يُستخرج من البيانات اليوم.

- **(أ)** تُترك فارغة، ويختارها الموظف في كل طلبية.
- **(ب)** تُقترح مدينة آخر طلبية لهذا العميل — طلبٌ إضافي، واقتراحٌ يخطئ حين يشتري الرجل لفرعه الثاني.

**توصيتي: (أ).** والاقتراح الخاطئ في حقلٍ يحدّد سعر التوصيل أسوأ من حقلٍ فارغ.

**الجواب: (أ).** تُبدأ فارغة، ولا اقتراح.

### س٣ — التصاميم: تُربَط أثناء الأخذ أم بعده؟

`POST /orders/{id}/designs` **مسارٌ ثانٍ بعد** إنشاء الطلبية — لا يقبل `POST /orders` تصاميم
معه. فربطها في نفس الشاشة يعني طلبين، والثاني قد يفشل والطلبية قد نشأت.

- **(أ)** لا تصاميم في شاشة الأخذ. تُنشأ الطلبية، تُفتح، ومنها تُضاف — والزرّ موجود اليوم.
- **(ب)** تُختار في الشاشة وتُرسل بعد النجاح، وفشلها رسالةٌ على طلبية قائمة لا فشلٌ للأخذ.

**توصيتي: (أ).** `design_source` يُسجَّل عند الأخذ لأنه يمسّ المال، والملفات تُلحق بعده.

**الجواب: (أ).** `design_source` يُسجَّل عند الأخذ، والملفات تُلحق من شاشة الطلبية بعده.

### س٤ — هل يبقى مدخل من شاشة الطلبيات؟

الموظف الذي يستقبل مكالمة يكون على شاشة الطلبيات لا على العميل.

- **(أ)** لا زر هناك. الطريق: العملاء ← العميل ← طلبية جديدة.
- **(ب)** زر يذهب إلى **قائمة العملاء** ببحثها، فيُختار العميل فتُفتح شاشته… خطوةٌ زائدة.
- **(ج)** زر يفتح منتقي عميل ثم يقفز إلى نموذج الطلبية مباشرة — وهذا يعيد «اختيار العميل أولاً»
  من الباب الخلفي.

**توصيتي: (أ)، ثم (ب) إن اشتكى الاستعمال.** طلبك كان أن يكون الأخذ داخل العميل، و(ج) يخالفه.

**الجواب: (أ).** لا مدخل من شاشة الطلبيات.

### س٥ — العميل المعطَّل: يُمنع أم يُحذَّر؟

الخادم لا يمنع اليوم: `StoreOrderRequest` يطلب `customers,id` موجوداً غير محذوف، ولا يسأل عن
`is_active`.

- **(أ)** يُخفى الإجراء على عميل معطَّل — والتنشيط على بُعد ضغطة في نفس الشاشة.
- **(ب)** يُسمح مع تحذير.
- **(ج)** (أ) **ومعه** رفضٌ في `CreateOrder` — لأن حقلاً مخفياً اقتراح، والطلب المرفوض قاعدة.

**توصيتي: (ج)** — وهي نفس الجملة المكتوبة في `CreateOrder::guardDiscount`. وإن اخترتَها فهي
تعديلٌ على الخادم أيضاً، ويلزمها اختبارٌ في `OrderTest`.

**الجواب: (ج).** الإجراء يُخفى على شاشة العميل، **و**`CreateOrder` يرفض — والرفض هو الشطر الذي يُعتمد عليه.
