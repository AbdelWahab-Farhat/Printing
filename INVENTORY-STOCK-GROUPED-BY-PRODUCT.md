# شاشة الأرصدة — تجميع المقاسات تحت منتجها

نعم، ممكن — وأرخص مما يبدو: البيانات موجودة كلها في الصفّ الحالي، والخادم يحتاج **ترتيباً** لا
نقطة جديدة. هذا المستند يصف الشكل، والتكلفة الحقيقية (الترقيم)، والأسئلة المفتوحة في آخره.

---

## ١ · لماذا

في اللقطة أربعة صفوف، ثلاثة منها لنفس المنتج:

```
[صورة]  P1  أكياس الشحن · 25*35     350   كيلوغرام  ✏️
[صورة]  P1  أكياس الشحن · 50*45   1,200   كيلوغرام  ✏️
[صورة]  P1  أكياس الشحن · 60*50   1,000   كيلوغرام  ✏️
[صورة]  P11 أكياس شحن سادة · 20*30  890   كيلوغرام  ✏️
```

«أكياس الشحن» مكتوبة ثلاث مرات، و`P1` ثلاث مرات، والصورة نفسها مرسومة ثلاث مرات — والمختلف بين
الصفوف الثلاثة **كلمة واحدة**: المقاس. كل ما يتكرّر هو حقيقة عن المنتج لا عن الرفّ، وثلاثة أصناف
اليوم تصير ثلاثين حين يمتلئ الكتالوج.

---

## ٢ · التصميم

### ٢.١ المجموعة

```
┌──────────────────────────────────────────────────────┐
│ [صورة]  P1  أكياس الشحن              2,550          │
│         ٣ مقاسات · ١ تحت الحد        كيلوغرام    ⌄  │
│ ─────────────────────────────────────────────────── │
│    25*35                              350        ✏️  │
│    50*45                            1,200        ✏️  │
│    60*50                            1,000        ✏️  │
└──────────────────────────────────────────────────────┘
```

* **الصورة والاسم والرمز يصعدون إلى الترويسة** — يُرسمون مرة واحدة لأنّهم حقيقة واحدة.
* **صفّ المقاس يصير سطراً**: المقاس، الرقم، وحدّ التنبيه/الحالة تحته كما هي اليوم، بلا صورة ولا
  اسم منتج. الرقم يبقى أعلى الصفّ صوتاً كما هو الآن.
* **الحالة تصعد أيضاً**: الترويسة تحمل أسوأ حالة في المجموعة («١ نافد» تسبق «٢ تحت الحد»)، فمجموعة
  مطويّة لا تُخفي رفّاً متوقفاً.

### ٢.٢ المجموع تحت الترويسة **رقمٌ صحيح** — بخلاف مجموع الشاشة

`stock_unit` حقيقة عن **المنتج** لا عن الرفّ: `SetStockUnit` مُطبَّق على المنتج كله ويكتب كل
`WarehouseStock` لمقاساته في المعاملة نفسها (انظر
[SetStockUnit.php](backend/app/Domain/Inventory/Actions/SetStockUnit.php) — «a bag counted in kilos
in one warehouse and pieces in another is not a number anyone could reconcile»). فمقاسات المنتج
الواحد تتقاسم وحدة واحدة دائماً، و`350 + 1,200 + 1,000 = 2,550 كيلوغرام` جملة لها معنى.

وهذا يعطي **جواباً للسؤال المفتوح الأول** في
[INVENTORY-STOCK-SCREEN.md](INVENTORY-STOCK-SCREEN.md#٥--أسئلة-مفتوحة): مجموع الشاشة كله يجمع كيلوغرامات
على قطع، أما مجموع المجموعة فلا يفعل — والمستوى الذي يصحّ فيه الجمع هو المنتج.

**الاحتياط الوحيد:** إن اختلفت وحدات صفوف مجموعة واحدة (بيانات قديمة، أو رفّ نجا من تغيير وحدة)
فلا يُطبع مجموع أصلاً — لا رقم أفضل من رقم يجمع كيلوغراماً على قطعة.

### ٢.٣ منتج بمقاس واحد يبقى صفّاً مسطّحاً — هذا هو `has_variations`

`P11` في اللقطة مقاس واحد. ترويسةٌ فوقه تطوي سطراً واحداً هي طيٌّ لا يُخفي شيئاً، وتفصلة إضافية
لتصل إلى رقم كان ظاهراً. فالقاعدة: **المجموعة تظهر حين يكون للمنتج أكثر من رفّ واحد في هذا المخزن**
— لا حين يكون له أكثر من مقاس في الكتالوج. مقاسٌ لم يدخل هذا المخزن قط ليس على أيّ رفّ هنا،
وترويسة تعدّه ضمن «٣ مقاسات» تَعِد بصفّين لا وجود لهما.

### ٢.٤ مفتوحة أم مطويّة

**مفتوحة افتراضياً.** الأرقام هي سبب فتح الشاشة، وشاشةٌ تُفتح على أربع ترويسات مطويّة تطلب نقرة
قبل أن تجيب. الطيّ يدوي ويبقى للقوائم الطويلة.

هذا يختلف عن [variant_picker_sheet.dart](frontend/lib/features/warehouses/presentation/widgets/variant_picker_sheet.dart)
— وهي السابقة الجاهزة لهذا النمط، «منتج تُفتح تحته مقاساته، واحد في كل مرة» — لأنّ تلك ورقة
**اختيار**: القارئ يبحث عن مقاس واحد، لا عن كل الأرقام.

---

## ٣ · التكلفة الحقيقية: الترتيب والترقيم

هنا العمل كله، وليس في الرسم.

### ٣.١ الترتيب اليوم مصادفة

```php
// Warehouse.php:52
return $this->hasMany(WarehouseStock::class)->orderBy('product_variant_id');
```

مقاسات المنتج الواحد متجاورة في اللقطة لأنّها أُنشئت متتابعة فحملت أرقاماً متتابعة — لا لأنّ أحداً
رتّبها. مقاسٌ يُضاف بعد سنة إلى «أكياس الشحن» يأخذ `id` كبيراً ويهبط في آخر القائمة، بعيداً عن
إخوته، فتظهر مجموعتان لمنتج واحد.

**اللازم:** ضمّ (`join`) على `product_variants` و`products` في
[StockListQuery.php](backend/app/Domain/Inventory/Queries/StockListQuery.php)، وترتيب صريح:
`products.sort_order, products.id, product_variants.sort_order, product_variants.id`.

### ٣.٢ المجموعة تنقسم بين صفحتين

الصفحة ١٥ صفّاً. منتج بثمانية مقاسات يبدأ في آخر صفحة وينتهي في أول التالية، فتُرسم ترويستان له.

خياران:

| | كيف | الثمن |
|---|---|---|
| **أ · التجميع في العميل** (المقترح) | الخادم يضمن الترتيب فقط؛ العميل يجمّع المتجاورين، وإذا كان آخر منتج في الصفحة هو أول منتج في التالية دُمجت المجموعتان | تغيير خادمٍ من سطرين، وقاعدة دمج واحدة في `PagedCubit`. المجموع في الترويسة يكون **مجموع ما وصل**، ويتحرّك حين تصل بقيّته |
| **ب · الترقيم بالمنتج** | الخادم يرقّم المنتجات ويُرجع مقاساتها كاملة | شكل استجابة جديد، و`total_lines` يصير معنيين، و`per_page` يصير عدداً غير معلوم من الصفوف. ويحلّ مشكلة المجموع الجزئي وحدها |

المقترح **(أ)**، ومعه قاعدة: الترويسة لا تعِد بما لم يصل — «٣ مقاسات» تعني ثلاثة ظاهرة تحتها الآن.

### ٣.٣ المرشّحات

مع «تحت الحد» أو «نافد» لا تصل المجموعة كاملة: يصل منها ما طابق. فالترويسة تصف **ما هو تحتها**
(«٢ مقاسات تحت الحد») ولا تدّعي عدد مقاسات المنتج كلها — وإلا احتاجت عدّاً ثانياً من الخادم
لعددٍ لا يُغيّر قراراً.

---

## ٤ · ما يتغيّر فعلاً

| الملف | التغيير |
|---|---|
| [StockListQuery.php](backend/app/Domain/Inventory/Queries/StockListQuery.php) | ضمّ وترتيب صريح — الجزء الوحيد الإلزامي في الخادم |
| [WarehouseStockResource.php](backend/app/Application/Api/V1/Resources/WarehouseStockResource.php) | لا شيء. `product_id` و`product_code` و`product_name` و`image_url` تصل أصلاً داخل `product_variant` |
| [warehouse_stocks_cubit.dart](frontend/lib/features/warehouses/presentation/viewmodel/warehouse_stocks_cubit.dart) | تجميع الصفحات المتراكمة إلى مجموعات، ودمج المجموعة المنقسمة |
| [stock_row.dart](frontend/lib/features/warehouses/presentation/widgets/stock_row.dart) | يُقسم إلى ترويسة مجموعة + سطر مقاس، ويبقى الصفّ المسطّح كما هو لمنتج بمقاس واحد |

---

## ٥ · خارج النطاق عمداً

* **أرصدة المنتج عبر كل المخازن** («أين بقيّة `P1`؟»). سؤال آخر وشاشة أخرى — هذه الشاشة عن مخزن واحد.
* **صورة لكل مقاس.** لا وجود لها في قاعدة البيانات، والتجميع يجعل غيابها أوضح لا أسوأ: صورة واحدة
  في الترويسة هي بالضبط ما تعنيه.

---

## ٦ · أسئلة مفتوحة

1. **مفتوحة أم مطويّة عند الفتح؟** المقترح: مفتوحة (§٢.٤). المطويّة تعطي شاشة أقصر وقائمة منتجات
   أسرع مسحاً بالعين — إن كان الجرد يبدأ من «أيّ منتج؟» لا من «كم فيه؟» فالمطويّة أصحّ.
2. **مجموع في الترويسة؟** آمنٌ حسابياً (§٢.٢)، لكنّه رقم رابع في صفٍّ فيه ثلاثة. أبقيه أم أكتفي
   بـ«٣ مقاسات»؟
3. **منتج بمقاس واحد**: صفّ مسطّح كما اليوم (المقترح)، أم مجموعة من واحد ليكون شكل القائمة واحداً؟
4. **بطاقة الملخّص أعلى الشاشة**: تقول «٤ صنفاً» اليوم بمعنى أربعة رفوف. مع التجميع، هل تصير
   «٤ مقاسات · منتجان»؟ ثمنها عدّ مميّز إضافي في استعلام الملخّص.
5. **الترتيب**: `products.sort_order` هو ترتيب الكتالوج. هل تريد بدله الأثقل أولاً (المنتج صاحب
   أكبر رصيد) أو الأخطر أولاً (صاحب النافد)؟ الترتيب بالحالة يجعل القائمة تتحرّك تحت اليد كلما
   تغيّر رصيد، والترتيب الثابت أسهل حفظاً بالعين.

---

## ٧ · ما نُفِّذ فعلاً (٢٣ أغسطس ٢٠٢٦)

الشاشة تجمّع الآن. ما اختلف عن المقترح أعلاه، ولماذا:

### التجميع بالمفتاح لا بالتجاور — فلا حاجة لتغيير في الخادم

[stock_group.dart](frontend/lib/features/warehouses/models/stock_group.dart) يجمّع بـ
`product_variant.product_id` على **كل ما وصل** من صفحات، لا على المتجاورين. فالمقاس الذي أُضيف
بعد سنة وحمل `id` كبيراً ينضمّ إلى بطاقة إخوته حالما تصل صفحته، ولا تظهر ترويستان لمنتج واحد —
لا داخل الصفحة ولا عبر حدودها (§٣.١ و§٣.٢ يسقطان كشرطين لازمين).

الضمّ والترتيب الصريح في [StockListQuery.php](backend/app/Domain/Inventory/Queries/StockListQuery.php)
يبقيان **تحسيناً مرغوباً لا شرطاً**: بدونهما ترتيب البطاقات هو ترتيب أول مقاس وصل من كل منتج،
وهو مستقرّ لكنه ليس ترتيب الكتالوج. لم يُنفَّذ — الشاشة صحيحة بدونه.

### إجابات الأسئلة المفتوحة

| السؤال | ما شُحن |
|---|---|
| ١ · مفتوحة أم مطويّة | **مفتوحة**، بلا طيّ يدوي بعد. الأرقام هي سبب فتح الشاشة |
| ٢ · مجموع في الترويسة | **لا**. الترويسة تحمل الاسم والرمز والصورة و«٣ مقاسات» فقط. المجموع آمن حسابياً كما في §٢.٢، لكنّه رقم رابع في صفٍّ فيه ثلاثة أرقام، وهو أرخص إضافةً لاحقاً من إزالةً |
| ٣ · منتج بمقاس واحد | **صفّ مسطّح** كما اليوم — `StockRow` بلا ترويسة فوقه |
| ٤ · بطاقة الملخّص | **بلا تغيير**. «٤ صنفاً» ما زالت تعني أربعة رفوف، وهي ما يعدّه الخادم |
| ٥ · الترتيب | ترتيب الخادم كما هو (لم يُضف ضمّ) |

كذلك **لم تصعد الحالة إلى الترويسة** (§٢.١): «نافد» و«تحت الحد» تبقى على سطر المقاس الذي تصفه.
مع مجموعة مفتوحة دائماً لا تُخفي الترويسةُ شيئاً، وشارةٌ فيها تعدّ ما هو مرئي تحتها مباشرة هي
تكرار لا تلخيص.

### أثر جانبي أُصلح مع التجميع

[paged_list_view.dart](frontend/lib/core/widgets/paged_list_view.dart): صفحةٌ لا تملأ الشاشة لا
تُمرَّر، والتمرير هو الشيء الوحيد الذي يطلب الصفحة التالية — فكانت القائمة تقف عند الصفحة الأولى.
كان بعيد المنال حين كان كل صفّ صفّاً من الـ API، وصار قريباً حين صارت خمسة عشر رفّاً بطاقتين.
تُطلب الصفحة التالية الآن بعد الإطار الذي ينتهي بلا ما يُمرَّر. يثبّته
[paged_list_view_test.dart](frontend/test/core/widgets/paged_list_view_test.dart).
