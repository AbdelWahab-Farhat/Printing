# أوامر الشراء على شاشة المورد

شاشة المورد اليوم تقول من هو المورد — الاسم، الهاتف، الشخص المسؤول، العنوان، تاريخ الإضافة — ولا
تقول شيئاً عمّا اشترينا منه. الطريق الوحيد إلى أوامر شراء موردٍ بعينه هو تبويب «أوامر الشراء» ثم
قراءة أسماء الموردين في البطاقات واحداً واحداً: القائمة تقبل `vendor_id` على الخادم، لكن لا شاشة
ترسله.

هذا هو نفس النقص الذي عالجه [CUSTOMER-ORDERS-SECTION.md](CUSTOMER-ORDERS-SECTION.md) على شاشة
العميل، والقرار هنا هو **نفس الشكل نفسه**: قسم «إدارة أوامر الشراء» بأربعة مداخل وعدد بجانب كلٍّ
منها.

---

## ١. المداخل الأربعة

| المدخل | ماذا يفتح | الحالات |
|---|---|---|
| **كل أوامر الشراء** | كل ما طُلب من هذا المورد، بلا استثناء | الحالات الأربع |
| **أوامر الشراء الجارية** | ما لم ينتهِ بعد | `new` + `arrived` |
| **أوامر الشراء المكتملة** | ما وصل كاملاً | `completed` |
| **أوامر الشراء الملغاة** | ما كُفَّ عنه | `cancelled` |

### ما الذي يجعل أمر الشراء «جارياً»

كل ما لم ينتهِ: `new` (مسوّدة لم تُرسل بعد) و`arrived` (أُرسل، أو وصل بعضه). وهو **حرفياً**
`! PurchaseOrderStatus::isFinal()` الموجود على الخادم اليوم — لا قائمة ثانية مكتوبة باليد، تماماً
كما اشتُقّت مجموعات الطلبيات من `isFinished`.

`new` داخل «الجارية» رغم أنها لم تُرسل: مسوّدةٌ عند مورد هي عملٌ مُعلَّق على أحدنا لا عليه، وهي أحوج
ما تكون إلى الظهور — أمر شراء نُسي في «جديد» شهراً هو بضاعة لم تُطلب أصلاً.

### ما الذي يجعله «مكتملاً»

`completed` وحدها: كل بند وصل بالكامل. وهي حالة لا تُبلغ يدوياً على الخادم أصلاً — `ReceivePurchaseOrder`
هو من يقرّرها حين يستلم آخر قطعة، فالعدد الذي تحمله البطاقة هو عدد ما وصل فعلاً لا ما قال أحدٌ إنه وصل.

### و«الملغاة» — صندوقها الخاصّ

خارج «الجارية» و«المكتملة»: ليست جارية لأن أحداً لن يعمل عليها، وليست مكتملة لأنها لم تصل. وداخل
«الكل» بالطبع، لأن «كل» تعني كلّ شيء.

**ولها مدخلها الرابع** — قرار ١٤ أغسطس. كان الاقتراح إبقاءها بلا مدخل يُفتح بالشرائح من «الكل»،
والردّ كان أن المُلغى سؤالٌ يُسأل مباشرةً («كم أمرَ شراءٍ ألغينا مع هذا المورد؟») لا شيءٌ يُصادَف
أثناء تصفّح الكل. والأربعة معاً تقسّم الحالات قسمةً كاملة لا يسقط منها شيء: جارٍ، مكتمل، ملغى — ثم
«الكل» فوقها.

القرار نفسه ينطبق على شاشة العميل: مدخل رابع «الطلبات الملغاة» يُضاف هناك أيضاً، فتبقى الشاشتان
تقولان الشيء نفسه بالشكل نفسه. المجموعة مشتقّة لا مكتوبة — كل حالة إلغاء، لا الحالة الواحدة
المسمّاة اليوم — حتى لو أضيفت حالة إلغاء ثانية غداً.

---

## ٢. ما ينقص الخادم

شيئان، وكلاهما صغير:

### أ. `status` يقبل أكثر من واحدة

`PurchaseOrderFilters` اليوم يحمل `?PurchaseOrderStatus $status` مفردة، فلا سبيل لسؤال «الجديدة
والواصلة معاً» في نداء واحد. الحل هو **نفس ما فعلته `OrderFilters` حرفياً**: `?array $statuses`،
يقبل `status=new` و`status[]=new&status[]=arrived` معاً، ويُسقِط القيم المجهولة بدل أن يرمي ٥٠٠.

الشرائح الحالية في `PurchaseOrdersPage` ترسل حالة واحدة وتبقى تعمل كما هي — القائمة تتّسع ولا تتغيّر.

### ب. `GET /purchase-orders/summary`

عدّاد لكل حالة، بنفس شكل `GET /orders/summary` ونفس صلاحيته (`purchase_orders.view`):

```
GET /api/v1/purchase-orders/summary?vendor_id=7

{ "counts": { "new": 2, "arrived": 1, "completed": 9, "cancelled": 0 }, "total": 12 }
```

- **نداء واحد لا ثلاثة**: الأعداد الثلاثة على الشاشة جمعٌ محلّي من `counts`، تماماً كما تفعل
  `CustomerOrderCountsCubit`.
- **يُعلن قبل `{purchase_order}` في `api.php`** وإلا قُرئت كلمة «summary» كمعرّف أمر شراء وردّت ٤٠٤ —
  الفخّ نفسه المكتوب عند `orders/summary`.
- يقبل `vendor_id` و`warehouse_id` — نفس مرشّحات القائمة، فالملخّص والقائمة يجيبان عن سؤال واحد.

**لا يُضاف `Vendor::purchaseOrders()`**: نموذج `PurchaseOrder` يعرف `Vendor` بالفعل، وعلاقة معاكسة
تجعل النطاقين دائرة — نفس ما قيل عن `Customer::orders()`. العدّ استعلامٌ في نطاق أوامر الشراء.

---

## ٣. ما ينقص التطبيق

| القطعة | ماذا |
|---|---|
| `PurchaseOrdersFilter` | صنف بسيط: `title`، `statuses`، `vendorId` — نظير `OrdersFilter`، يسافر كـ `extra` على المسار |
| `Routes.filteredPurchaseOrders` | مسار يفتح `PurchaseOrdersPage` وهي تعرف أنها تجيب عن سؤال، فتضع العنوان في الشريط وتُخفي الشرائح حين يكون السؤال محدَّداً |
| `PurchaseOrderStatus.inProgress` / `.completedGroup` | المجموعتان، مشتقّتان من `isFinal` لا مكتوبتين — واختبار عقد يقرأ `isFinal()` من الخادم ويفشل إن تباعدا |
| `VendorPurchaseOrderCountsCubit` | نداء `summary` واحد، وحالته تُمرَّر إلى القسم |
| `VendorPurchaseOrdersSection` | نسخة `CustomerOrdersSection` نفسها بأسمائها العربية الثلاثة |

الصفر يبقى ظاهراً، وفشل الأعداد لا يُخفي المداخل، والصفّ يُفتح بلا رقم — القواعد الثلاث نفسها
المكتوبة في مستند العميل، لأن سببها واحد.

### الصلاحية

القسم كلّه **غائب لا معطَّل** لمن لا يملك `purchase_orders.view` — نفس ما يفعله «أمر شراء جديد» في
قائمة الإجراءات على هذه الشاشة بالضبط.

---

## ٤. أين يقع القسم على الشاشة

بين بطاقة الهوية و«معلومات الاتصال»:

```
┌ الهوية: الاسم + «نتعامل معه» ┐
├ إدارة أوامر الشراء (٤ مداخل) ┤   ← جديد
├ معلومات الاتصال              ┤
├ الملاحظات (سطر واحد)         ┤   ← انظر GENERAL-COMMENTS.md
└ السجل                        ┘
```

أعلى من «معلومات الاتصال» لأن من يفتح مورداً في وقت العمل يسأل «أين طلبيتنا؟» أكثر ممّا يسأل عن رقم
هاتفه — والهاتف على بُعد تمريرة واحدة.

**«معلومات الاتصال» تبقى كما هي هنا، ولا تُحذف كما حُذفت على شاشة العميل.** بطاقة المورد تحمل أربعة
صفوف — هاتف، شخص مسؤول، بريد، عنوان — لا صفّين، والشخص المسؤول تحديداً لا يُفهم بلا عنوانه فوقه.

---

## ٥. الأسئلة — ما أُجيب وما بقي

### ١. هل «الجارية» تشمل المسوّدات (`new`)؟ — ✅ نعم

**ردك:** نعم. أمر شراء لم يُرسل هو عملٌ مُعلَّق علينا، فيُحسب جارياً.

### ٣. مدخل رابع للملغاة؟ — ✅ نعم، على الشاشتين

**ردك:** «يليت حالة cancelled أو حالات cancel كلهم تكون بصندوق لحالها، سواء للعميل أو المورد.»
منفَّذ كما هو موصوف في القسم ١.

### ٢. هل يظهر عدد أوامر الشراء على بطاقة المورد في القائمة؟

كما يظهر `orders_count` بجانب رمز العميل. يحتاج حقلاً على `VendorResource` يُحسب باستعلام واحد لكل
صفحة، محجوباً خلف `purchase_orders.view`.

**ردك:**


### ٤. هل تُعرض قيمة ما اشتريناه من هذا المورد (مجموع الأموال)؟

سؤال آخر تماماً — يخصّ الدفعات لا الحالات — ومكانه شاشة حساب المورد إن وُجدت. مقترحي تأجيله إلى
`BACKLOG.md`.

**ردك:**


---

## ٦. خطة التنفيذ — **منفَّذة بالكامل** (١٤ أغسطس ٢٠٢٦)

الشرائح الستّ أدناه كلّها مكتوبة والاختبار قبل الكود في كلٍّ منها. ما تبقّى من هذا المستند هو
السؤالان ٢ و٤ في القسم ٥ لا غير.

1. **الخادم — `statuses` جمعاً**: اختبار يطلب `status[]=new&status[]=arrived` ويتوقّع الاثنين، ثم
   تعديل `PurchaseOrderFilters` و`PurchaseOrderListQuery`. اختبار الحالة المفردة القائم يبقى أخضر.
2. **الخادم — `/purchase-orders/summary`**: اختبار للأعداد مع `vendor_id` وبدونه، واختبار ٤٠٣ لمن لا
   يملك `purchase_orders.view`، ثم `PurchaseOrderStatusCountsQuery` والمسار قبل `{purchase_order}`.
3. **التطبيق — المجموعات الثلاث واختبار العقد**: `inProgress` و`completed` و`cancellations`
   مشتقّة من `isFinal`، واختبار يقرأ تعداد الخادم.
4. **التطبيق — المرشِّح والمسار**: `PurchaseOrdersFilter` وفتح القائمة معنونةً، مع اختبار ويدجت أن
   العنوان يظهر والشرائح تغيب.
5. **التطبيق — الأعداد والقسم**: الـ cubit، ثم `VendorPurchaseOrdersSection`، ثم إدخاله في
   `VendorDetailPage` خلف الصلاحية.
6. **شاشة العميل — المدخل الرابع**: `OrderStatus.cancellations` مشتقّة، وصفٌّ رابع في
   `CustomerOrdersSection` يقرأ عدده من `counts` الموجودة أصلاً — بلا نداء جديد.

بعد كل شريحة: `php artisan test` على الخادم، و`flutter analyze` + `flutter test` على التطبيق. لا
تشغيل للتطبيق — الاختبار عندك.
