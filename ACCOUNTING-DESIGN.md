# المحاسبة — دفتر الأستاذ المزدوج — المواصفة

> **مقترح — ينتظر أجوبة [§١١](#١١-الأسئلة).** ما دونه هو ما سيُبنى ما لم يُجَب بغيره، والافتراضات المطبَّقة في [§١٣](#١٣-الافتراضات-المطبَّقة).
> ما يُؤجَّل مسجَّل في [BACKLOG.md § المحاسبة](BACKLOG.md#المحاسبة) بسببه.
> الفرع: `Accounting`.

---

## ١. الفكرة في ثلاثة أسطر

كل حدث مالي في النظام ينتهي إلى **قيد** له طرف مدين وطرف دائن متساويان. لا حركة مالية خارج ذلك.

الموظف لا يرى القيود ولا يكتبها: يسجّل **دفعة** أو **مصروفاً** أو **توريداً** كما يفعل اليوم، والنظام يحوّلها إلى قيد بقاعدة ثابتة مكتوبة مرة واحدة ([§٦](#٦-قواعد-الترحيل)).

الرصيد لا يُخزَّن. الرصيد = مجموع القيود ([§٧](#٧-الرصيد-يُحسب-ولا-يُخزَّن)).

```
فعل الموظف → معاملة تجارية (order_payment · stock_movement · expense …) → قاعدة ترحيل → journal_entry → journal_lines
```

---

## ٢. ما هو قائم اليوم — ثلاثة دفاتر لا تتكلم مع بعضها

المشروع فيه أصلاً ثلاثة سجلات مالية، كلها مضافٌ إليها لا يُعدَّل، والتصحيح فيها قيد عكسي:

| السجل | يجيب عن | لا يجيب عن |
| --- | --- | --- |
| `order_payments` | كم دفع العميل على هذه الطلبية، وبأي طريقة | **كم في الصندوق الآن؟** كم على العميل عبر طلبياته كلها؟ |
| `stock_movements` + `stock_batches` + `stock_batch_consumptions` | كم على الرف، وبكم اشتُري (FIFO) | **كم علينا للموردين؟** |
| `production_cost_entries` | كم كلّف إنتاج هذه الطلبية — عمالة وماكينة ومصاريف معيارية، وتلف | كم دُفع فعلاً من رواتب وإيجار وكهرباء |

وما لا يوجد أصلاً: **مصروفات**، **دفعات للموردين**، **رأس المال ومسحوبات المالك**، **تحويل بين الصندوق والمصرف**. وتقرير الأرباح القائم (`/reports/profit-loss`) استعلامٌ فوق أعمدة مخبَّأة لا ميزانية وراءه.

**القرار: الدفاتر الثلاثة تبقى كما هي.** هي الحقيقة التشغيلية — الطلبية تعرف حالتها وسدادها منها، والرف يعرف رصيده منها. دفتر الأستاذ **طبقة فوقها تُكتب في نفس المعاملة**، لا بديلٌ عنها ولا نسخة ثانية تُصان بيد أحد. واختبارٌ يثبت أن كل دفتر فرعي يساوي حسابه في الأستاذ ([§٧](#٧-الرصيد-يُحسب-ولا-يُخزَّن)).

---

## ٣. أين يعيش الكود

سياق جديد `app/Domain/Accounting/` بابُه `AccountingService`، **لا يستورد `Order` ولا `Inventory` ولا أي سياق آخر** — الاتجاه واحد: الطلبيات والمخزون يستدعيان المحاسبة، والمحاسبة لا تعرف ما الطلبية. ما تحتاجه من المصدر يصلها أرقاماً في DTO، وما تعرضه عنه تقرأه عبر الـ morph map (`AuditSubject`) لا عبر استيراد.

```
Domain/Accounting/
├── AccountingService.php        الباب: بابٌ لكل حدث تجاري + reverse + القراءات
├── Enums/AccountType.php        asset · liability · equity · revenue · expense
├── Enums/JournalEntryKind.php   مفردات الترحيل كلها — §٦
├── Enums/SystemAccount.php      رموز الحسابات التي تعتمد عليها القواعد (1000 · 1100 · …)
├── Models/{Account, JournalEntry, JournalLine, Expense, VendorPayment}.php
├── Actions/PostJournalEntry.php الكاتب الوحيد للقيود — التوازن يُفحص هنا
├── Actions/ReverseJournalEntry.php
├── Actions/{RecordExpense, ReverseExpense, RecordVendorPayment, …}.php
├── Postings/                    قاعدة ترحيل لكل حدث: DTO داخل → أطراف خارج، بلا قاعدة بيانات
├── Queries/                     الأرصدة، الكشوف، ميزان المراجعة، القوائم
├── DTOs/ · Exceptions/ · Support/Money.php
```

**كاتبٌ واحد.** لا يصل صفٌّ إلى `journal_entries` إلا عبر `PostJournalEntry`، وهو الذي يفحص التوازن ويرمي `JournalEntryNotBalanced`. الأبواب على `AccountingService` كلها ثلاثة أسطر: ابنِ الأطراف من قاعدة الترحيل، ثم اكتب.

**في نفس المعاملة.** كل باب يُستدعى من داخل `DB::transaction` الذي كتب المعاملة التجارية، فإما أن يُكتب الصف وقيده معاً أو لا يُكتب شيء — نفس القاعدة التي تربط `order_payments` بـ`paid_amount`.

نقاط الاستدعاء في الكود القائم، وهي كلها:

| أين | ماذا يُرحَّل |
| --- | --- |
| `Order\Actions\RecordOrderPayment` · `RefundOrderPayment` · `WriteOffOrderBalance` · `ReverseOrderPayment` | صف `order_payments` الذي كُتب للتو |
| `Order\Actions\ChangeOrderStatus` عند الدخول إلى «تم الاستلام» | إيراد الطلبية |
| `Inventory\Actions\RecordStockMovement` | كل حركة مخزون بتكلفتها |
| `Inventory\Actions\RevalueStockBatch` | فرق إعادة التقييم |

لا مكان آخر. و**التحويل الداخلي** بين مخزنين لا قيد له: القيمة لم تتغير، والمخازن ليست حسابات.

---

## ٤. الجداول الثلاثة

### `accounts` — دليل الحسابات

| العمود | النوع | ملاحظة |
| --- | --- | --- |
| `id` | bigint | |
| `code` | string(10) | فريد جزئياً — `1000`، `1100`… يُعرض ويُفرز به |
| `name` | string(100) | عربي |
| `type` | string(20) | `AccountType` — يحدد طبيعة الرصيد ([§٧](#٧-الرصيد-يُحسب-ولا-يُخزَّن)) |
| `party_type` | string(20) nullable | **الحساب المحلَّل**: `customer` أو `vendor`. سطرٌ على هذا الحساب **يجب** أن يحمل طرفاً — ذممٌ بلا عميل رقمٌ بلا صاحب |
| `is_system` | bool | تعتمد عليه قاعدة ترحيل: لا يُعطَّل ولا يُحذف، ورمزه ثابت في `SystemAccount` |
| `is_active` | bool | يُخفى من المنتقيات. تعطيلٌ لا حذف، كالعملاء |
| `timestamps` + `deleted_at` | | القاعدة الثابتة |

**لا عمود `balance`.** ولا `parent_id` الآن: الرمز يحمل التجميع (`1xxx` أصول، `5xxx` مصاريف)، وشجرةٌ فوق عشرين حساباً عبءٌ بلا سؤال يطلبه.

### `journal_entries` — القيد

| العمود | النوع | ملاحظة |
| --- | --- | --- |
| `id` | bigint | **هو رقم القيد** — لا عمود `entry_number`، [§١٢](#١٢-تصحيحات-وملاحظات-على-ما-وصفتَه) |
| `kind` | string(30) | `JournalEntryKind` — أي قاعدة ترحيل أنتجته ([§٦](#٦-قواعد-الترحيل)) |
| `accounting_date` | date | **تاريخ الحدث لا تاريخ التسجيل**: `paid_at` للدفعة، `delivered_at` للإيراد، تاريخ الحركة للمخزون |
| `description` | string(255) | جملة مقروءة: «دفعة من العميل C12 على الطلبية #1226» |
| `source_type` · `source_id` | string(40) nullable · bigint nullable | **لماذا أنا موجود** — اسم النوع من الـ morph map: `order_payment` · `order` · `stock_movement` · `stock_batch_revaluation` · `expense` · `vendor_payment` |
| `reverses_entry_id` | FK self nullable | القيد الذي يعكسه هذا. **فريد جزئياً** — لا يُعكس قيدٌ مرتين |
| `posted_at` | timestamp | متى دخل الدفتر. يساوي `created_at` اليوم، ويفترقان يوم تُضاف مسودات — [س٨](#س٨--مسودات-القيود) |
| `posted_by` | FK users nullable | يُختم من المستخدم الموقَّع، غير قابل للإرسال |
| `timestamps` + `deleted_at` | | |

**الفهارس والقيود:**

- `UNIQUE (source_type, source_id, kind) WHERE source_type IS NOT NULL AND deleted_at IS NULL` — **القانون ٦**: لا يُرحَّل الحدث الواحد مرتين. طلبيةٌ لها قيد إيراد واحد، وصف الدفعة له قيد واحد، وطلبٌ متزامن يصطدم بالفهرس لا بفحصٍ في الكود.
- `UNIQUE (reverses_entry_id) WHERE reverses_entry_id IS NOT NULL AND deleted_at IS NULL`.
- `CHECK` على الشكل: الأنواع اليدوية (`manual` · `transfer` · `capital` · `drawing` · `opening` · `reversal`) مصدرها فارغ، وكل نوع آخر مصدره مطلوب؛ و`reversal` يشير إلى قيد وجوباً.
- `INDEX (accounting_date)`.

### `journal_lines` — الأطراف

| العمود | النوع | ملاحظة |
| --- | --- | --- |
| `id` | bigint | |
| `journal_entry_id` | FK · cascade | |
| `account_id` | FK accounts | |
| `debit` · `credit` | decimal(14,2) default 0 | **أحدهما موجب والآخر صفر، دائماً** — `CHECK ((debit > 0 AND credit = 0) OR (credit > 0 AND debit = 0))` |
| `party_type` · `party_id` | string(40) nullable · bigint nullable | العميل أو المورد الذي يخصّه السطر — من الـ morph map. `CHECK` أن يُملآ معاً أو يُتركا معاً |
| `description` | string(255) nullable | |
| `timestamps` + `deleted_at` | | |

**الفهارس:** `(account_id, journal_entry_id)` لكشف الحساب، و`(party_type, party_id, journal_entry_id)` لكشف العميل والمورد.

**التوازن — القانون ١ — في القاعدة لا في الكود وحده.** `CHECK` لا يرى أكثر من صف. الضمان الصادق في PostgreSQL هو **constraint trigger مؤجَّل إلى لحظة الـ commit** يتحقق أن `SUM(debit) = SUM(credit)` لكل قيد مسّته المعاملة، ويرفض المعاملة كلها إن اختلّ. خمسة عشر سطراً من SQL في الـ migration، ومعها فحص `PostJournalEntry` الذي يعطي الـ 422 المقروء. نفس ترتيب «التحقق للرسالة، القاعدة للضمان» في `RULES.md` §٨.

**الجمود — القانون ٢.** لا مسار `PUT` ولا `DELETE` على القيود أو أطرافها، والنموذجان يرميان `JournalEntryIsImmutable` من `updating` و`deleting`. عمود `deleted_at` موجود لأن كل جدول يحمله والاختبار يفرضه، لا لأن شيئاً يحذف.

**كل الجداول: `Auditable` + `SoftDeletes` + حالة في `AuditSubject` + `GET …/logs`** — القاعدة الثابتة، و`ModelConventionsTest` يفرضها.

---

## ٥. دليل الحسابات المبدئي

ثلاثة وعشرون حساباً. **السبعة عشر المعلَّمة `system` تعتمد عليها قواعد الترحيل ولا تُعطَّل**؛ الباقي مصروفاتٌ يختارها المحاسب في نموذج المصروف، ويزيد عليها ويعطّل منها كما يشاء.

| الرمز | الحساب | النوع | محلَّل | system |
| --- | --- | --- | --- | --- |
| `1000` | الصندوق | asset | | ✓ — طريقة الدفع `cash` |
| `1010` | المصرف | asset | | ✓ — `bank_transfer` و`bank_card` — [س١](#س١--دليل-الحسابات) |
| `1020` | محفظة ليبيانا | asset | | ✓ — `libyana` |
| `1100` | ذمم العملاء | asset | customer | ✓ |
| `1200` | المخزون | asset | | ✓ — يساوي Σ `stock_batches` (المتبقي × التكلفة) دائماً |
| `2000` | ذمم الموردين | liability | vendor | ✓ |
| `3000` | رأس المال | equity | | ✓ — والطرف المقابل لقيد الافتتاح |
| `3100` | مسحوبات المالك | equity | | ✓ |
| `4000` | مبيعات المنتجات | revenue | | ✓ — `items_total` |
| `4100` | إيراد التصميم | revenue | | ✓ — `design_fee` حين يكون التصميم داخلياً |
| `4200` | إيراد التوصيل | revenue | | ✓ — `delivery_price` — [س٦](#س٦--سعر-التوصيل) |
| `4300` | رسوم إضافية على الطلبيات | revenue | | ✓ — `additional_cost` |
| `4900` | خصومات ممنوحة | revenue (عكسي) | | ✓ — `discount`؛ رصيده مدين فيظهر سالباً تحت الإيرادات، وهذا معناه |
| `5000` | تكلفة المواد المباعة | expense | | ✓ — من FIFO |
| `5100` | تلف الإنتاج | expense | | ✓ |
| `5200` | فروق المخزون — جرد وتقييم | expense | | ✓ |
| `5300` | ديون معدومة | expense | | ✓ — شطب الفرق |
| `5400` | مصروف التوصيل | expense | | |
| `5500` | رواتب وأجور | expense | | |
| `5600` | إيجار | expense | | |
| `5700` | كهرباء ومياه واتصالات | expense | | |
| `5800` | مصاريف نقل | expense | | |
| `5900` | مصاريف أخرى | expense | | |

يُزرع بـ`AccountSeeder` من `SystemAccount` — الرموز في enum لا في نصوص متناثرة، فلا يكتب أحد `'1100'` داخل قاعدة ترحيل.

---

## ٦. قواعد الترحيل

**كل صف هنا هو الطبقة التي سمّيتَها «Accounting Rule».** مكتوبة مرة واحدة في `Postings/`، وكل قاعدة دالة نقية: DTO من المصدر يدخل، أطرافٌ تخرج، ولا قاعدة بيانات — فتُختبر بلا `RefreshDatabase`.

### أ. العملاء — المصدر `order_payments` و`orders`

| الحدث | `kind` | مدين | دائن | التاريخ | المبلغ |
| --- | --- | --- | --- | --- | --- |
| دفعة من عميل | `customer_payment` | حساب طريقة الدفع (1000/1010/1020) | 1100 [العميل] | `paid_at` | `amount` |
| ردّ مبلغ | `customer_refund` | 1100 [العميل] | حساب الطريقة | `paid_at` | `amount` |
| شطب فرق | `customer_write_off` | 5300 | 1100 [العميل] | `paid_at` | `amount` |
| إلغاء قيد (صف `reversal`) | `customer_payment_reversal` | **مرآة قاعدة الصف الملغى** | | تاريخ الإلغاء | مبلغه |
| **الاعتراف بالإيراد** — الدخول إلى «تم الاستلام» | `order_revenue` | 1100 [العميل] `grand_total` · 4900 `discount` | 4000 `items_total` · 4100 `design_fee` (إن كان محسوباً) · 4200 `delivery_price` · 4300 `additional_cost` | `delivered_at` | كما حسبه `RecalculateOrderTotals` — يُقرأ ولا يُعاد حسابه |

القيد الأخير متوازن بالضرورة: `grand_total + discount = items + design + delivery + additional` هي معادلة `RecalculateOrderTotals` نفسها. و«تم الاستلام» تُدخَل مرة واحدة على الأكثر (لا شيء يعود منها إلا إلى «تم التسوية»)، فالفهرس الفريد ضمانٌ لا يُختبر إلا في سباق.

**لماذا «تم الاستلام» لا الإنشاء ولا التسوية؟** لأنها لحظة انتقال البضاعة، وهي ما يفعله تقرير الأرباح القائم أصلاً. قبلها الفاتورة تتغير بحرية — بنود، خصم، مدينة — ولا قيد لها. والعربون الذي يُدفع عند الطلب يقيَّد على ذمة العميل فيصير **رصيده دائناً** حتى التسليم: رصيدٌ سالب في كشف العميل معناه «عنده مال عندنا». قاعدة واحدة للدفعة أينما وقعت — [س٣](#س٣--العربون).

**والإلغاء التام بعد عربون** لا قيد له: الإيراد لم يُقيَّد أصلاً، والعربون باقٍ على ذمة العميل حتى يُردّ (قيد ردّ) أو يُستهلك في طلبية تالية.

### ب. المخزون والمشتريات — المصدر `stock_movements` و`stock_batch_revaluations`

قاعدة واحدة تغطي الرف كله: **كل حركة مخزون تُرحَّل بأثرها على القيمة**، والتكلفة تُقرأ مما كتبه FIFO لا تُحسب ثانيةً.

| الحركة | `kind` | مدين | دائن | المبلغ |
| --- | --- | --- | --- | --- |
| توريد `purchase_arrival` | `inventory_receipt` | 1200 | 2000 [المورد] | الكمية × تكلفة الوحدة **الواصلة** (بعد توزيع التكاليف الإضافية) — [س٥](#س٥--التكاليف-الإضافية-لأمر-الشراء) |
| صرف لطلب `order_fulfillment` | `cost_of_goods` | 5000 | 1200 | Σ `stock_batch_consumptions.total_cost` للحركة |
| إرجاع بعد إلغاء `order_reversal` | `inventory_return` | 1200 | 5000 | ما رُدّ إلى الدفعات |
| تلف `scrap_loss` | `scrap_loss` | 5100 | 1200 | Σ consumptions |
| تسوية جرد — زيادة | `inventory_adjustment` | 1200 | 5200 | الكمية × التكلفة المكتوبة |
| تسوية جرد — نقص | `inventory_adjustment` | 5200 | 1200 | Σ consumptions |
| تحويل داخلي | — | **لا قيد** | | |
| إعادة تقييم دفعة | `inventory_revaluation` | 1200 أو 5200 حسب الإشارة | الآخر | (الجديدة − القديمة) × الكمية |

وما يترتب على ذلك بلا كود إضافي: تصحيح الكمية عند «جاهزة» (`RestateOrderStockDeduction`) إرجاعٌ كامل ثم صرفٌ جديد — قيدان بالقاعدة نفسها. وحركةٌ قيمتها صفر (تسوية بلا تكلفة، دفعة افتتاحية على صفر) **لا قيد لها**: قيدٌ بلا مبلغ ليس حدثاً.

**ذمة المورد تنشأ عند الاستلام لا عند أمر الشراء.** أمر الشراء التزامٌ لا دَين؛ ما يُقيَّد هو ما وصل فعلاً وبتكلفته الواصلة.

### ج. المعاملات الجديدة — ما لا يوجد اليوم

| الحدث | `kind` | مدين | دائن |
| --- | --- | --- | --- |
| مصروف | `expense` | حساب المصروف المختار (5400–5900) | حساب الدفع (1000/1010/1020)، أو 2000 [الطرف] إن كان آجلاً |
| عكس مصروف | `expense_reversal` | مرآة | |
| دفعة لمورد | `vendor_payment` | 2000 [المورد] | حساب الطريقة |
| ردّ من مورد · إلغاء قيد | `vendor_refund` · `vendor_payment_reversal` | مرآة | |
| تحويل بين الحسابات النقدية | `transfer` | إلى | من |
| إيداع رأس مال | `capital` | 1000/1010 | 3000 |
| سحب المالك | `drawing` | 3100 | 1000/1010 |
| قيد يدوي | `manual` | كما كُتب | كما كُتب |
| عكس أي قيد | `reversal` | مرآة القيد المشار إليه | |
| قيد الافتتاح | `opening` | [§٩](#٩-الافتتاح) | |

الأربعة الأولى معاملات تجارية لها جداولها — `expenses` و`vendor_payments` — على شكل `order_payments` حرفياً: نوع الصف، مبلغ موجب، طريقة، `reverses_*_id` فريد جزئياً، `recorded_by` مختوم. **`transfer` و`capital` و`drawing` قيود يدوية بنوع مسمّى**: نفس المسار ونفس الصلاحية، والنوع لأجل القراءة والتقارير لا لأجل قاعدة مختلفة.

### د. ما لا يُرحَّل — عن قصد

**التكاليف المعيارية** (`production_cost_entries` من نوع عمالة/ماكينة/مصاريف) **لا تدخل الأستاذ.** هي أرقام إدارية لحساب هامش الطلبية، لا مالاً تحرك؛ المال الحقيقي — الرواتب والإيجار والكهرباء — يدخل كمصروف حين يُدفع. لو رُحّل الاثنان لحُسب الراتب مرتين. فيبقى تقرير الأرباح القائم **تقرير هامشٍ إداري** بالتكلفة المعيارية، وقائمة الدخل الجديدة **قائمة مالية** بالمصروفات الفعلية: تقريران لسؤالين — [س٤](#س٤--التكاليف-المعيارية).

---

## ٧. الرصيد يُحسب ولا يُخزَّن

| نوع الحساب | الرصيد |
| --- | --- |
| asset · expense | `SUM(debit) − SUM(credit)` |
| liability · equity · revenue | `SUM(credit) − SUM(debit)` |

لا عمود رصيد على `accounts` ولا على العميل ولا على المورد — ولا حتى cache الآن. حجم هذا العمل بضعة آلاف سطر في السنة، وPostgreSQL يجمعها على الفهرس في أجزاء من الثانية. يوم يثقل، يُضاف جدول لقطات شهرية **كذاكرة** يُعاد بناؤه من الأسطر ويثبت اختبارٌ مطابقته — مؤجَّل بسببه.

**الاستعلامات** — `Accounting/Queries/`:

| الاستعلام | يجيب عن |
| --- | --- |
| `AccountBalanceQuery` | رصيد حساب حتى تاريخ، ولطرف بعينه إن طُلب |
| `CashPositionQuery` | **«كم عندنا الآن؟»** — أرصدة 1000/1010/1020 معاً. شاشة الخزينة |
| `AccountLedgerQuery` | كشف حساب: أسطره مرقّمة برصيد متحرك |
| `PartyStatementQuery` | **كشف حساب عميل أو مورد** — يُغلق بند «مجموع ما على العميل من مال» في المؤجَّل |
| `TrialBalanceQuery` | ميزان المراجعة حتى تاريخ — مجموع المدين = مجموع الدائن، أو الدفتر مكسور |
| `IncomeStatementQuery` | قائمة الدخل لفترة: إيرادات − مصروفات |
| `BalanceSheetQuery` | الميزانية حتى تاريخ. ذمم العملاء تُعرض مقسومة: الأرصدة المدينة «لنا»، والدائنة «عرابين» |

**ثلاث مطابقات يثبتها الاختبار على كل مسار كتابة**، وتظهر كسطر «فرق» في التقارير إن اختلّت:

1. `1200` = Σ `stock_batches.quantity_remaining × unit_cost`.
2. `1100` لعميل = Σ (طلبياته المسلَّمة: `grand_total − paid − written_off`) − Σ (مدفوعاته على غير المسلَّمة).
3. `2000` لمورد = Σ توريداته منذ الافتتاح − Σ دفعاته.

---

## ٨. القوانين السبعة — أين يُنفَّذ كل واحد

| القانون | الآلية | الضمان في القاعدة |
| --- | --- | --- |
| ١ · مدين = دائن | `PostJournalEntry` يرمي `JournalEntryNotBalanced` | constraint trigger مؤجَّل يرفض الـ commit |
| ٢ · لا تعديل ولا حذف | لا مسار `PUT`/`DELETE`؛ `JournalEntryIsImmutable` من النموذج | اختبار يثبت غياب المسارين |
| ٣ · الخطأ يُعكس ثم يُعاد | `ReverseJournalEntry`: مرآة الأطراف، `reverses_entry_id`، سبب إجباري | فهرس فريد جزئي — لا يُعكس قيد مرتين |
| ٤ · لا أرصدة يدوية | لا عمود رصيد في المخطط كله | — |
| ٥ · كل قيد يعرف مصدره | `source_type` + `source_id` + `kind` | `CHECK` على الشكل |
| ٦ · لا ترحيل مزدوج | `JournalEntry::forSource()` قبل الكتابة | `UNIQUE (source_type, source_id, kind)` |
| ٧ · DECIMAL | `decimal(14,2)` + `bcmath` عبر `Money` — قاعدة المشروع أصلاً | نوع العمود |

---

## ٩. الافتتاح

**يوم انتقال، لا إعادة كتابة للتاريخ.** ما قبل التاريخ يُلخَّص في **قيد افتتاح** واحد؛ ما بعده يُرحَّل حياً. لا نولّد قيوداً من `order_payments` القديمة: طرفها الآخر — أين ذهب المال؟ كم دُفع للموردين؟ — لم يُسجَّل قط، وقيودٌ نصفها مخمَّن ذممٌ كاذبة.

أمر كونسول `accounting:open-books --date= --cash= --bank= --libyana=` يكتب قيد الافتتاح:

| الطرف | من أين |
| --- | --- |
| 1000 · 1010 · 1020 | **تُعدّ وتُكتب** — جرد الدرج وكشف المصرف يوم الانتقال |
| 1200 المخزون | **تلقائي** — Σ الدفعات القائمة بتكلفتها |
| 1100 لكل عميل | **تلقائي** — من الطلبيات: ما على المسلَّمة، وما دُفع مقدماً على غير المسلَّمة |
| 2000 لكل مورد | **يُكتب** — قيود افتتاح يدوية بعد الأمر، لأن النظام لم يسجّل يوماً ما دُفع للموردين |
| 3000 رأس المال | **الفرق** — ما يوازن القيد |

**لا قيد بتاريخ يسبق الافتتاح** — الحارس في `PostJournalEntry` يقرأ تاريخ قيد الافتتاح، بلا جدول إعدادات. ودفعةٌ قديمة تُلغى بعد الافتتاح تعمل بقاعدتها العادية (مرآة الدفعة على ذمة العميل والصندوق): الافتتاح احتسبها، فعكسها الآن يصحّحه — و`reverses_entry_id` يبقى فارغاً لأن قيد الأصل لم يوجد.

---

## ١٠. الصلاحيات والـ API

**أربع صلاحيات**، على نسق مدفوعات الطلبية: المال الخارج قرارٌ غير المال الداخل.

| الصلاحية | تعطي |
| --- | --- |
| `accounting.view` | الحسابات، القيود، الكشوف، الخزينة، التقارير المالية |
| `accounting.record` | مصروف، دفعة لمورد، تحويل بين الحسابات — **المال الخارج** |
| `accounting.reverse` | عكس قيد أو مصروف أو دفعة مورد |
| `accounting.manual` | القيد اليدوي، رأس المال والمسحوبات، دليل الحسابات، الافتتاح — باب المحاسب وحده |

تسجيل دفعة **من** عميل يبقى تحت `orders.payments.*` كما هو؛ الترحيل يحدث خلفه بلا صلاحية إضافية.

```
GET    /accounting/accounts                         · POST · PUT · PATCH …/activation
GET    /accounting/accounts/{account}/ledger?from&to
GET    /accounting/journal-entries?from&to&kind&account_id&party_type&party_id&source_type&source_id
GET    /accounting/journal-entries/{entry}          · GET …/logs
POST   /accounting/journal-entries                  kind ∈ manual | transfer | capital | drawing
POST   /accounting/journal-entries/{entry}/reverse  { reason }
GET    /accounting/expenses  · POST  · POST …/{expense}/reverse
GET    /accounting/statements/{party_type}/{party_id}?from&to
GET    /accounting/reports/cash-position
GET    /accounting/reports/trial-balance?as_of
GET    /accounting/reports/income-statement?from&to
GET    /accounting/reports/balance-sheet?as_of
GET    /vendors/{vendor}/payments  · POST  · POST …/{payment}/reverse
```

الغلاف والأخطاء والـ Resources كما في بقية الـ API؛ الأرقام نصوص `"1250.00"` كما في كل مكان.

---

## ١١. الأسئلة

### س١ — دليل الحسابات

هل القائمة في §٥ تكفي بداية؟ وتحديداً: مصرف واحد أم أكثر (ذكرتَ «مصرف الجمهورية» — أُسمّي `1010` به؟)، وهل **البطاقة المصرفية** تصل إلى نفس حساب الحوالة؟

**التوصية:** القائمة كما هي، `1010` = مصرف الجمهورية، والبطاقة إليه. مصرفٌ ثانٍ = حساب ثانٍ يوم يوجد.

**ردك:**

### س٢ — لحظة الإيراد

«تم الاستلام» (التوصية — ما يفعله تقرير الأرباح اليوم)، أم عند إنشاء الطلبية، أم عند «تم التسوية»؟

**ردك:**

### س٣ — العربون

التوصية: قاعدة واحدة — كل دفعة على ذمة العميل، ورصيده الدائن قبل التسليم هو العربون؛ الميزانية تعرض الأرصدة الدائنة سطراً مستقلاً «عرابين العملاء». البديل الدفتري الصِّرف: حساب التزام مستقل `2100 دفعات مقدمة من العملاء` يُقيَّد عليه العربون ثم يُحوَّل إلى الذمة لحظة التسليم — قيدٌ إضافي تلقائي لكل طلبية، وسؤالان بدل سؤال في كشف العميل.

**ردك:**

### س٤ — التكاليف المعيارية

التوصية: خارج الأستاذ (§٦ د). البديل: ترحيلها إلى `5010/5020` مقابل حساب «تكاليف محمَّلة» يُسوَّى شهرياً مع الفعلي — أدقّ في محاسبة التكاليف، وأثقل بحساب تسويةٍ لا يفهمه غير المحاسب.

**ردك:**

### س٥ — التكاليف الإضافية لأمر الشراء

جمرك ونقل وتفريغ على أمر الشراء: تُدفع **للمورد نفسه** أم لجهات أخرى؟ إن كانت لغيره فذمة المورد = سعر البضاعة فقط، والباقي مصروفٌ مستقلّ أو ذمة جهة أخرى. الترحيل المقترح يضعها كلها على ذمة المورد لأنها تدخل التكلفة الواصلة.

**ردك:**

### س٦ — سعر التوصيل

إيراد (`4200`) ومصروف التوصيل (`5400`) منفصلان فيظهر صافيهما — التوصية. أم يمرّ التوصيل «كما هو» إلى شركة التوصيل فلا يُعدّ إيراداً؟

**ردك:**

### س٧ — الافتتاح

تاريخ الانتقال؟ ومن يعدّ الدرج والمصرف وليبيانا ذلك اليوم، ومن يعرف ما على الشركة لكل مورد؟

**ردك:**

### س٨ — مسودات القيود

هل تريد قيداً يُكتب اليوم ويعتمده غيرك غداً (`draft` → `posted`)؟ التوصية: **لا الآن** — القيد يولد منشوراً، وعمود `status` لا يقول إلا `posted` عمودٌ ينتظر من ينسى قراءته. يُضاف مع الاعتماد يوم يُطلب.

**ردك:**

### س٩ — إقفال الفترات

منع أي قيد بتاريخ داخل شهر أُقفل. التوصية: مؤجَّل — الحارس نفسه الذي يمنع ما قبل الافتتاح يمتدّ إلى «آخر شهر مقفل» يوم يُطلب.

**ردك:**

### س١٠ — ترقيم القيود

`id` هو الرقم، وقد تظهر فجوة بعد معاملة مُلغاة. ترقيمٌ متصل بلا فجوات يحتاج عدّاداً مقفولاً في كل كتابة، ولا يُطلب إلا لمراجعة رسمية. التوصية: `id`.

**ردك:**

### س١١ — نقد لدى المندوب

بين «تم الاستلام» و«تم التسوية» المال في يد المندوب. التوصية: لا حساب له الآن — الدفعة تُسجَّل بطريقتها حين تُسجَّل. إن كان المندوبون يحملون مبالغ أياماً وتريد معرفة «كم عند كل مندوب»، فهو حساب `1050` محلَّل بالموظف وقيدٌ وسيط عند التسليم.

**ردك:**

### س١٢ — من يستخدمه

هل يوجد محاسب يفهم القيد اليدوي ودليل الحسابات، أم كل المستخدمين موظفو مكتب؟ يقرّر ما يظهر في التطبيق: شاشات مسمّاة (مصروف، تحويل، كشف حساب) وحدها، أم معها شاشة القيود الخام.

**ردك:**

---

## ١٢. تصحيحات وملاحظات على ما وصفتَه

- **`journal_entries.status` و`posted_at`:** أخذتُ `posted_at` وتركتُ `status` — [س٨](#س٨--مسودات-القيود).
- **`entry_number`:** المشروع يرقّم بالـ `id` في كل مكان (`C1`، والطلبيات من ١٢٠٠)؛ قيدٌ رقمه `id` يُعرض «قيد #45» — [س١٠](#س١٠--ترقيم-القيود).
- **`party_type/party_id` على الأسطر:** كما وصفتَ، وزدتُ `accounts.party_type` ليقول الحساب نفسه إنه يشترط طرفاً، فلا يُكتب سطر ذمم بلا صاحب.
- **«ثلاثة جداول»:** صحيح للأستاذ. ما يُضاف فوقها جدولان لمعاملتين لا وجود لهما اليوم — `expenses` و`vendor_payments` — لأن قاعدة المشروع أن المعاملة التجارية لها صفّها والقيد يشير إليه، لا أن يُكتب المصروف قيداً مباشرة.
- **`source_type` ليس نصاً حراً:** قيم الـ morph map في `AuditSubject` — نفس الأسماء المنشورة في سجل التدقيق.
- **الفرق بين الإلغاء والردّ** في [PAYMENTS-DESIGN.md](PAYMENTS-DESIGN.md) باقٍ في الأستاذ: الردّ قيدٌ حقيقي (المال خرج)، والإلغاء مرآةٌ تشير إلى الأصل.
- **`Money`:** يوجد اليوم نسختان (`Order\Support\Money` و`PurchaseOrder\Support\Money`)؛ المحاسبة تأخذ نسختها على النمط نفسه، وتوحيدها في `App\Support` عملٌ مستقلّ — في المؤجَّل.

---

## ١٣. الافتراضات المطبَّقة

ما لم يُجَب بغيره:

1. الدينار الليبي وحده. لا عمود عملة — يوم يُطلب هو عمودٌ وسعر صرف على السطر.
2. السنة المالية = السنة الميلادية؛ الإقفال السنوي (ترحيل صافي الدخل إلى أرباح محتجزة `3900`) مؤجَّل.
3. تاريخ القيد يقبل الماضي كما تقبله الدفعة اليوم، ويرفض ما قبل الافتتاح.
4. الحسابات المعلَّمة `system` لا تُعطَّل ولا تُحذف، ورموزها ثابتة في الكود.
5. التقرير القائم `/reports/profit-loss` يبقى كما هو باسمه ومعناه.

---

## ١٤. خطة التنفيذ — شرائح

كل شريحة: اختبار أولاً (AAA)، ثم الكود، ثم `pint` على الملفات المسمّاة، ثم السلسلة خضراء قبل الوصل.

| # | الشريحة | تعطي |
| --- | --- | --- |
| ١ | **الأستاذ** — الجداول الثلاثة، الـ enums، `PostJournalEntry` + `ReverseJournalEntry`، `AccountSeeder`، الـ trigger، الصلاحيات، القراءة والقيد اليدوي عبر الـ API، `AuditSubject`، الاختبارات البنيوية (توازن، جمود، لا ترحيل مزدوج) | دفتر يعمل، بلا مصادر بعد |
| ٢ | **العملاء** — الأبواب الأربعة لمدفوعات الطلبية + إيراد «تم الاستلام»، ومطابقة `1100` | كشف حساب عميل، و«كم في الصندوق» من دفعات العملاء |
| ٣ | **المخزون** — `RecordStockMovement` و`RevalueStockBatch`، ومطابقة `1200` | ذمم الموردين تنشأ، وتكلفة البضاعة تُقيَّد |
| ٤ | **المصروفات والتحويلات** — `expenses`، والقيود المسمّاة (`transfer`/`capital`/`drawing`) | الخزينة صادقة |
| ٥ | **دفعات الموردين** — `vendor_payments` على شكل `order_payments` | ذمة المورد تُسدَّد |
| ٦ | **الافتتاح** — `accounting:open-books` والحارس الزمني | التشغيل الفعلي |
| ٧ | **التقارير** — الاستعلامات السبعة والـ API | ميزان، دخل، ميزانية، كشوف |
| ٨ | **التطبيق** — `features/accounting`: الخزينة، المصروفات، كشف الحساب من شاشة العميل والمورد، التقارير | الشاشات |

الشريحتان ١ و٢ تُبنيان قبل أجوبة س٥–س٧؛ الباقي بعدها.

---

## ١٥. ما يذهب إلى المؤجَّل

مسجَّل في [BACKLOG.md § المحاسبة](BACKLOG.md#المحاسبة): لقطات الأرصدة الشهرية، إقفال الفترات، الترقيم المتصل، الإقفال السنوي، التكاليف المحمَّلة، نقد لدى المندوب، ربط الرواتب، تخصيص دفعة المورد على أمر شراء بعينه، صورة إيصال المصروف، توحيد `Money`.

---

*آخر تحديث: 2026-09-03.*
