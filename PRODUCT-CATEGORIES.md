# تصنيفات المنتجات

الكتالوج على [daaya.ly](https://daaya.ly/catalog.html) يعرض المنتجات تحت ثلاثة تصنيفات: **أكياس**،
**علب وكراتين التغليف**، **ستيكرات ومطبوعات أخرى**. النظام لم يكن يعرف عنها شيئاً — كان في المنتج
حقل واحد اسمه «التصنيف» وهو في الحقيقة مطبوعة/سادة.

## الكلمة

| المعنى | قبل | الآن |
| --- | --- | --- |
| أكياس / علب / ستيكرات | — | **«التصنيف»** — `ProductCategory` |
| مطبوعة / سادة | «التصنيف» ثم «النوع» | **سطران في نفس الجدول** |

الحلّ الأول كان تسميتين: «النوع» لمطبوعة/سادة و«التصنيف» للكتالوج. عاش ذلك إصداراً واحداً، وكان
حقلين يسألان السؤال نفسه تقريباً — استمارة فيها اختياران متجاوران، وصفحة منتجات فيها صفّا رقائق
فوق بعضهما. فحُذف «النوع»، ودخلت **مطبوعة** و**سادة** قائمة التصنيفات، وبقي في النظام **مفهوم
واحد** يُصنَّف به المنتج.

### لماذا كان حذفه سليماً

«النوع» **لم يدخل في أي حساب**: لا في `QuoteProductPrice`، ولا في تسعير الطلبيات، ولا في معدلات
تكلفة التصنيع، ولا في المخزن. كان يُتحقَّق منه، ويُحفظ، ويُفلتَر به، ويُعرض — لا أكثر. أما ما كان
يقوله فعلاً عن المحاسبة فتقوله `pricing_unit` وحدها: الكيس السادة يُباع بالكيلو لأنه يُباع
بالكيلو، لا لأن بجانبه كلمة تقول ذلك.

**والمعلومة لم تضع.** الترحيل الذي أسقط العمود ينقل — قبل الإسقاط، في اللحظة الوحيدة التي تكون
فيها الحقيقتان موجودتين معاً — كل منتج بلا تصنيف إلى «مطبوعة» أو «سادة» حسب نوعه القديم. ومنتج
صنّفه أحدهم بيده يبقى حيث وضعه.

**السلك تغيّر هذه المرة.** اختفى العمود `category` من `products`، واختفى `category` و
`category_label` من مورد المنتج، واختفى `?category=` من الفلترة. الموقع لا يُغذَّى من هذا الـ API
بعد — وهو بند مؤجَّل في آخر هذا الملف — فلا مستهلك خارجي انكسر.

## القرارات

| السؤال | القرار |
| --- | --- |
| كم تصنيفاً للمنتج | **واحد، إجباري** — لا يُحفظ منتج بلا تصنيف |
| من يضيف التصنيفات | الأدمن، CRUD كامل: إضافة، تعديل، إيقاف، حذف |
| الصلاحيات | `products.view` للقراءة و`products.manage` للإدارة — لا زوج جديد |
| المنتجات الحالية | يُصنّفها الترحيل حسب نوعها القديم، وما بقي منها تملؤه `ProductCategorySeeder` |
| قائمة التصنيفات | أكياس · علب وكراتين التغليف · ستيكرات ومطبوعات أخرى · **مطبوعة** · **سادة** |

### إجباري في الطلب، واسع في العمود

العمود `product_category_id` **قابل لأن يكون فارغاً** بينما الحقل **مطلوب** في `StoreProductRequest`
و`UpdateProductRequest`. ليس تناقضاً: المنتجات المسجّلة قبل هذه الميزة موجودة فعلاً، وجعل العمود
`NOT NULL` كان سيتطلّب اختراع تصنيف «غير مصنّف» يلوّث الكتالوج إلى الأبد. البذرة تملأها كلها، وأي
منتج قديم بقي فارغاً يُجبَر صاحبه على اختيار تصنيف أول مرة يفتح استمارته.

### لماذا لا صلاحية جديدة

`business_fields.*` لها زوج مستقل لأن **كل** دور يحتاج قراءتها — استمارة العميل لا تُملأ بدونها —
بينما تنسيق القائمة عمل نادر. التصنيفات ليست كذلك: من يقرأ المنتجات يقرأ تصنيفاتها، ومن يعدّل
المنتجات يرتّب تصنيفاتها. زوجٌ ثالث كان سيكون خانة تُؤشَّر دائماً مع `products.manage`.

### الحذف مرفوض إذا كان مستعملاً

نفس قاعدة `BusinessField`: التصنيف الذي يشير إليه منتج واحد لا يُحذف — يُوقَف. الحذف للسطر الذي ما
كان يجب أن يوجد أصلاً (خطأ إملائي، تكرار). والمنتجات المحذوفة حذفاً ناعماً تُحسب ضمن «مستعمل»، لأن
منتجاً يُستعاد لاحقاً يجب ألّا يجد تصنيفه قد اختفى.

## النقاط

```
GET    /api/v1/product-categories                 products.view
POST   /api/v1/product-categories                 products.manage
GET    /api/v1/product-categories/{id}            products.view
PUT    /api/v1/product-categories/{id}            products.manage
PATCH  /api/v1/product-categories/{id}/activation products.manage
DELETE /api/v1/product-categories/{id}            products.manage
GET    /api/v1/product-categories/{id}/logs       logs.view
```

وعلى المنتجات: `product_category_id` في الإنشاء والتعديل، و`?product_category_id=` في الفلترة،
و`product_category` كائناً في المورد.

## في التطبيق

- شاشة **«تصنيفات المنتجات»** من القائمة الجانبية، على شكل شاشة «مجالات العمل» نفسها.
- **حقل إجباري** في استمارة المنتج — وهو سؤال التصنيف الوحيد فيها.
- **صفّ رقائق واحد** في صفحة المنتجات، يظهر فقط حين يوجد أكثر من تصنيف.
- **شارة** في زاوية بطاقة المنتج تحمل اسم التصنيف. كانت تحمل «النوع» بأيقونة ولون لكل قيمة، وذلك
  كان ممكناً لأن القيمتين اثنتان يعرفهما التطبيق؛ التصنيف الذي يضيفه المتجر غداً اسم لم يره هذا
  البناء، فصارت أيقونة واحدة ولون واحد — والشارة التي تخمّن أسوأ من التي لا تخمّن.

## الثلاثة التي كانت مؤجَّلة

### صورة لكل تصنيف

أعمدة على السطر لا جدولاً: للمنتج صور كثيرة إحداها رئيسية، وللتصنيف صورة واحدة — وجدولٌ لعلاقة
واحد‑لواحد يشتري وصلة وترتيباً لا يُستعملان أبداً. القرص يُسجَّل مع كل سطر كعادة المشروع، فتبديل
`MEDIA_DISK` إلى S3 يترك كل صورة قديمة تُقرأ من مكانها.

**الاستبدال يحذف القديمة**، بخلاف تصميم العميل: لا شيء يشير إلى صورة تصنيف كما تشير الطلبية إلى
تصميم، فالاحتفاظ بكل نسخة مستبدلة نموٌّ بلا قارئ. والحذف عمليةٌ لا تفشل الطلب: يُكتب السطر أولاً
وتُمسح البايتات بعده — فشلٌ يترك ملفاً يتيماً على القرص، وهو أرخص الفشلين.

```
POST   /api/v1/product-categories/{id}/image   multipart, products.manage
DELETE /api/v1/product-categories/{id}/image   products.manage
```

### ترتيب بالسحب

`PATCH /product-categories/order` يأخذ `ids` كاملة بالترتيب المطلوب. **الترتيب كله في نداء واحد**:
السحبة تحرّك بطاقة وتعيد ترقيم كل ما بعدها، ونداءٌ لكل صفٍّ تحرّك يعني رشقة كتابات مكان واحدة —
واتصالاً ينقطع في منتصفها يترك القائمة بترتيب لم يختره أحد. المعرّفات غير المذكورة تبقى في أماكنها،
فشاشةٌ تعرض صفحة واحدة ترتّبها دون أن تدّعي شيئاً عن البقية.

في التطبيق: مقبض سحب بجانب كل بطاقة — لا ضغطة مطوّلة، لأن البطاقة نفسها تفتح ورقة التعديل بالنقر.
والسحب معطّل أثناء البحث وحين تكون هناك صفحات أخرى: لا يُرتَّب ما لا يُرى.

### تصنيفات فرعية

`parent_id` بمستوى واحد: «أكياس» تحمل «أكياس ورقية»، والابن لا يحمل أبناء. الحدّ قرار لا سهو —
لا شيء في الكتالوج بثلاثة مستويات، وشجرةٌ بعمق مفتوح تكلّف كل شاشة رسماً تعاودياً وكل استعلام
CTE لشكلٍ لم يطلبه أحد. القاعدة في الطلب حيث تُشرَح، والعمود مرجع ذاتي عادي، فرفع الحدّ لاحقاً
تغييرُ تحقّقٍ لا ترحيل.

**المنتج يُصنَّف تحت ورقة فقط.** تصنيفٌ له أبناء عنوانٌ لا خانة — والسماح بالاثنين يجعل «كم منتجاً
تحت أكياس؟» سؤالين: «مباشرة» و«بالمجموع»، ويُلزم كل عدد في كل شاشة أن يقول أيّهما يعني.

يترتّب على ذلك:

- **الفلترة بأبٍ تشمل أبناءه** — وإلا أجاب «أكياس» بـ«لا منتجات» وتحته فرعٌ ممتلئ.
- **`total_products_count`** هو ما تعرضه الشاشة، و`products_count` يبقى المباشر (صفرٌ لأبٍ بحكم
  البناء)، و`children_count` هو ما يقول إن السطر عنوان.
- **`leaf_only=1`** ما يطلبه منتقي الاستمارة، ومعه لا يُعرض فرعٌ خلف أبٍ موقوف: الأب أُوقف ليخرج
  ذلك الجزء من التداول، واحترام راية الابن وحدها تطبيقٌ لنصف القرار.
- **الحذف يُرفض بالأبناء قبل المنتجات**، ورسالةً مستقلة: أبٌ يُحذف من تحت أبنائه يتركهم يشيرون إلى
  سطرٍ لا تعيده الواجهة.

**واجهة إدارة الشجرة مؤجَّلة عمداً** — التطبيق لا يضيف فرعاً ولا ينقله بعد. ما يفعله: يقرأ
`parent_id` فلا يرسم ابناً كأنه جذر، ويطلب `leaf_only=1` في المنتقي، ويعرض عدد الفروع على البطاقة.

## ما زال مؤجَّلاً

- **إدارة الشجرة من التطبيق**: إضافة فرع، ونقله بين الآباء. الخادم يقبلهما اليوم.
- **الكتالوج يُغذَّى من هذا الـ API**: الموقع ما زال ملفات ثابتة، وهو ما يجعل حقول الصورة بلا قارئ
  خارجي حتى الآن.
