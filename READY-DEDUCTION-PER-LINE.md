# الكمية المخصومة من المخزن — تُسأل عند «جاهزة»، لا عند إنشاء الطلبية

> **مُنفَّذ.** الخادم والتطبيق معاً. `php artisan test` **١٣٠٨/١٣٠٨**، و`flutter test` **١٣١٢/١٣١٢**.

---

## ١ · المشكلة

منتج يُباع **بالقطعة** ويُخزَّن **بالكيلوغرام**. قلتَ للزبون «٥٠٠ قطعة»؛ الرفّ يُعدّ بالكيلوغرام.
لا معامل يحوّل أحدهما إلى الآخر — أكياس المقاس الواحد تختلف أوزانها، ولهذا يُوزن الرفّ بدل أن
يُعدّ. فمن أين يعرف النظام كم يخصم؟

لم يكن يعرف، وكان يخصم على أي حال:

```php
// OrderItem::producedQuantity()
return $this->warehouse_quantity === null ? (string) $this->quantity : …
```

فإن لم يُدخل أحد وزناً، خرج **٥٠٠** من رفٍّ وحدته الكيلوغرام. ٥٠٠ قطعة صارت ٥٠٠ كيلوغرام، بحركة
مخزون موقّعة باسم موظف، ولا سطر في أي شاشة يقول ذلك. ولقطة شاشة «تغيير الحالة» كانت تطبع الكذبة
نفسها في تلميح المخزن:

```
يُخصم منه ما تستهلكه هذه الطلبية من المخزون:
• 25*35 — 250.000 كيلوغرام      ← والمباع 250 قطعة
```

والحقل الذي كان يُفترض أن يمنع هذا كان **اختيارياً في شاشة الطلبية الجديدة**: يُسأل عنه موظفٌ على
الهاتف، قبل الطباعة، وقبل أن يوجد الطرد الذي يصفه، وقبل أن يقترب أحد من ميزان.

---

## ٢ · القاعدة الجديدة

**السؤال يُطرح حيث توجد إجابته: عند نقل الطلبية إلى «جاهزة»، من الشخص الذي أمامه الطرد والميزان.**

**ويُطرح فقط حين يكون بلا إجابة تلقائية** — أي حين تختلف وحدة المخزون عن وحدة البيع:

| السطر | ما يحدث |
|---|---|
| يُباع بالقطعة ويُخزَّن بالقطعة (تسعة أسطر من عشرة) | **لا حقل**. المباع هو الخارج، وصندوقٌ يطلب إعادة كتابة رقمٍ تحمله الطلبية أصلاً لا يفعل شيئاً سوى أنه قد يختلف عنه |
| يُباع بالكيلوغرام ويُخزَّن بالكيلوغرام | **لا حقل**. نفس السبب |
| يُباع بالقطعة ويُخزَّن بالكيلوغرام | **حقل إجباري**، واحد لكل بند، بوحدة الرفّ |

الحقل كما يصل التطبيق:

```
المخصوم من 25*35 (كيلوغرام)          ← الوحدة في الاسم، لا في ملاحظة تحته
المباع 500.000 قطعة — والمخزن يُنقص بالكيلوغرام
```

* **إجباري** ما دام `stock_deducted_at` فارغاً — نفس البوابة التي يقف عليها حقل «المخزن» فوقه.
* **يفتح على قيمة إن وُجدت**: طلبية أُخذت أيام كانت الشاشة القديمة تسأل تحمل ما قيسَ حينها، وإعادة
  السؤال من صندوق فارغ تدعو إلى رقمٍ ثانٍ لطردٍ وُزن مرة واحدة.
* **تلميح المخزن لم يعد يخترع رقماً**: السطر غير الموزون يُسمّى ولا يُرقَّم —
  `• 25*35 — بالكيلوغرام، حسب ما تُدخله أدناه`.

### ٢.١ التطبيق لم يحتج سطر Dart واحداً

شاشة الحالة ترسم `available_transitions[].fields` كما تصل، وحقل `number` له ودجت أصلاً باسمٍ
وتلميحٍ وقيمة ابتدائية (بُني لحقول «الواصل من نواقص»). فالحقول الجديدة تظهر في النسخ المثبَّتة
بلا إصدار — والتغيير الوحيد في التطبيق هو **حذف** الصندوق القديم.

---

## ٣ · الأرضية تحت النموذج

النموذج يمكن تخطّيه: أمر Console، أو مستورد، أو عميلٌ قديم. فـ`DeductOrderStock` يرفض الآن قبل أن
يخرج أي سطر من المخزن:

```php
LineNeedsAMeasuredStockQuantity  →  422
«25*35» يُخزَّن بالكيلوغرام ويُباع بالقطعة — أدخل الكمية التي تخرج من المخزن
```

كل الأسطر غير الموزونة تُسمّى دفعةً واحدة لا واحداً بعد الآخر — نفس منطق `OrderStockShortfall`
المجاور له. والخطأ يُرفع على حقل كل بند (`fields.warehouse_quantity_{id}`) فيقع الكلام تحت الصندوق
الفارغ نفسه.

---

## ٤ · ما تغيّر

### الخادم

| الملف | التغيير |
|---|---|
| [TransitionFields.php](backend/app/Domain/Order/Support/TransitionFields.php) | حقل `number` لكل بند تختلف وحدتاه، ومفتاح `warehouse_quantity_{id}` معرّف مرة واحدة في `stockQuantityKey()`؛ وتلميح المخزن توقّف عن طباعة رقمٍ لم يقسه أحد |
| [OrderItem.php](backend/app/Domain/Order/Models/OrderItem.php) | `stockUnit()` و`isStockedInAnotherUnit()` — القاعدة في مكان واحد يقرأه النموذج والحارس والحقل |
| [SetOrderStockQuantities.php](backend/app/Domain/Order/Actions/SetOrderStockQuantities.php) | **جديد.** الكاتب الوحيد لـ`warehouse_quantity`. المفتاح الغائب يعني «هذا البند لا يحتاج قياساً»، لا «امسح ما فيه» |
| [ChangeOrderStatus.php](backend/app/Domain/Order/Actions/ChangeOrderStatus.php) | يكتب المقاسات **قبل** الخصم مباشرة، في المعاملة نفسها |
| [DeductOrderStock.php](backend/app/Domain/Order/Actions/DeductOrderStock.php) | `guardEveryLineIsMeasured()` قبل أي قراءة رصيد |
| [LineNeedsAMeasuredStockQuantity.php](backend/app/Domain/Order/Exceptions/LineNeedsAMeasuredStockQuantity.php) | **جديد** |
| [StoreOrderRequest.php](backend/app/Application/Api/V1/Requests/Order/StoreOrderRequest.php) · [UpdateOrderRequest.php](backend/app/Application/Api/V1/Requests/Order/UpdateOrderRequest.php) · [OrderItemData.php](backend/app/Domain/Order/DTOs/OrderItemData.php) · [AddOrderItem.php](backend/app/Domain/Order/Actions/AddOrderItem.php) | `items.*.warehouse_quantity` **لم يعد مقبولاً**. كاتبٌ واحد للعمود، في اللحظة الوحيدة التي توجد فيها الإجابة |

### التطبيق

[order_line_row.dart](frontend/lib/features/orders/presentation/widgets/order_line_row.dart) فقد
الصندوق ومتحكّمه وقاعدة `isWeighedOffTheShelf`، ومعه
[new_order_page.dart](frontend/lib/features/orders/presentation/views/new_order_page.dart)
و[new_order.dart](frontend/lib/features/orders/models/new_order.dart)
و[take_order.dart](frontend/lib/features/orders/usecases/take_order.dart) و`warehouseQuantityError`.

وحُذف معه **حمل المفتاح خلال التعديل** في
[update_order_invoice.dart](frontend/lib/features/orders/usecases/update_order_invoice.dart): كان
موجوداً لأنّ `PUT /orders/{id}` يُعيد بناء البنود فيمحو ما لا يُذكر — والخادم لم يعد يقبل المفتاح
أصلاً، والحالة الوحيدة التي تحمل قياساً («جاهزة») هي حالةٌ `itemsAreEditable` فيها `false`. فلا
شيء ليُحفظ ولا شيء ليُفقد.

---

## ٥ · كسر في العقد، مقصود

`POST /orders` و`PUT /orders/{id}` كانا يقبلان `items.*.warehouse_quantity` ولم يعودا. تطبيقٌ
قديمٌ يرسله: **يُتجاهل بصمت** (لا رفض)، ثم يُسأل عن الرقم عند «جاهزة» كأي طلبية أخرى.

طلبيات قائمة اليوم قبل «جاهزة» وتحمل قيمة من الشاشة القديمة: تحتفظ بها، وحقل «جاهزة» يفتح عليها —
إلّا أن يُعدَّل الطلب، فإعادة بناء البنود تمحوها ويُسأل عنها من جديد.

---

## ٦ · وحُذف معه «الوزن (كجم)»

كان آخر ما بقي في الشاشة صندوقاً وحيداً بلا فائدة، فحُذف كلّه: الحقل، والكتابة في
`ChangeOrderStatus`، و`Order::isPricedByWeight()` التي لم يبق لها نداء، والمفتاح في
`OrderResource`، والسطر في شاشة الطلبية.

**لماذا كان بلا فائدة:** `orders.weight_kg` كان يُكتب في هذه الحركة ويُقرأ في موضعين — مفتاح في
الاستجابة، وسطر «الوزن» في شاشة الطلبية. لا الفاتورة تحسب منه (تُبنى من كميات البنود) ولا الناقل
يقرأه ولا التكلفة تعرفه. وكان **مطلوباً** لكل طلبية فيها بند مسعّر بالكيلوغرام بحجّة «الوزن هو ما
تُحاسب عليه» — وهي جملة لم تكن صحيحة عن الكود يوماً.

**والعمود سقط معه.** كان الاقتراح إبقاءه كعمود ميت حفظاً لما سُجّل فيه، فقيل: احذفه — قاعدة
البيانات ستُمسح قبل الانطلاق أصلاً. فهجرة `drop_weight_kg_from_orders_table` تُسقطه، ومعه سقط
`casts()` ومدخله في `AuditAttributeLabels`. `down()` يُعيد العمود لا الأرقام: إسقاط عمود يأخذ
بياناته معه، والتراجع يُعيد الشكل وكل صفّ فيه `null` — وهذا مذكور في الهجرة نفسها بدل أن يُفهم من
«قابلة للتراجع» أنّها قابلة للاسترجاع.

**كسر ثانٍ في العقد:** `weight_kg` لم يعد يصل في `OrderResource`، ولم يعد مقبولاً في `fields` عند
«جاهزة» — وإرساله الآن يُرفض ٤٢٢، لأنّ النموذج يرفض كل مفتاح لم يصفه.

---

## ٧ · أسئلة مفتوحة

1. **تصحيح وزنٍ أُدخل خطأً بعد «جاهزة».** اليوم الطريق الوحيد حركة تسوية يدوية على الرفّ (أو
   «تسجيل تلف» إن كان النقص تلفاً فعلياً). هل نضيف تصحيحاً صريحاً يعكس الحركة ويعيد الخصم؟
2. **صفر مقبول.** الحقل `min: 0`، فمن يكتب صفراً يقول «لم يخرج من هذا الرفّ شيء» وتُسجَّل حركة
   بصفر. أأمنعه (وأجعل الأدنى `0.001`)، أم أتركه جملةً مشروعة؟
3. **البنود المتّفقة الوحدة**: لا حقل لها الآن. هل تريد صندوقاً اختيارياً لها أيضاً — لطلبيةٍ خرج
   منها فعلياً أكثر مما بيع — أم يبقى ذلك شأن «تسجيل تلف» وحده؟
