<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
	<dict>
		<key>CADisableMinimumFrameDurationOnPhone</key>
		<true/>
		<key>CFBundleDevelopmentRegion</key>
		<string>$(DEVELOPMENT_LANGUAGE)</string>
		<!--
		الاسم تحت الأيقونة على الشاشة الرئيسية، وفي مبدّل التطبيقات، وفي الإعدادات.

		بالعربية ومكتوب هنا مباشرةً لا في `InfoPlist.strings`: التطبيق بلغة واحدة، وملف ترجمة
		للغة وحيدة ملفٌّ يُنسى تحديثه ولا يفيد أحداً. `CFBundleDevelopmentRegion` لا يغيّر هذا —
		القيمة واحدة لكل الأجهزة مهما كانت لغة النظام، وهو المقصود.
	-->
		<key>CFBundleDisplayName</key>
		<!--
		متغيّرٌ لا نصّ: نكهةُ الاختبار يجب أن تحمل اسماً آخر تحت الأيقونة، وهذا الملفُّ واحدٌ
		للنكهتين. القيمةُ الفعلية في `APP_DISPLAY_NAME` على كل تهيئة — «دعاية» للإنتاج
		و«دعاية تجريبي» لـ`*-dev`. و`INFOPLIST_KEY_CFBundleDisplayName` لا ينفع هنا: لا يقرؤه
		Xcode إلا حين يولّد الـplist بنفسه، ومشروع Flutter يشير إلى ملفٍّ صريح.
	-->
		<string>$(APP_DISPLAY_NAME)</string>
		<key>CFBundleExecutable</key>
		<string>$(EXECUTABLE_NAME)</string>
		<key>CFBundleIdentifier</key>
		<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
		<key>CFBundleInfoDictionaryVersion</key>
		<string>6.0</string>
		<!--
		الاسم القصير الذي يستعمله النظام حيث لا يتّسع `CFBundleDisplayName` — وبعض أدوات
		التوقيع والتقارير تقرأه. لاتيني هنا عن قصد: هذه خانة تقنية تظهر في سجلات ومسارات
		ملفات، لا على شاشة المستخدم. حدّها خمسة عشر حرفاً.
	-->
		<key>CFBundleName</key>
		<string>Dayaa</string>
		<key>CFBundlePackageType</key>
		<string>APPL</string>
		<key>CFBundleShortVersionString</key>
		<string>$(FLUTTER_BUILD_NAME)</string>
		<key>CFBundleSignature</key>
		<string>????</string>
		<key>CFBundleVersion</key>
		<string>$(FLUTTER_BUILD_NUMBER)</string>
		<key>LSRequiresIPhoneOS</key>
		<true/>
		<!--
		Why this app asks for the camera, the photo library and location, in the words iOS shows
		the user in the permission alert.

		In Arabic, and deliberately: this app has one language and its users are the staff of a
		printing business in Libya. An English sentence in the one system alert they cannot skip
		would be the only English they ever see.

		Each names what the app does with it rather than what it wants. Apple rejects a build
		whose purpose string is vague, and a person reading «يحتاج التطبيق إلى الكاميرا» has been
		told nothing they did not already infer from being asked.

		Location is the odd one out: nothing here reads a position. `file_picker` links
		DKImagePickerController on iOS, whose `DKAsset` exposes the `CLLocation` stored in a
		photo's EXIF, and App Store validation rejects the build over that reference alone
		(ITMS-90683) whether or not the code path is ever reached. So the string is here, and it
		says the true thing rather than inventing a feature: the alert will not appear, because
		the app never asks.

		No NSMicrophoneUsageDescription: nothing here records video, and a permission string for
		a capability the app never uses is one more thing to justify at review.
	-->
		<key>NSCameraUsageDescription</key>
		<string>لتصوير تصميم أو عيّنة مطبوعة وإضافتها مباشرةً إلى تصاميم العميل.</string>
		<key>NSLocationWhenInUseUsageDescription</key>
		<string>لا يتتبّع التطبيق موقعك ولا يطلبه. تظهر هذه الرسالة لأن منتقي الصور قادر على قراءة مكان التقاط الصورة المحفوظ داخلها، ولا نقرأه.</string>
		<key>NSPhotoLibraryUsageDescription</key>
		<string>لاختيار تصاميم العميل من صورك — كالتي تصلك عبر واتساب — ورفعها إلى ملفه.</string>
		<key>UIApplicationSceneManifest</key>
		<dict>
			<key>UIApplicationSupportsMultipleScenes</key>
			<false/>
			<key>UISceneConfigurations</key>
			<dict>
				<key>UIWindowSceneSessionRoleApplication</key>
				<array>
					<dict>
						<key>UISceneClassName</key>
						<string>UIWindowScene</string>
						<key>UISceneConfigurationName</key>
						<string>flutter</string>
						<key>UISceneDelegateClassName</key>
						<string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
						<key>UISceneStoryboardFile</key>
						<string>Main</string>
					</dict>
				</array>
			</dict>
		</dict>
		<key>UIApplicationSupportsIndirectInputEvents</key>
		<true/>
		<key>UILaunchStoryboardName</key>
		<string>LaunchScreen</string>
		<key>UIMainStoryboardFile</key>
		<string>Main</string>
		<key>UISupportedInterfaceOrientations</key>
		<array>
			<string>UIInterfaceOrientationPortrait</string>
			<string>UIInterfaceOrientationLandscapeLeft</string>
			<string>UIInterfaceOrientationLandscapeRight</string>
		</array>
		<key>UISupportedInterfaceOrientations~ipad</key>
		<array>
			<string>UIInterfaceOrientationPortrait</string>
			<string>UIInterfaceOrientationPortraitUpsideDown</string>
			<string>UIInterfaceOrientationLandscapeLeft</string>
			<string>UIInterfaceOrientationLandscapeRight</string>
		</array>
		<key>UIStatusBarHidden</key>
		<false/>
		<!-- Lets a push wake the app to handle a data payload while it is in the background.
	     Without it, iOS delivers the banner but the app never runs, so nothing is refreshed
	     until the user taps. -->
	<key>UIBackgroundModes</key>
	<array>
		<string>remote-notification</string>
	</array>
</dict>
</plist>
