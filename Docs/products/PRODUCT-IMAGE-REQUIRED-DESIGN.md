# صورة المنتج — قاعدة إلزامية

كل منتج في الكتالوج لازم يكون عنده صورة واحدة على الأقل. هذي الوثيقة تشرح كيف تُفرض القاعدة،
وليش اتخذنا القرارات اللي اتخذناها.

## الحالة قبل التغيير

- `POST /api/v1/products` يقبل JSON، ولا يعرف شي عن الصور.
- الصور تُرفع بعدين على `POST /api/v1/products/{product}/images` كـ `multipart/form-data`.
- النتيجة: منتج بلا صورة حالة صالحة تماماً. وفعلاً، كل الـ 12 منتج في قاعدة البيانات المحلية
  كانوا بلا أي صورة.

## القاعدة

> **منتج بلا صورة لا يمكن أن يوجد.**

مش «لا يمكن أن يُنشأ» — لا يمكن أن *يوجد*. الفرق مهم، وهو سبب القرار الثاني تحت.

## القرارات

### ١. الصورة تُرسل في نفس طلب الإنشاء

`POST /api/v1/products` صار `multipart/form-data` مع حقل `image` مطلوب. المنتج وصورته يُخلقان في
`transaction` واحدة.

البديل اللي رُفض: إبقاء الإنشاء JSON ومنع *تنشيط* منتج بلا صورة. رُفض لأنه يترك منتجات بلا صور
موجودة فعلاً في الجدول — مخفية عن الكتالوج، لكن موجودة. القاعدة تصير عن العرض، مش عن الوجود،
وكل استعلام لاحق يضطر يتذكر الفرق.

الثمن المدفوع: **هذا تغيير كاسر لعقد منشور.** `routes/api.php` يقول إن العقد المنشور لا يُغيَّر في
مكانه، وهذا استثناء صريح بقرار من صاحب المنتج. أي عميل غير تطبيق Flutter هذا راح ينكسر.

نتيجة جانبية للـ multipart تستحق الذكر: الـ form encoding يبعث كل قيمة كنص، و`(bool) "false"` في
PHP تساوي `true`. بدون تطبيع، `is_active=false` كان راح ينقلب إلى `true` بصمت. التطبيع في
`StoreProductRequest::prepareForValidation()`.

### ٢. حذف آخر صورة مرفوض

`DELETE /api/v1/products/{product}/images/{image}` يرجّع 422 لما تكون الصورة الوحيدة الباقية.

هذا اللي يحوّل القاعدة من شرط لحظة إنشاء إلى ثابت دائم. بدونه، الطريق من «كل منتج عنده صورة» إلى
«هذا المنتج بلا صورة» يبقى مفتوحاً وطوله نقرة واحدة.

الرسالة تقترح المخرج الصحيح: ارفع البديل، بعدين احذف القديمة.

### ٣. الصور على مستوى المنتج

`product_images` مربوط بـ `product_id` فقط — ما فيه `variant_id` ولا كان. المقاسات (`25*35`
مقابل `30*40`) تشترك في نفس صور المنتج.

مسجّل هنا كقرار مقصود، مش كنقص: صورة لكل مقاس تعني على المنتج الواحد سبع صور لنفس الكيس بفروق لا
تُرى في شبكة الكتالوج. لو احتاجها العمل لاحقاً، `variant_id` عمود nullable على نفس الجدول ولا
يستدعي جدولاً ثانياً.

## التعبئة الأثرية

`php artisan catalog:backfill-images` يعطي كل منتج بلا صور نسخته من
`database/seeders/data/default-product-image.png`.

**نسخة لكل منتج، مش مسار مشترك.** `DeleteProductImage` يمسح الملف فعلياً من القرص، فمسار واحد
مشترك بين 12 منتج يعني أن حذف صورة منتج واحد يترك 11 صف يشيرون إلى ملف غير موجود.

الملف المصدر يعيش في `database/seeders/data/` لأن `storage/app/public/.gitignore` فيه `*` — نسخة
هناك ما تنجو من clone جديد.

الأمر idempotent: يتخطى أي منتج عنده صورة أصلاً، فإعادة تشغيله ما تضاعف شي. ويستعمل
`ConfirmableTrait` فيسأل قبل الكتابة خارج بيئة `local`.

## ما لم يتغير

- `PUT /api/v1/products/{product}` ما زال JSON. المنتج عنده صورة أصلاً، والتحديث ما يمس الصور.
- رفع صورة إضافية، تغيير الصورة الرئيسية، وحذف صورة ليست الأخيرة — كلها كما كانت.
- الحد الأقصى للحجم والصيغ المسموحة تبقى في `config/media.php` تحت `product_images`.
