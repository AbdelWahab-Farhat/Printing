# الملاحظات، مُعمَّمة

الملاحظات موجودة اليوم على العميل وحده — [CUSTOMER-COMMENTS.md](CUSTOMER-COMMENTS.md) — وقد كُتب
هناك صراحةً أن التعميم مؤجَّل «بلا حالة ثانية تستعمله»، وأن مكانه `BACKLOG.md`. الحالة الثانية وصلت:
ملاحظة على المورد — «لا يسلّم قبل الظهر»، «يرفع السعر إن طلبنا أقل من طن»، «المندوب الجديد اسمه
سالم». فالتجريد الآن يُشترى بازدواجٍ قائم لا بازدواجٍ متوقَّع، وهو الشرط الذي وضعناه لأنفسنا.

هذا المستند هو خطة تحويل `customer_comments` إلى جدول ملاحظاتٍ واحد يصلح لأي سجل.

---

## ١. الشكل المقترح على الخادم

### الجدول: `customer_comments` ← `comments`

| العمود | ماذا |
|---|---|
| `commentable_type` + `commentable_id` | صاحب الملاحظة — عميل، مورد، وما يأتي بعدهما |
| `user_id` | كاتبها، `restrictOnDelete` كما هو |
| `body` | كما هو |
| `edited_at` | كما هو |
| `timestamps` + `softDeletes()->index()` | كما هو |
| فهرس `['commentable_type', 'commentable_id', 'created_at']` | بديل `['customer_id','created_at']` — القائمة هي «ملاحظات هذا السجل، الأحدث أولاً» ولا شيء غيرها |

الهجرة تنقل الصفوف القائمة: `commentable_type = 'customer'` و`commentable_id = customer_id`، ثم
تُسقط العمود. **القيمة نصّها `customer` لا اسم الصنف الكامل**، لأن `AuditSubject::morphMap()`
مُسجَّلة عالمياً بالفعل — فالجدول يكتب الأسماء المختصرة نفسها التي يكتبها `activity_log`، ولا يحمل
مسار PHP يكسره أول نقل ملف.

**`cascadeOnDelete` يسقط**: قيد المفتاح الأجنبي لا وجود له في علاقة مورفية. وهو غير مهمّ عملياً — لا
شيء في هذا النظام يُحذف حذفاً صلباً — لكن القاعدة المكتوبة في `RULES.md` §10 تبقى كما هي: الأبناء
يتتالون عبر `CascadesSoftDeletes` لا عبر قيد قاعدة البيانات.

### النموذج: `App\Domain\Comment\Models\Comment`

نطاق خاص به، لا داخل `Customer` ولا `Vendor`: ملاحظةٌ تخصّ الاثنين معاً وأكثر، ووضعها في نطاق أحدهما
يجعل النطاق الآخر يعتمد عليه بلا سبب.

- `commentable(): MorphTo`
- `author(): BelongsTo` — كما هي
- `isChangeableBy(?User)` — **تنتقل كما هي حرفياً**، وهي القاعدة الوحيدة في الملف
- `Auditable`, `SoftDeletes`, `#[Fillable(['body'])]` — و`user_id` يبقى غير قابل للتعبئة، وهو سبب
  وجود القاعدة أصلاً

وسِمة `HasComments` على كل نموذجٍ يقبل الملاحظات: `morphMany` واحدة، لا أكثر. `Customer` و`Vendor`
اليوم.

### `AuditSubject`

`case CustomerComment = 'customer_comment'` يصير `case Comment = 'comment'`. صفوف `activity_log`
القائمة تحمل `subject_type = 'customer_comment'`، فتحتاج الهجرة أن تحدّثها — وإلا صار سجل ملاحظةٍ
قديمة تاريخاً لا يقرؤه أحد. (انظر السؤال ٣.)

### الصلاحية

`customers.comments.moderate` تصير `comments.moderate`: «تعديل وحذف ملاحظات الآخرين». صلاحية واحدة
لكل الملاحظات، لأن السؤال الذي تجيب عنه واحد — «هل يُعدِّل هذا الموظف كلام زميله؟» — ولا معنى لأن
يُصحَّح كلام زميلٍ على عميل ولا يُصحَّح على مورد. (السؤال ٢ يعرض البديل.)

الجدول في قاعدة البيانات يحمل الاسم القديم على الخادم التجريبي وهو مرتبط بأدوار، فالهجرة **تعيد
تسمية الصفّ** لا تحذفه وتخلق غيره — إسقاطه يسقط معه إسناده لكل دور يحمله.

### النقاط

المسارات تبقى متداخلة تحت صاحبها — لا `/comments?type=vendor&id=7`. مسارٌ يقول ما يخصّه بالعربية
المستقيمة هو مسارٌ لا يحتاج توثيقاً، والتداخل هو ما يجعل `scoped()` يردّ ٤٠٤ على معرّف ملاحظة مورد
آخر بحكم البناء.

```
GET|POST     /api/v1/customers/{customer}/comments          customers.view      (كما هي)
PATCH|DELETE /api/v1/customers/{customer}/comments/{comment} customers.view + صاحبه أو moderate
GET|POST     /api/v1/vendors/{vendor}/comments              vendors.view        ← جديد
PATCH|DELETE /api/v1/vendors/{vendor}/comments/{comment}     vendors.view + صاحبه أو moderate
GET          /api/v1/{...}/comments/{comment}/logs           logs.view
```

**النداءات القائمة على العميل لا تتغيّر ولا حرفاً** — لا مسارها ولا شكل ردّها — فالتطبيق المنشور
اليوم يبقى عاملاً بعد الهجرة.

خلف المسارات: `CommentController` مجرَّد يحمل الأفعال الأربعة وقاعدة «صاحبه أو مشرف» مرة واحدة،
وتحته صنفان نحيفان (`CustomerCommentController`، `VendorCommentController`) لا يقول كلٌّ منهما إلا
أيّ نموذجٍ هو الأب. الوراثة هنا لأن Laravel يربط الأب بنوعه في توقيع الدالة؛ صنفٌ واحد يقرأ الأب من
مُعاملات المسار كان سيوفّر عشرين سطراً ويكسر الربط الضمني.

**من يكتب**: من يقرأ. `vendors.view` على المورد كما `customers.view` على العميل — الملاحظة أداة عمل
لا امتياز.

---

## ٢. الشكل في التطبيق

`lib/features/customers/.../customer_comment*` ينتقل إلى `lib/features/comments/`:

| اليوم | بعده |
|---|---|
| `CustomerComment` | `Comment` |
| `CustomerCommentRepository` | `CommentRepository` — كل دوالّه تأخذ `CommentSubject` بدل `customerId` |
| `CustomerCommentsCubit` | `CommentsCubit` |
| `CustomerCommentsPage` | `CommentsPage` |

و`CommentSubject`: نوعٌ صغير يحمل المسار الأساسي والعنوان — `CommentSubject.customer(id)`،
`CommentSubject.vendor(id)` — فيبني الـ repository النقطة منه بدل أن يعرف الشاشة التي استدعته.

المسارات في التطبيق تبقى متداخلة أيضاً: `/customers/:id/comments` كما هي، و`/vendors/:id/comments`
تُضاف. وعلى شاشة المورد: بند «الملاحظات» في الزرّ العائم، في نفس موضعه على شاشة العميل.

`canEdit` و`canDelete` تبقيان كما هما: يحسبهما الخادم لكل قارئ ويرسلهما مع الصفّ، والتطبيق يرسم بهما
ولا يعيد كتابة القاعدة.

---

## ٣. أسئلة مفتوحة

### ١. من يقبل الملاحظات في هذه الجولة؟ — ✅ العميل والمورد فقط

**ردك:** «العميل والمورد فقط حاليا.» منفَّذ. الجدول يقبل غيرهما بلا تعديل، وإضافة صاحبٍ جديد لاحقاً
هي سطر `HasComments` وصنف متحكّم من خمسة أسطر ومساران — أمر الشراء والطلبية مرشَّحان لِما بعدها.


### ٢. صلاحية واحدة `comments.moderate`، أم واحدة لكل نوع؟ — ✅ واحدة

**ردك:** «واحدة أفضل.» الهجرة تعيد تسمية `customers.comments.moderate` في مكانها، فكل دورٍ يحملها
يبقى حاملاً لها — و`VendorCommentTest` يثبت أنها تغطّي ملاحظات المورد أيضاً.


### ٣. سجلّ الملاحظات القديم: نحدّث `activity_log` أم نُبقي الاسم القديم؟ — ✅ نُحدِّثه

**ردك:** «حدثتها.» الهجرة تنقل `subject_type` من `customer_comment` إلى `comment` في الصفوف
القائمة، ويبقى `AuditSubject` باسمٍ واحد لا اسمين.


### ٤. هل تظهر آخر ملاحظة على شاشة صاحبها؟

السؤال نفسه بقي مفتوحاً في مستند العميل ولم يُجَب. الآن أصبح لصاحبَين، فالجواب الواحد يخدم الشاشتين:
سطر واحد تحت «معلومات الاتصال» يقول آخر ما كُتب ومن كتبه، والباقي خلف الزرّ العائم.

**ردك:**


### ٥. هل يُعرض عدد الملاحظات على بند الزرّ العائم — «الملاحظات (٣)»؟

يحتاج `comments_count` على `CustomerResource` و`VendorResource`، محسوباً في `show` وحدها لا في
القائمة — نفس الفخّ الذي عالجه `orders_count`. بند مؤجَّل في `BACKLOG.md` اليوم.

**ردك:**


---

## ٤. ما نُفِّذ فعلاً

- التعميم على العميل والمورد فقط.
- `comments.moderate` واحدة، والصفّ القديم أُعيدت تسميته ولم يُحذف.
- `activity_log` حُدِّث في الهجرة نفسها.
- لا سطر معاينة على الشاشة ولا عدد على الزرّ — السؤالان ٤ و٥ أعلاه ما زالا مفتوحين، والشكل بقي كما
  اعتاده الناس على شاشة العميل.

---

## ٥. خطة التنفيذ — **منفَّذة بالكامل** (١٤ أغسطس ٢٠٢٦)

الشرائح الخمس أدناه كلّها مكتوبة، الاختبار قبل الكود في كلٍّ منها، و`CustomerCommentTest` بأربعة
عشر اختباراً بقي أخضر بلا تغيير في سلوكه — وهو مقياس نجاح النقل كلّه.

1. **الهجرة**: اختبار يخلق ملاحظة عميل قبل الهجرة ويقرؤها بعدها بنفس المعرّف والكاتب والنصّ، ثم
   الهجرة — إعادة التسمية، العمودان المورفيان، نقل القيم، الفهرس، وتحديث `activity_log` واسم
   الصلاحية.
2. **النموذج والنطاق**: `Comment` + `HasComments`، ونقل `isChangeableBy` بلا تغيير. اختبارات
   `CustomerCommentTest` القائمة **تبقى كما هي وتبقى خضراء** — وهي مقياس نجاح هذه الشريحة كلّه.
3. **المتحكّم المجرَّد + مسارات المورد**: اختبارات جديدة لكل نقطة على المورد — إنشاء، تعديل من غير
   صاحبها (٤٠٣)، تعديل بـ `comments.moderate` (٢٠٠)، حذف ناعم، `logs`، وملاحظة موردٍ آخر (٤٠٤).
4. **التطبيق — النقل**: `comments/` مع `CommentSubject`، واختبارات الـ repository والـ cubit
   القائمة تُنقل وتُعدَّل معه.
5. **التطبيق — شاشة المورد**: بند الزرّ العائم والمسار، مع اختبار ويدجت.

`php artisan test` بعد كل شريحة على الخادم، و`flutter analyze` + `flutter test` على التطبيق. لا
تشغيل للتطبيق.

> **ترتيب النشر على الخادم التجريبي**: الهجرة تلمس الصلاحيات و`activity_log` معاً، فتُنفَّذ ونسخة
> قاعدة البيانات محفوظة قبلها — إعادة تسمية صلاحيةٍ مسندة لأدوار ليست شيئاً يُجرَّب مرتين.
