# النواقص تنقص الفاتورة · وبندٌ واحد لكل مقاس

> **مُنفَّذ بالكامل — ١٢ أغسطس ٢٠٢٦**، على الجانبين.
> يكمّل [ORDERS-STATUS-FLOW.md](ORDERS-STATUS-FLOW.md) القسم ١٥.

---

## ١. ما كان يحدث

**عند الدخول إلى «نواقص»:** `ChangeOrderStatus::recordShortages()` يكتب `shortage_quantity` على كل بند. **وعند الخروج: لا شيء** — الرقم يبقى إلى الأبد، ولذلك طلبية #54 حالتها «قيد الطباعة» وما تزال تعرض «ناقص: ١٠٠٫٠٠٠».

**والمال لا يعرف بالنقص:** `line_total` محسوب على الكمية المطلوبة كاملة. `shortage_quantity` ملاحظة على البند، لا رقم محاسبي.

---

## ٢. القرار: الفاتورة تتبع النقص، في الاتجاهين

**القاعدة الواحدة التي يُشتقّ منها الباقي:**

```text
الكمية المحتسبة = الكمية المطلوبة − الناقص المسجَّل الآن
سطر البند       = سعر الوحدة المتفق عليه × الكمية المحتسبة
```

وهي **عكوسة بطبيعتها**: لا شيء يُمسح ولا كمية تُنقَص من مكانها. `quantity` تبقى ٣٠٠ إلى الأبد — هي ما طلبه العميل — و`shortage_quantity` وحدها تتحرك. أنزلها إلى صفر فترجع الفاتورة كاملةً بلا قيدٍ عكسي ولا استثناء: الرقم مُشتَقّ، لا رصيد مكتوب.

### الأجوبة الثلاثة

**سعر الوحدة لا يتغيّر.** البند طُلب ٣٠٠ فأخذ شريحة الـ٣٠٠ (١٫٥٥)، ونقص منه مئة فصار المحتسب ٢٠٠ — والسعر يبقى ١٫٥٥. النقص ذنبنا لا ذنب العميل، ورفع السعر عليه لأننا لم نسلّمه ما طلب عقوبةٌ في الاتجاه الخطأ.

| | الكمية | السعر | السطر |
| --- | --- | --- | --- |
| طُلب | ٣٠٠ | ١٫٥٥ | ٤٦٥٫٠٠ |
| نقص | ١٠٠ | — | — |
| **يُحاسَب** | **٢٠٠** | **١٫٥٥** | **٣١٠٫٠٠** |

**الخروج من «نواقص» يسأل «كم وصل».** حقل لكل بند على مسار الخروج، ممتلئ مسبقاً بكامل الناقص — فالحالة الشائعة (وصلت الكمية كلها) ضغطةٌ واحدة، والجزئية تُكتب رقماً. الناقص الجديد = المسجَّل − الواصل، والفاتورة تتبعه في نفس المعاملة.

**والتعديل بعد ذلك من شاشة الطلبية**، زرٌّ مستقل «تعديل النواقص» يفتح ورقة فيها كل البنود.

### ما لا يحتاج بناءً جديداً

**الدفع الزائد محلول اليوم.** إن نقصت الفاتورة تحت ما دُفع، `Order::remainingAmount()` يصير سالباً — والشاشة تقول «زائد ٥٠» ومسار الاسترجاع (`RefundOrderPayment`) موجود. لا قيد جديد ولا عمود.

**والأثر مسجَّل:** `order_item` مشمول بسجل النشاط، فكل تحريك للنقص له سطر يقول مَن ومتى.

### القفل

| | مفتوح في | مقفل |
| --- | --- | --- |
| البنود والنواقص | جديدة · قيد التصميم · **نواقص** · قيد الطباعة | «جاهزة» فصاعداً |

«جاهزة» تعني أن الأكياس صُنعت وعُدّت، فالرقم لم يعد تقديراً يُصحَّح. وملاحظة: `Order::itemsAreEditable()` اليوم **لا تذكر «نواقص»** — فطلبية واقفة على نقص لا تُعدَّل بنودها أصلاً. سطرٌ يُضاف.

---

## ٣. ما نُفِّذ

| # | الشريحة | أين |
| --- | --- | --- |
| ١ | `OrderItem::billableQuantity()` و`deriveLineTotal()` — القاعدة في مكان واحد، و`AddOrderItem` يمرّ عليها | `Models/OrderItem.php` |
| ٢ | `SetOrderShortages` — **الكاتب الوحيد** لـ`shortage_quantity`: يكتب، يعيد اشتقاق السطور، ثم الإجماليات، في معاملة واحدة | `Actions/SetOrderShortages.php` |
| ٣ | `ChangeOrderStatus` ينادي (٢) في الاتجاهين: `shortage_{بند}` داخلاً و`received_{بند}` خارجاً | `Actions/ChangeOrderStatus.php` |
| ٤ | حقول الخروج، **ممتلئة مسبقاً** بكامل الناقص عبر `value` الجديدة على `TransitionField` | `Support/TransitionFields.php` |
| ٥ | `PATCH /orders/{order}/shortages` خلف `orders.status.shortage` | `routes/api.php` |
| ٦ | «نواقص» أُضيفت إلى `itemsAreEditable()` | `Models/Order.php` |
| ٧ | `billable_quantity` في جواب البند، والسطر على الشاشة صار «٢٠٠ قطعة × ١٫٥٥٠» مع «ناقص ١٠٠ من ٣٠٠ — غير محتسب» | باك + فرونت |
| ٨ | ذراع «تعديل النواقص» على الزر العائم، وورقتها — **تظهر في «نواقص» وحدها** | `edit_shortages_sheet.dart` |

> **ولماذا في «نواقص» وحدها:** الورقة موجودة للطلبية الواقفة لأن البضاعة ليست هناك. والحركتان على طرفيها تسألان نفس السؤال في موضعه — الدخول يسأل «كم الناقص» والخروج يسأل «كم وصل» — فذراعٌ ثالثة في حالة أخرى بابٌ ثالث لغرفة لها بابان. `Order.shortagesAreEditable` في التطبيق هي الشرط، لا `items_are_editable`.
>
> **وما بقي مفتوحاً على الخادم عمداً:** `PATCH /orders/{order}/shortages` ما يزال يقبل ما دامت البنود مفتوحة، لا في «نواقص» فقط. والسبب حالة واحدة حقيقية: طلبية نقصها ١٠٠ ووصل منها ٦٠ تخرج إلى «قيد الطباعة» و٤٠ ما تزال ناقصة وغير محتسبة — فإن وصلت الأربعون لاحقاً، إغلاق الخادم كان سيترك الفاتورة ناقصة بلا باب. الذراع مخفية، والباب موجود.

**والحقول الجديدة لم تكلّف إصدار تطبيق** لولا `value`: الشاشة ترسم `fields` مما يصلها، وهذا ما بُنيت له. الخاصية الجديدة تحتاج التطبيق ليحترمها، وتطبيقٌ أقدم يفتح الحقل فارغاً بدل أن ينكسر.

### الاختبارات

**الخادم — `OrderShortageTest` (١٧ اختباراً):** السطر يُحاسب على الباقي · الكمية المطلوبة لا تُكتب فوقها · السعر لا يُعاد اقتباسه · إرجاع النقص يعيد الفاتورة **بالفلس** · بند نقص كله بصفر · الناقص أكبر من المطلوب مرفوض · بند من طلبية أخرى مرفوض · طلبية مدفوعة تصير «زائد ١٥٥» · الخروج بكامل الواصل · بجزء منه · بصفر · الإلغاء لا يمسّ الأرقام · «جاهزة» تُقفل · «نواقص» تقبل التصحيح · بلا إذن ٤٠٣ · الجواب يحمل الأرقام الثلاثة.

**التطبيق:** `order_item_shortage_test` (٥) · `set_order_shortages_test` (٥) · `edit_shortages_sheet_test` (٧) · `order_status_cubit_test` (٣ جديدة للتعبئة المسبقة) · `product_picker_sheet_test` (٣ للتكرار).

**المجموع:** ١٠١٨ اختبار خلف خضراء، و٨٥٠ في التطبيق، و`pint`/`flutter analyze` نظيفان.

### القرار المتّخذ افتراضياً

**الإذن أُعيد استعماله**: `orders.status.shortage` هو ما يعلن النقص وما يصحّحه — مَن يعدّ البضاعة هو مَن يكتب الرقم. صار هذا الإذن **يحرّك مالاً**، فإن أردته منفصلاً (`orders.shortages.manage`) فهو `case` في `PermissionName` وسطر في المسار.

---

## ٤. بندٌ واحد لكل مقاس — ✅ مُنفَّذ

**كانت** الطلبية #54 فيها ثلاثة صفوف حقيقية في `order_items`، كلها «كيس شحن — ٢٥*٣٥». ولا شيء يدمج: الخادم يمرّ على `items` صفاً صفاً، وزر «إضافة منتج» يضيف بطاقة بلا أن ينظر.

**وكانت تكلّف مالاً**، لأن التسعير بالشرائح ولكل بند على حدة: مئة في بند ومئتان في بند آخر ليستا ثلاثمئة. والشاشة نفسها تقول «أضف ٢٠٠ ليصبح السعر ١٫٥٥» — نصيحةٌ يبطلها الزر.

**والمنع صار في التطبيق، عند الاختيار لا بعده.** `showProductPicker` يأخذ `addedVariantIds`، والمقاس الموجود يُعرض **رمادياً** بعلامة صح وسطر «مضاف — عدّل كميته من البنود»، ولا يقبل الضغط. ويُعرض ولا يُخفى: مقاسٌ يختفي من القائمة يُقرأ «نفد»، والجواب المطلوب عكسه.

والاختصار الذي يقفز خطوة المقاس للمنتج ذي المقاس الواحد **صار يقف** إن كان ذلك المقاس مضافاً — وإلا لانزلق التكرار من الباب الوحيد الذي لا حارس عليه.

**الخادم بقي كما هو**: يقبل ما يُرسل إليه. وطلبيات الشراء (`purchase_order_form_page`) لم تتغيّر — الوسيط اختياري وفارغ افتراضياً.

---

## ٥. ملاحظة جانبية: «المنتجات ٠٫٠٠» في #54

ليست علّة في الكود: `CreateOrder` و`UpdateOrder` كلاهما ينادي `RecalculateOrderTotals`، وكل الطلبيات الأخرى إجمالياتها صحيحة. الطلبية #54 كُتبت بـ**factories** على قاعدة التطوير — `product_variants` ٣٢ و٣٣ أُنشئت في نفس الثانية بمقاسات مولَّدة (٢٢*٣٢ · ٢٣*٣٣) لا تطابق `variant_label` على البند، والـfactory لا يمرّ على حساب الإجماليات.

---

آخر تحديث: ١٢ أغسطس ٢٠٢٦
