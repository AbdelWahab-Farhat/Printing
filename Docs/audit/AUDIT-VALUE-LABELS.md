# تعريب القيم في سجلّ التغييرات

أسماء الأعمدة عُرِّبت — `page_url` صارت «رابط الصفحة» عبر `AuditAttributeLabels`. أمّا **القيم**
فما زالت كما خرجت من قاعدة البيانات، فتقرأ الشاشة اليوم:

```
الحالة        new  ←  printing
طريقة التسعير  tiered
```

بينما الشاشة نفسها — في بطاقة الطلبية، في الفلتر، في الخطّ الزمني — تكتب **«جديدة ← قيد الطباعة»**.
الترجمة موجودة أصلاً في `OrderStatus::label()`؛ سجلّ التغييرات وحده هو الذي لا يمرّ بها.

هذا المستند يقترح **قاعدة واحدة تعميمية** لذلك، لا قائمة ترقيعات لكل عمود.

---

## ١. ما الذي يظهر إنجليزياً اليوم؟

| نوع القيمة | مثال حقيقي | من يعرّبه الآن |
|---|---|---|
| **enum** | `status: printing` · `pricing_mode: tiered` · `fulfilment_type: delivery` | **لا أحد** — رغم أن `label()` موجودة على ٢١ enum من ٢٢ |
| **مفتاح أجنبي** | `customer_id: 12` · `city_id: 3` | **لا أحد** — الرقم يُعرض عارياً |
| **تاريخ** | `placed_at: 2026-08-13T09:00:00+00:00` | **لا أحد** — سطر ISO كامل داخل خانة سطرين |
| **صلاحيات الدور** في `properties` | `products.view` | **لا أحد** — رغم أن `PermissionName::label()` موجودة |
| منطقي | `true` / `false` | التطبيق — `_show()` تكتب «نعم/لا» |
| فارغ | `null` / `''` | التطبيق — «—» و«فارغ» |
| نصّ ورقم ومبلغ | `"أكياس ورقية"` · `450.00` | يبقى كما هو، وهذا صحيح |

الأربعة الأولى هي المشكلة. والثلاثة الأخيرة تقول شيئاً مهمّاً: التطبيق يعرف كيف يعرّب ما لا يحتاج
معرفةً بالمخطط، ويعجز تماماً عمّا يحتاجها.

---

## ٢. القاعدة المقترحة

> **القيمة تُترجَم من المكان الذي يعرف نوعها: `casts()` في الموديل.**

لا قاموس ثانٍ يُكتب باليد. الموديل يصرّح أصلاً بأن `status` هي `OrderStatus::class`، وأن
`placed_at` تاريخ. و`AuditSubject::modelClass()` يوصل من `subject_type` إلى الموديل. السلسلة
كاملة وموجودة — ينقص الوصل:

```
subject_type ──AuditSubject::modelClass()──▶ Order ──casts()──▶ status: OrderStatus ──label()──▶ «قيد الطباعة»
```

الترتيب الذي تُجرّب به القيمة:

| # | الشرط | الناتج | يحتاج استعلاماً؟ |
|---|---|---|---|
| ١ | الـ cast هو enum مدعوم بقيمة وله `label()` | `label()` | لا |
| ٢ | الـ cast هو `datetime` / `date` | تاريخ مقروء | لا |
| ٣ | العمود ينتهي بـ `_id` وللموديل علاقة `belongsTo` تطابقه | اسم السجلّ المرتبط | **نعم** |
| ٤ | غير ذلك | القيمة كما هي | لا |

وقاعدة السقوط هي نفسها قاعدة `attribute_labels` الحالية: **قيمة بلا ترجمة تُعرض كما هي**. لا
خطأ، ولا شاشة فارغة — تماماً ما تعرضه الشاشة اليوم.

**لماذا هذا الاتجاه وليس قاموساً يدوياً؟** لأن القاموس اليدوي يصير خاطئاً صباح اليوم الذي يُضاف
فيه enum جديد، بلا build يسقط ليقول ذلك. أمّا الاشتقاق من `casts()` فيغطّي العمود الجديد يوم
كتابته، وقبل أن يراه أحد.

---

## ٣. العقد — شكل الاستجابة

`value_labels` يوازي `changes` عضواً بعضو:

```jsonc
{
  "changes": {
    "old":        { "status": "new",      "customer_id": 12, "placed_at": null },
    "attributes": { "status": "printing", "customer_id": 12,
                    "placed_at": "2026-08-13T09:00:00+00:00" }
  },
  "attribute_labels": { "status": "الحالة", "customer_id": "العميل", "placed_at": "تاريخ الطلب" },
  "value_labels": {
    "old":        { "status": "جديدة" },
    "attributes": { "status": "قيد الطباعة", "customer_id": "مطبعة النور",
                    "placed_at": "١٣ أغسطس ٢٠٢٦" }
  }
}
```

**لماذا يوازي `changes` ولا يكون قاموس «قيمة ← ترجمة»؟** لأن مفاتيح JSON نصوص فقط، فالقيمة `12`
تصير `"12"`؛ ولأن `12` في `customer_id` و`12` في `city_id` رقمان مختلفان تماماً في نفس الصف؛ ولأن
`null` لا تصلح مفتاحاً أصلاً. المفتاح الوحيد الذي لا يلتبس هو **العمود + أيّ نصفَي التغيير**.

ولاحظ في المثال: `old.customer_id` ليست في `value_labels`. القيمة لم تتغيّر، فلا داعي لتكرار
الاسم مرّتين في كل صفّ — والتطبيق يقرأ `value_labels[half][col] ?? changes[half][col]` فيقع على
الصواب في الحالتين.

---

## ٤. أين يُكتب

| الملف | التغيير |
|---|---|
| `backend/app/Domain/Audit/AuditValueLabels.php` | **جديد** — بجانب `AuditAttributeLabels`، وبنفس منطقها: الحقيقة على الجهة التي تملك المخطط |
| `backend/app/Application/Api/V1/Resources/ActivityLogResource.php` | يضيف `value_labels` |
| `frontend/lib/features/audit/models/activity_log_entry.dart` | `valueLabels` + دالّة `valueFor(half, field)` |
| `frontend/lib/.../activity_log_page.dart` | `_show()` تصير **الملاذ الأخير** لا المترجم الأول |

---

## ٥. المفاتيح الأجنبية — الجزء المكلف وحده

القاعدة ١ و٢ ترجمة حسابية خالصة: صفر استعلامات. أمّا القاعدة ٣ فصفحة من ١٥ سجلّاً قد تحمل
٤٠ مفتاحاً أجنبياً، وحلّها الساذج ٤٠ استعلاماً.

العلاج: التجميع على مستوى **المجموعة** لا السجلّ — تُجمع كل أزواج (موديل، معرّف) في الصفحة، ثم
استعلام واحد لكل موديل (`whereIn`)، ثم تُوزّع النتائج. وهذا يعني `ActivityLogCollection` بدل
`ActivityLogResource::collection()` وحدها.

**لذلك أقترح مرحلتين:** المرحلة ١ (enum + تواريخ + الصلاحيات) بلا أي استعلام، والمرحلة ٢
(المفاتيح الأجنبية) بعدها. المرحلة ١ وحدها تغطّي أغلب ما يُقرأ فعلاً في الشاشة.

---

## ٦. اختبارات حارسة

بالترتيب المعتاد: الاختبار الساقط أوّلاً.

1. **كل enum يُستخدم في `casts()` على موديل مُدقَّق، عنده `label()`.** اليوم
   `TransitionFieldType` هو الوحيد بلا `label()` — يجب أن نعرف إن كان يظهر في سجلّ أصلاً.
2. **كل قيمة enum لها ترجمة** — `match` شامل يمنع الحالة الجديدة من المرور صامتة.
3. **سجلّ لموديل بلا أي عمود مترجَم يُرجع `{}` لا `[]`** — نفس فخّ `attribute_labels`.
4. **قيمة بلا ترجمة تصل كما هي** — القاعدة تسقط بأمان، ولا تُخفي العمود.

> ملاحظة جانبية: توثيق `AuditAttributeLabels` يذكر `AuditAttributeLabelsTest` الذي «يقرأ الجداول
> الحقيقية ويسقط قبل النشر» — وهذا الاختبار **غير موجود في المستودع**. يحسن كتابته مع هذا العمل،
> فهو الحارس الذي يفترضه الملف على نفسه.

---

## ٧. أسئلة مفتوحة

أُقرّت كلّها كما اقتُرحت (١٣ أغسطس ٢٠٢٦).

| # | السؤال | القرار |
|---|---|---|
| ١ | هل يعرّب الخادم **كل** شيء (نعم/لا، —، فارغ) أم ما يحتاج معرفةً بالمخطط فقط؟ | **ما يحتاج المخطط فقط.** «نعم/لا» صحيحة في التطبيق اليوم وإرسالها مع كل صف يضخّم الاستجابة بلا فائدة |
| ٢ | من يُنسّق التاريخ — الخادم أم التطبيق؟ | **التطبيق**، فهو وحده يعرف منطقة الجهاز الزمنية. الخادم يبقى يرسل ISO |
| ٣ | المفاتيح الأجنبية: الآن أم مرحلة ٢؟ | **الآن** — نُفِّذت المرحلتان معاً مع التجميع على مستوى الصفحة |
| ٤ | ما الاسم المعروض لكل موديل؟ | `name` ← `label` ← `title` ← `code`، بهذا الترتيب، مشتقّاً لا مكتوباً. و`HasAuditDisplayName` لمن لا يكفيه ذلك |
| ٥ | ماذا لو حُذف السجلّ المرتبط؟ | يُقرأ مع المحذوفين — السجلّ يجب ألّا يفقد اسماً |
| ٦ | هل تُعرَّب صلاحيات الدور في `properties`؟ | **نعم** — عبر `property_labels` |
| ٧ | الأرقام: هندية (١٢٣) أم عربية (123)؟ | لاتينية، كبقيّة أرقام التطبيق — انظر `lib/core/utils/digits.dart` |

---

## ٨. ما نُفِّذ

| الملف | الحالة |
|---|---|
| `backend/app/Domain/Audit/AuditValueLabels.php` | جديد — القاعدة: enum من `casts()`، ومفتاح أجنبي من `belongsTo` |
| `backend/app/Domain/Audit/AuditReferenceNames.php` | جديد — استعلام واحد لكل نوع سجلّ في الصفحة، ويقرأ المحذوفين |
| `backend/app/Domain/Audit/Contracts/HasAuditDisplayName.php` | جديد — لمن لا يكفيه `name/label/title/code` |
| `backend/app/Application/Api/V1/Resources/ActivityLogCollection.php` | جديد — التجميع. يُهيَّأ في الباني لا في `toArray()`، لأن المسار المرقَّم لا يمرّ بالثانية |
| `backend/.../ActivityLogResource.php` | `value_labels` و`property_labels` |
| `backend/.../AuditAttributeLabels.php` | **١١٠ عمود كانت بلا اسم عربي** — انظر أدناه |
| `frontend/.../activity_log_entry.dart` | `valueLabels` و`valueLabelFor` و`permissionLabelFor` |
| `frontend/.../activity_log_page.dart` | ترجمة الخادم تسبق `_show`، والتاريخ يُنسَّق على الجهاز |

**الاختبارات:** `AuditValueLabelsTest` (١٤)، `AuditAttributeLabelsTest` (٢، وهو الحارس المفقود من §٦)،
خمسة في `ActivityLogTest` منها سقفٌ لعدد الاستعلامات، وسبعة في التطبيق. الحزمتان خضراوان بالكامل.

### ما وجده الحارس المفقود

اختبار `AuditAttributeLabels` الذي لم يكن موجوداً كشف **١١٠ أعمدة تصل شاشة السجلّ بلا اسم عربي** —
المخازن والموردون وأوامر الشراء والدفعات وتتبّع التكلفة كلّها بلا مدخلٍ واحد. أي أن نصف الشاشة
كان سيبقى إنجليزياً حتى بعد تعريب القيم. أُضيفت كلّها، والاختبار الآن يمنع تكرارها.

كما تبيّن أن `password` و`remember_token` **لا يصلان السجلّ أصلاً** —
`activitylog.default_except_attributes` يحذفهما لكل الموديلات. الاختبار يقرأ ذلك الإعداد بدل
تكراره.

### ما لم يُنفَّذ عمداً

`created_by` و`reviewed_by` و`recorded_by` و`received_by` تحمل معرّفات مستخدمين لكنها **لا
تُترجَم إلى أسماء**. القاعدة تشتقّ العلاقة من اسم العمود (`customer_id` ← `customer()`)، وهذه
الأعمدة لا اسم علاقةٍ يُشتقّ منها. تخمين `creator()` قاعدةٌ تعمل حتى تتوقّف. أسماؤها العربية
موجودة («أنشأها»، «سجّلها») والمعرّف يُعرض كما هو. مُدرَج في `BACKLOG.md`.
