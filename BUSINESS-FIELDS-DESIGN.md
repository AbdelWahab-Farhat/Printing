# مجالات العمل — المواصفة

> **مُنفَّذ.** كل ما دونه أدناه هو ما بُني فعلاً — الجداول والـ API والصلاحيات والشاشة والربط.
> ما يُؤجَّل يُسجَّل في [BACKLOG.md](BACKLOG.md).

---

## ١. لماذا

الزبون له محل، والمحل له مجال عمل: شحن، تجارة عامة، بيع ملابس…

اليوم لا نعرف من نبيع له. تخزين المجال يجعل أسئلة مثل «كم عميلاً في الشحن؟» و«ما المقاس الذي تطلبه محلات الملابس؟» قابلة للإجابة بعد ستة أشهر من التشغيل، بلا أن نعود لنسأل العملاء واحداً واحداً. القيمة كلها في **التراكم**، ولهذا يُخزَّن الآن حتى قبل أن توجد شاشة تقارير تستهلكه.

**مجال العمل صفة المحل لا العميل.** عميل واحد قد يملك محل ملابس ومحل عطور — وربطه بالعميل يجبرنا على اختيار أحدهما.

---

## ٢. الجدول

`business_fields` — جدول مرجعي صغير، تُحرِّره الإدارة.

| العمود | النوع | ملاحظة |
| --- | --- | --- |
| `id` | bigint | |
| `name` | string(100) **unique** | «شحن»، «بيع ملابس». الفريد يتجاهل المحذوف كعادة بقية الجداول |
| `is_active` | boolean · افتراضي `true` | مجال توقّفنا عن استعماله يختفي من قوائم الاختيار ولا يكسر المحلات المرتبطة به |
| `sort_order` | int · افتراضي `0` | ترتيب القائمة أمام الموظف؛ الأكثر شيوعاً أولاً بدل الترتيب الأبجدي |
| `timestamps` + `deleted_at` | | القاعدة الثابتة: كل نموذج soft delete + سجل تدقيق |

وعمود واحد على الجدول الموجود:

```php
// customer_shops
$table->foreignId('business_field_id')->nullable()->constrained()->nullOnDelete();
```

**nullable إجبار لا اختيار:** المحلات المسجَّلة اليوم ليس لها مجال، وأي عمود NOT NULL يعني اختراع قيمة لها.

---

## ٣. الـ API

كلها تحت `/api/v1/business-fields`، بنفس مغلّف الرد وترقيم الصفحات المعتاد.

| الفعل | المسار | الصلاحية |
| --- | --- | --- |
| قائمة (`search`، `is_active`، `per_page`) | `GET /business-fields` | `business_fields.view` |
| واحد | `GET /business-fields/{id}` | `business_fields.view` |
| إضافة | `POST /business-fields` | `business_fields.manage` |
| تعديل | `PUT /business-fields/{id}` | `business_fields.manage` |
| تفعيل/إيقاف | `PATCH /business-fields/{id}/activation` | `business_fields.manage` |
| حذف | `DELETE /business-fields/{id}` | `business_fields.manage` |
| السجل | `GET /business-fields/{id}/logs` | `logs.view` |

وتُضاف `business_field_id` إلى ما يقبله إنشاء العميل وتعديله داخل كل عنصر من `shops[]`، ويُرجِع `CustomerShopResource` الحقل كاملاً:

```json
"business_field": { "id": 3, "name": "بيع ملابس" }
```

كائن لا `id` مجرد: الشاشة تعرض الاسم، وإرسال الرقم وحده يجبر كل عميل على تحميل الجدول كله ليترجمه.

### الحذف

**الحذف يُرفض إذا كان المجال مستعملاً** — 422 برسالة «لا يمكن حذف مجال عمل مرتبط بمحلات، أوقفه بدل ذلك». مجال بلا محلات يُحذف حذفاً ناعماً كالمعتاد.

السبب: المجال بيانات يشير إليها غيره، والحذف الناعم يُخفي الصف من الـ API بينما يبقى المحل مشيراً إلى شيء لا يستطيع أحد عرضه. الإيقاف يفعل ما يريده المستخدم فعلاً — «لا تعرضه في القائمة» — دون كسر ما مضى.

---

## ٤. الصلاحيات

حالتان جديدتان في [PermissionName](backend/app/Domain/Identity/Enums/PermissionName.php):

| القيمة | اللبل | المجموعة |
| --- | --- | --- |
| `business_fields.view` | عرض مجالات العمل | مجالات العمل |
| `business_fields.manage` | إضافة وتعديل مجالات العمل | مجالات العمل |

**العرض يُمنح للجميع مبدئياً** كما طلبت: صلاحية حقيقية محروسة على المسار — لأن أي موظف يفتح نموذج عميل يحتاج القائمة ليختار منها — لكن migration يمنحها لكل دور موجود في قاعدة البيانات وقت الترقية، و`RoleSeeder` يضيفها لدور «موظف». الإدارة تبقى للمدير وحده حتى تقرر غير ذلك.

هذا أفضل من ترك المسار بلا حراسة: اليوم «الكل» و«بلا صلاحية» متساويان، ويوم تريد سحبها من دور واحد يكون المكان جاهزاً بدل migration جديد وتعديل مسار.

---

## ٥. أين يسكن الكود

داخل وحدة `Customer` — الموديل في `app/Domain/Customer/Models/BusinessField.php`، والأفعال والاستعلام بجانب أفعال العملاء، والواجهة عبر `CustomerService` كباب واحد للوحدة كما هو الحال في بقية الوحدات.

**البديل المرفوض:** وحدة `Business` مستقلة على غرار `Delivery`. المدن استحقت وحدتها لأن الطلبيات تستهلكها من خارج وحدة العملاء؛ مجال العمل لا يعرفه أحد خارج محل العميل، ووحدة كاملة لجدول مرجعي واحد ملفات أكثر بلا فاصل حقيقي.

---

## ٦. الاختبارات

بالترتيب: الاختبار الفاشل أولاً، وكل اختبار مقسوم Arrange - Act - Assert.

- `BusinessFieldTest` — القائمة، الإنشاء، التعديل، التفعيل، الحذف، تفرّد الاسم، ورفض الحذف عند الارتباط بمحل.
- الصلاحيات — 403 بلا `business_fields.manage`، ونجاح العرض لمن يملك `view` فقط.
- ربط المحل — إنشاء عميل بمحل له مجال، تعديل المجال، وتصفيره.
- السجل — `GET /business-fields/{id}/logs` يعرض من غيّر ماذا.
- `ModelConventionsTest` يمرّ تلقائياً (soft delete + Auditable + حالة في `AuditSubject`).

---

## ٧. القرارات المحسومة

| السؤال | القرار |
| --- | --- |
| النطاق | باك إند + شاشة إدارة في التطبيق + قائمة اختيار في كل صف محل داخل نموذج العميل |
| واحد أم متعدد | **مجال واحد للمحل**. التحوّل إلى متعدد لاحقاً migration واحد وجدول ربط، والشاشات تتغيّر قليلاً |
| القائمة الأولية | مزروعة كما هي أدناه، والإدارة تعدّلها من الشاشة |

القائمة المزروعة، بالترتيب:

شحن وتوصيل · تجارة عامة · بيع ملابس · مطاعم ومقاهي · حلويات ومخابز · عطور ومستحضرات تجميل · صيدليات · إلكترونيات وهواتف · بقالة وسوبرماركت · مكتبات وقرطاسية · أخرى

**لا شيء في الكود يعرف اسم مجال بعينه**، فتغييرها كلها لا يكسر شيئاً.

## ٧أ. الشاشة

- **مجالات العمل** في القائمة الجانبية (بلا حراسة — `business_fields.view` ممنوحة للجميع).
- بحث + بطاقة لكل مجال تحمل الاسم و«٣٤ محلاً» ومفتاح تشغيل/إيقاف.
- النقر على البطاقة يفتح ورقة التعديل، وفيها زر الحذف حين لا يكون المجال مرتبطاً بمحلات — وحين يكون، جملة تشرح لماذا الإيقاف هو المطلوب.
- من لا يملك `business_fields.manage` يرى القائمة بلا مفتاح ولا زر ولا ورقة.
- في نموذج العميل: قائمة **مجال العمل** تحت اسم كل محل، أولها «غير محدد»، تُحمَّل مرة واحدة للنموذج كله.

---

## ٨. المؤجَّل

- **التقارير** — «كم عميلاً في كل مجال» وتصفية العملاء بالمجال. الغرض من التخزين، لكنه بلا معنى قبل تراكم بيانات؛ ينزل في [BACKLOG.md](BACKLOG.md).
- **مجال للعميل نفسه** بجانب مجال المحل. لا حاجة له ما دام لكل محل مجاله.
