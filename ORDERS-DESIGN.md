# الطلبيات — تصميم آلة الحالات

> المخطط المعتمد للتنفيذ. **كل القرارات محسومة.**
> ما يُؤجَّل يُسجَّل في [BACKLOG.md](BACKLOG.md) ولا يُنفَّذ الآن.

---

## ١. الحالات

| # | القيمة (في الكود) | الاسم بالعربي | النوع |
| --- | --- | --- | --- |
| 1 | `new` | جديدة | بداية |
| 2 | `designing` | قيد التصميم | تنفيذ |
| 3 | `printing` | قيد الطباعة | تنفيذ |
| 4 | `ready` | جاهزة | تنفيذ |
| 5 | `shortage` | نواقص | تنفيذ |
| 6 | `office_pickup` | استلام مكتب | تسليم |
| 7 | `out_for_delivery` | جاري التوصيل | تسليم |
| 8 | `delivered` | تم الاستلام | **نهائية** |
| 9 | `returned_courier` | راجع لدى المندوب | رواجع |
| 10 | `returned_carrier` | راجع لدى شركة التوصيل | رواجع |
| 11 | `returned_office` | راجع مكتب | رواجع |
| 12 | `cancelled` | ملغاة | **نهائية** |

---

## ٢. خريطة الانتقالات المسموحة

**مصدر الحقيقة الوحيد.** أي انتقال غير مذكور هنا يُرفض بـ 422 ورسالة عربية.

| من | إلى (المسموح) |
| --- | --- |
| جديدة | قيد التصميم · قيد الطباعة — **ولا شيء غيرهما** |
| قيد التصميم | قيد الطباعة · ملغاة |
| قيد الطباعة | جاهزة · نواقص · **قيد التصميم** · ملغاة |
| نواقص | قيد الطباعة · جاهزة · ملغاة |
| جاهزة | **التسليم** (الخادم يقرر: استلام مكتب \| جاري التوصيل) · نواقص · قيد الطباعة · ملغاة |
| استلام مكتب | تم الاستلام · ملغاة |
| جاري التوصيل | تم الاستلام · راجع لدى المندوب · راجع لدى شركة التوصيل · راجع مكتب · ملغاة |
| راجع لدى المندوب | راجع لدى شركة التوصيل · راجع مكتب · جاري التوصيل · ملغاة |
| راجع لدى شركة التوصيل | راجع مكتب · جاري التوصيل · ملغاة |
| راجع مكتب | جاهزة (إعادة إرسال) · ملغاة |
| تم الاستلام | — نهائية، لا رجعة |
| ملغاة | — نهائية، لا رجعة |

```text
جديدة ─┬─→ قيد التصميم ⇄ قيد الطباعة ─┬─→ جاهزة ──(الخادم يقرر)─┬─→ استلام مكتب ──→ تم الاستلام
       └────────────────────↗           └─→ نواقص                 └─→ جاري التوصيل ─→ تم الاستلام
                              ↑___________↕ (نواقص ⇄ طباعة، نواقص → جاهزة)              │
                                                                                        ↓
                        جاهزة ←── راجع مكتب ←── راجع لدى شركة التوصيل ←── راجع لدى المندوب
                                      (وكل واحدة منها تقدر ترجع مباشرة إلى جاري التوصيل)

الإلغاء: متاح من أي حالة غير نهائية **عدا «جديدة»** — طلبية لم يبدأها أحد على بُعد ضغطتين من أن تبدأ.
```

**«استلام مكتب» لا تخرج إلى الرواجع.** الطلبية التي لم يأتِ صاحبها تبقى «استلام مكتب» حتى يستلمها أو تُلغى — لا مندوب فيها ولا شركة توصيل.

---

## ٣. القرارات المحسومة

| # | القرار | الاختيار |
| --- | --- | --- |
| ١ | مخرج «نواقص» | نواقص → قيد الطباعة **أو** جاهزة |
| ٢ | تمييز «استلام مكتب» | عمود `fulfilment_type` على جدول **المدن** (`delivery` \| `office_pickup`) |
| ٣ | محور الدفع | **مؤجَّل** → [BACKLOG](BACKLOG.md) |
| ٤ | سجل الانتقالات | جدول مستقل `order_status_transitions` |
| ٥ | الرجوع من الطباعة للتصميم | **مسموح** — الموظف يخطئ، والمسار يكمل تصميم → طباعة كالمعتاد |
| ٦ | العميل ما جاء للمكتب | تبقى «استلام مكتب» |
| ٧ | المرتجع بعد الاستلام | **غير مسموح** — لا توجد سياسة إرجاع حالياً. «تم الاستلام» نهائية |
| ٨ | حالة «معلّقة/مؤجلة» | **لا تُضاف الآن** → [BACKLOG](BACKLOG.md) |
| ٩ | الصلاحيات | **صلاحية لكل حالة** (تفصيلها في القسم ٦) |
| ١٠ | سعر التصميم | **سعر موحد للطلبية** مهما تعدّدت النسخ |
| ١١ | تعديل البنود | **مفتوحة حتى «قيد الطباعة» ضمناً، ومقفلة من «جاهزة» فصاعداً** (تعديل لاحق: الكمية تُصحَّح والمكبس يدور). والتصاميم خط آخر: تُقفل عند بدء الطباعة. التعديل عند الاستلام → [BACKLOG](BACKLOG.md) |
| ١٢ | تغيير المدينة | مسموح دائماً **عدا** جاري التوصيل · راجع لدى المندوب · راجع لدى شركة التوصيل · والحالتين النهائيتين |
| ١٣ | الخصم | مسموح، خلف صلاحية `orders.discount` — والخادم يرفض الطلب ممن لا يملكها |
| ١٤ | رقم الطلبية | عمود `code` مستقل — **أرقام تسلسلية بلا حروف**: 1، 2، 3… |
| ١٥ | تصاميم العميل | **مكتبة دائمة — مبنية ومُسلَّمة بالفعل** في commit `8b56913`. لا يُعاد بناؤها |
| ١٦ | اتجاه المكتبة | **من المكتبة إلى الطلبية فقط.** لا حفظ عكسي من طلبية إلى المكتبة |
| ١٧ | ربط التصميم بالطلبية | **إشارة لا نسخ** — `order_designs.customer_design_id`. تصحيح لاقتراح سابق، السبب في القسم ٧ |

---

## ٤. تصحيحات على الترتيب الأصلي

1. **«نواقص» كانت طريقاً مسدوداً** — ذُكر الدخول إليها فقط. حُسم في القرار ١.
2. **الرواجع ليست سلسلة إجبارية** — مندوبنا داخل طرابلس يرجّع الطلبية للمكتب مباشرة بلا شركة توصيل، فالقفز مسموح.
3. **«راجع مكتب» كان مخرجه الإلغاء فقط** — أُضيفت إعادة الإرسال (راجع مكتب → جاهزة).
4. **«راجع لدى المندوب» و«راجع لدى شركة التوصيل» مستحيلتان على طلبية استلام مكتب** — حُسم في القرار ٦.
5. **الإلغاء** كان في نهاية سلسلة الرواجع فقط — والصحيح أنه متاح من أي حالة غير نهائية، **عدا «جديدة»**: طلبية لم يبدأها أحد مخرجاها التصميم والطباعة، وثالثٌ بينهما كان ينافس الاثنين اللذين يهمّان. (تعديل لاحق، مطبَّق في `allowedNext()` وفي اختباره.)
6. **«تم الاستلام»** ذُكرت بالمرور، وهي الحالة النهائية الناجحة للنظام كله.
7. **مشكلة تقنية حقيقية:** قاعدة البيانات اليوم لا تعرف أي مدينة هي «استلام مكتب» إلا **بالاسم** — `إستلام مكتب(قرجي)` و`إستلام مكتب(ولي العهد)`. أي تعديل إملائي من الأدمن كان سيكسر القرار التلقائي بصمت. حُلّت بعمود `fulfilment_type`.

**صحيح في الترتيب الأصلي:** «قيد التصميم لازم تمر على قيد الطباعة» — لا يوجد قيد التصميم → جاهزة إطلاقاً.

---

## ٥. البنية: إضافة حالة جديدة بدون تخريب الباقي

`enum OrderStatus` يحمل بنفسه خريطة الانتقالات وصلاحيتها:

```php
public function allowedNext(): array
{
    return match ($this) {
        self::New      => [self::Designing, self::Printing, self::Cancelled],
        self::Printing => [self::Ready, self::Shortage, self::Designing, self::Cancelled],
        // …
    };
}

public function permission(): PermissionName { /* … */ }
```

إضافة حالة = `case` جديد + إدراجه في الأسطر التي تخصه + صلاحيته. واختبار يمشي على **كل** الحالات ويؤكد أن ما تغيّر هو ما قُصد فقط.

فوقها ثلاث حمايات:

- `POST /orders/{order}/status` يرفض أي انتقال خارج الخريطة (422، رسالة عربية).
- `OrderResource` يرجّع `available_transitions` — **مفلترة بصلاحيات المستخدم الحالي**، فالتطبيق يرسم الأزرار المسموحة فقط، تماماً كما في «Gate the app on what the server says you may do».
- `order_status_transitions` يسجل كل حركة: من، إلى، مَن، متى، السبب.

**لماذا لا جدول حالات في قاعدة البيانات؟** نصف الحالات لها **سلوك** لا يُخزَّن كصف: «استلام مكتب» يقرره الخادم من المدينة، والإلغاء يتطلب سبباً، والنهائيات تُقفل، ولكل حالة صلاحية. جدول كهذا يعطي وهم المرونة ويترك السلوك في الكود على أي حال، ويخالف قاعدة «Enums لكل حالة» في `backend/RULES.md`.

---

## ٦. الصلاحيات — واحدة لكل حالة

| الصلاحية | ماذا تتيح |
| --- | --- |
| `orders.view` | عرض الطلبيات وتفاصيلها |
| `orders.manage` | إنشاء الطلبية وتعديل بياناتها وبنودها |
| `orders.discount` | وضع خصم على الطلبية |
| `orders.designs.manage` | رفع نسخ التصميم واعتمادها أو رفضها |
| `orders.status.designing` | التحويل إلى قيد التصميم |
| `orders.status.printing` | التحويل إلى قيد الطباعة |
| `orders.status.ready` | التحويل إلى جاهزة |
| `orders.status.shortage` | التحويل إلى نواقص |
| `orders.status.dispatch` | **التسليم** — استلام مكتب أو جاري التوصيل |
| `orders.status.delivered` | تأكيد تم الاستلام |
| `orders.status.returned_courier` | تسجيل راجع لدى المندوب |
| `orders.status.returned_carrier` | تسجيل راجع لدى شركة التوصيل |
| `orders.status.returned_office` | تسجيل راجع مكتب |
| `orders.status.cancelled` | إلغاء الطلبية |

> ⚠️ **استثناء واحد مقصود:** «استلام مكتب» و«جاري التوصيل» تشتركان في صلاحية `orders.status.dispatch` بدل صلاحيتين. السبب: الموظف لا يختار بينهما — الخادم يقرر من المدينة. لو فُصلتا، لكان نفس الزر يعمل مع مدينة ويفشل مع أخرى بلا سبب يفهمه الموظف. قل لي لو تفضّل فصلهما رغم ذلك.

الرواجع الثلاث بقيت منفصلة عمداً: هذه **يختارها** الموظف، وموظف المكتب الذي يسجّل «راجع مكتب» ليس بالضرورة منسّق التوصيل الذي يسجّل الاثنتين الأخريين.

الأدوار تُركَّب من هذه الصلاحيات من شاشة الأدوار — لا أدوار جاهزة في الكود، تماماً كالقاعدة الحالية.

---

## ٧. الجداول والأعمدة

### `orders`

| العمود | النوع | ملاحظة |
| --- | --- | --- |
| `code` | string، فريد | رقم الطلبية — **أرقام تسلسلية بلا حروف** (قرار ١٤). يُخصِّصه الخادم وغير قابل للإرسال في الطلب |
| `customer_id` | FK | |
| `customer_shop_id` | FK nullable | أي محل من محلات العميل |
| `city_id` · `region_id` | FK | المنطقة إجبارية متى كانت `city.is_region_required` |
| `city_name` · `region_name` | string | **لقطة** — تحمي العنوان لو حُذفت المدينة لاحقاً |
| `fulfilment_type` | enum | لقطة من المدينة وقت الطلب |
| `status` | enum | `OrderStatus`، مفهرس |
| `design_source` | enum | `none` \| `customer` (تصميم العميل) \| `in_house` (نحن نصممه) |
| `recipient_name` · `recipient_phone` | string nullable | تقعان على بيانات العميل عند تركهما |
| `address_details` · `notes` | text nullable | |
| `items_total` · `design_fee` · `delivery_price` · `discount` · `grand_total` | decimal(12,2) | `delivery_price` **لقطة** من المدينة — تعديل التسعيرة لاحقاً يجب ألا يعيد كتابة طلبية قديمة |
| `shipping_company` · `tracking_number` · `courier_name` | string nullable | |
| `placed_at` · `design_started_at` · `printing_started_at` · `ready_at` · `dispatched_at` · `delivered_at` · `returned_at` · `cancelled_at` | timestamp nullable | يملؤها الخادم عند الانتقال — للتقارير السريعة |
| `cancellation_reason` | text nullable | |
| `created_by` | FK users | |

> لقطة `city_name` و`delivery_price` تحلّ الملاحظة المعلّقة في `DeliveryService::deleteCity()` — طلبية تشير إلى مدينة محذوفة تحتفظ بعنوانها وسعرها.

### `order_items`

`product_id` · `product_variant_id` · و**لقطات**: `product_name` · `variant_label` · `pricing_unit` · `quantity` decimal(12,3) · `unit_price` decimal(12,3) · `line_total` decimal(14,3) · `notes`.

السعر يأتي من `CatalogService::quote()` وليس من حساب في التطبيق — نفس الكود الذي يُري العميل السعر هو الذي يكتبه في الطلبية.

**مقفلة للتعديل** من «جاهزة» فصاعداً (قرار ١١ بعد تعديله): الكمية تُصحَّح والمكبس لا يزال يدور، أما بعد أن صارت الأكياس على الرف فتعديل ما تقوله الطلبية يجعل الفاتورة تخالف المخزن.

### `customer_designs` — مبني بالفعل، لا يُعاد بناؤه

نزل في commit `8b56913` «Keep a customer's designs, so an order can point at one later» — بُني تحديداً لتشير إليه الطلبيات.

الأعمدة القائمة: `customer_id` · `disk` · `path` · `original_filename` · `mime_type` · `kind` (`image` \| `pdf`) · `size_bytes` · `checksum` (sha256) · `width_px` · `height_px` · `label` · `notes`. مساراته `/customers/{customer}/designs` خلف `customers.view` / `customers.manage`.

ثلاث خصائص فيه تحكم تصميم الطلبيات:

- **الملف لا يُحذف أبداً.** حذف تصميم يخفي الصف ويترك الكائن مكانه — بنص المهاجرة: «طلبية طُبعت العام الماضي يجب أن تظل قادرة على إظهار ما طُبع».
- **قرص خاص** (`local` ثم S3 بصلاحية خاصة)، و`url()` يوقّع رابطاً ينتهي. تصميم العميل ملكه، لا ملف تسويقي عام.
- **الرفع خامل التكرار** على `checksum`: إعادة المحاولة بعد انقطاع تجد الصف موجوداً بدل نسخة ثانية.

### `order_designs` — «السجلات» التي وُصفت

`order_id` · `customer_design_id` (FK **مطلوب**) · `version` (1، 2، 3…) · `status` (`proposed` \| `approved` \| `rejected`) · `rejection_reason` · `notes` · `reviewed_at` · `reviewed_by`.

**لا أعمدة ملف هنا، ولا نسخ للملف** (قرار ١٧). كنت اقترحت نسخ الملف إلى الطلبية خوفاً من أن يتغيّر تحت طلبية قديمة — **والاقتراح كان خطأ**: الجدول القائم لا يحذف ملفاً أبداً، و`path`/`checksum` لا يُعدَّلان (ملف جديد = صف جديد بحكم فهرس الـ checksum). فالخطر الذي كنت أتحوّط له غير قائم أصلاً، والنسخ كان سيضاعف تخزين ملفات طباعة تصل إلى ٢٥ ميغابايت مقابل لا شيء. المتغيّر الوحيد هو `label`/`notes` وهو تجميلي.

**لا عمود سعر هنا** (قرار ١٠): السعر موحّد للطلبية ويقع على `orders.design_fee`.

**كل تصميم يمر عبر المكتبة** (قرار ١٦): «تضيف في العميل وخلاص». حتى التصميم الذي نصنعه نحن يُرفع إلى مكتبة العميل — هو ملكه — ثم يُربط بالطلبية، و`orders.design_source = in_house` هو ما يقول إننا صنعناه وإن `design_fee` مستحق. فلا رفع مباشر على الطلبية ولا أعمدة ملف مكرّرة.

الطلبية تبقى في «قيد التصميم» بينما تتوالى النسخ — فلا نخترع ثلاث حالات لشيء هو في جوهره جدول.

### `order_status_transitions`

`order_id` · `from_status` (فارغ لأول انتقال) · `to_status` · `reason` · `user_id` · `created_at`.

### كلها معاً

`SoftDeletes` + `Auditable` + حالة في `AuditSubject` + `GET /orders/{order}/logs` يشمل بنود الطلبية وتصاميمها وانتقالاتها — حسب `backend/RULES.md` §10.

---

## ٨. كود الطلبية

`AllocateOrderIdentifier` على نمط `AllocateCustomerIdentifier`: يحجز المُعرِّف من تسلسل الجدول **قبل** الإدراج، فيحمل الصف كوداً نهائياً فريداً، ويبقى العمود NOT NULL، ولا تتصادم طلبيتان متزامنتان.

الفرق الوحيد عن العملاء والمنتجات أنه **بلا حرف بادئ**: `1` لا `O1`.

> الأكواد تتبع المُعرِّفات، وقد **تقفز رقماً** بعد معاملة تراجعت — هي مُعرِّفات لا عدّاد. هذا سلوك موثَّق أصلاً في `backend/RULES.md` §11 ويسري على العملاء والمنتجات منذ اليوم الأول.

**لماذا عمود وليس `id` مباشرة؟** ليكون قابلاً للفهرسة والبحث والتصدير مستقلاً، وليبقى تغيير الصيغة لاحقاً (سنة، فرع) تعديلاً في Action واحد بدل أن يكون مستحيلاً لأن رقم الطلبية هو المفتاح الأساسي نفسه.

---

## ٩. افتراضات مطبَّقة ما لم تعترض

هذه قرارات صغيرة اتخذتها لأكمل، وكلها سهلة التغيير:

1. **سبب الانتقال إجباري في الإلغاء فقط**، واختياري في باقي الانتقالات (بما فيها الرواجع والرجوع للتصميم).
2. **الخصم مبلغ ثابت** لا نسبة مئوية — رقم مضبوط بلا خلاف تقريب. النسبة → [BACKLOG](BACKLOG.md).
3. **الدخول إلى «قيد الطباعة» يتطلب نسخة تصميم معتمدة** متى كان `design_source` ليس `none`.
4. **تغيير المدينة يعيد لقطة سعر التوصيل** من المدينة الجديدة، ويعيد حساب الإجمالي.
5. **تغيير المدينة وهي في حالة تسليم** (استلام مكتب ↔ جاري التوصيل) ينقل الحالة تلقائياً إلى ما يوافق المدينة الجديدة، ويُسجَّل في `order_status_transitions` بسبب «تغيير المدينة».
6. **الطلبية تقبل عدة منتجات** بكميات ومقاسات مختلفة.

---

## ١٠. خطة التنفيذ

بالتسلسل، كل خطوة باختبارها الفاشل أولاً (TDD · Arrange-Act-Assert):

### شريحة أ — تمهيد على ما هو قائم

1. ~~Migration يضيف `fulfilment_type` للمدن~~ — **مُنجزة.** ١٢ اختباراً في `CityFulfilmentTypeTest`، والسويت ٤٧٥ خضراء.
2. ~~مكتبة تصاميم العميل~~ — **مُنجزة** في commit `8b56913`.

### شريحة ب — قلب آلة الحالات ✅

1. ~~`OrderStatus` · `FulfilmentType` · `DesignSource` · `OrderDesignStatus` enums~~ — **مُنجزة**، ٣٢ اختباراً في `OrderStatusTest` تغطي المصفوفة كاملة و١٤٤ زوج حالة.
2. ~~صلاحيات `orders.*`~~ — **مُنجزة**، ١٤ صلاحية.
3. ~~Migrations الأربع~~ — **مُنجزة**.
4. ~~النماذج + Factories + `AuditSubject`~~ — **مُنجزة**، `ModelConventionsTest` يمر.

### شريحة ج — السلوك والواجهة ✅

1. ~~`Domain/Order`~~ — **مُنجزة**: `AllocateOrderIdentifier` · `ResolveOrderDestination` · `AddOrderItem` · `SyncOrderItems` · `RecalculateOrderTotals` · `RecordStatusTransition` · `ChangeOrderStatus` · `CreateOrder` · `UpdateOrder` · `AddOrderDesign` · `ReviewOrderDesign` · `OrderService`.
2. ~~الاستثناءات~~ — **مُنجزة**، ١٥ استثناءً.
3. ~~`Application/Api/V1`~~ — **مُنجزة**: ٨ مسارات، ٥ FormRequests، ٤ Resources، `/logs`.
4. ~~التحقق~~ — **٥٧١ اختباراً خضراء** · `pint` نظيف · `scramble:analyze` نظيف.

---

## ١١. ما يبقى يدوياً

- **قاعدة بيانات التطوير لم تُهاجَر.** الاختبارات تبني قاعدتها من الصفر فالمهاجرات مثبتة، لكن `printing_bags` لم تُلمس: `php artisan migrate`.
- **`RoleSeeder` لم يُمسّ عمداً.** صلاحيات الطلبيات الأربع عشرة موجودة في الكتالوج، ولا دور جاهز يحملها — تُركَّب الأدوار من شاشة الأدوار كما نصّ القسم ٦. أضفها لدور «موظف» من الواجهة، أو قل لي وأضعها في الـ Seeder.
- **لا واجهة Flutter بعد.** هذا الباك إند فقط.

---

آخر تحديث: ٢ أغسطس ٢٠٢٦
